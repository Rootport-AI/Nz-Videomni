# 実バックエンド（real backend）通し確認チェックリスト

- 作成: 2026-07-15（グループ3・項目13の最終ゲート用手順書として新規作成）
- 対象: `Nz-LTX23-frontend-AviUtl2`（本リポジトリ）とAviUtl2実機
- 本書の位置づけ: **手順書のみ**。本書を作成したセッションではバックエンドの起動・実際の生成・デプロイは一切行っていない。オーナーが実GPU環境で後日まとめて実行するための案内である。
- バックエンド（`Nz-LTX23-backend`）は**凍結方針**のため、本書の作成にあたっては変更していない（既存手順を読み取って引用・参照するのみ）。ただし凍結は絶対の禁止ではなく、V2V結合（Join）機能の復活のために2026-07-21に限定解除してAPIを拡張した実績がある（[`API_REFERENCE.md`](API_REFERENCE.md) §3.17・§3.18・§6）。
- 更新: 2026-07-19（オーナー申告に基づくマトリクス更新。「mockでしか検証していない」という当初の前提を是正——詳細は§1.1。2026-07-19、後日明言のなかった残り6細部の最終判定を反映——§4.3/§4.6/§4.8/§4.9・第6節。同日さらに、reuseIfPresentリロード後保険経路・危険ゾーン〔N4〕表示有無の合格とAPIキーバッジ〔N13〕のα版スコープ外クローズを反映——§4.7/§4.8・第6節）。2026-07-20、右クリック再設計第2段階（[`DEVLOG.md`](DEVLOG.md) §41）でminimal/smallプリセットが481フレーム化されたことを受け、§4.1に確認行を追加。2026-07-27、§4.5のcutoutRange併用を「未配線につき検証対象外」として決着（§1.1・§4.5・第6節を整合。[`PENDING_TASKS_CLOSED.md`](PENDING_TASKS_CLOSED.md) §3-48）。2026-07-28、NAG（Normalized Attention Guidance）フロントエンド追随（[`DEVLOG.md`](DEVLOG.md)の該当節）に伴い§4.11を新設。2026-07-30、V2Vリボン範囲トリム（§1-6）とバッチi2v-long（§1-7）の実装完了に伴い§4.12・§4.13を新設（既存項目のチェック済み欄を書き換えないよう、§4.5の内側の細目である前者も独立節として追加した。[`DEVLOG.md`](DEVLOG.md) §54）。2026-07-31、§4.13が全項目合格しクローズ（[`PENDING_TASKS_CLOSED.md`](PENDING_TASKS_CLOSED.md) §3-57）。2026-08-01、§4.12が全5項目合格しクローズ（同§3-59。⑤＝IC-LoRA参照動画への拡張分は同日追加した項目。[`DEVLOG.md`](DEVLOG.md) §56）。項目別結果表と総合判定も同日に整合させた。2026-08-05、§4.10（ドラッグ＆ドロップ5項目）がオーナー実機で全項目合格しクローズ（`Docs/PENDING_TASKS.md` §2-1からは除去済み）。これにより§4.1〜§4.13すべてが全項目合格した。2026-08-09、Stage-2のクリップ長の選択（`stage2_window`）とOutpainting（動画キャンバス拡張）を実装したが、**本書には項目を追加していない**——どちらも同日中に専用の実機ゲート（バックエンド`outputs/stage2_window_optin/RUNBOOK.md`・`uploads/_outpaint_verify/RUNBOOK_W2.md`）とオーナーの実機確認まで通り、本書が受け皿とする「未消化の細部」が残らなかったためである。合格記録は[`PENDING_TASKS_CLOSED.md`](PENDING_TASKS_CLOSED.md) §3-68〜§3-70、測定値の正本はバックエンド[`VERIFICATION_LOG.md`](../../Nz-LTX23-backend/Docs/VERIFICATION_LOG.md) §53・§54。2026-08-10、Retakeを実装。本書には項目を追加していない——**同日中にオーナーの目視・実機確認が全項目合格し**、本書が受け皿とする「未消化の細部」が残らなかったためである。合格記録は[`PENDING_TASKS_CLOSED.md`](PENDING_TASKS_CLOSED.md) §3-73と[`DEVLOG.md`](DEVLOG.md) §68・§69・§71、実GPUゲートの測定値はバックエンド[`VERIFICATION_LOG.md`](../../Nz-LTX23-backend/Docs/VERIFICATION_LOG.md) §55（最後まで残っていた最小窓73フレームの仕様判断も同日に「73フレーム維持・注意文で伝える」で確定し、テーマは完結した）。**同日、台帳の運用が変わり、全項目が合格して空になった節は見出しごと削除することになった**（`PENDING_TASKS.md`末尾「本台帳の位置づけ」・[`PENDING_TASKS_CLOSED.md`](PENDING_TASKS_CLOSED.md)冒頭）。これにより本書が長く参照してきた`PENDING_TASKS.md` §2-1（実GPU細部）は削除されている——**本書中の§2-1への言及はすべて当時の記録であり、現在の参照先は[`PENDING_TASKS_CLOSED.md`](PENDING_TASKS_CLOSED.md) §3-67である。**

## 1. 目的と前提

### 1.1 目的

**2026-07-18に是正した経緯**: 本書は当初「これまでの開発・検証はすべてmockで行われ、実GPUでの生成E2Eは一度も実施していない」という前提で書かれていたが、これはエージェント側の記録だけを根拠にした誇張だった。正しくは以下のとおり区別される。

- **エージェントによる自動テスト・デプロイ疎通確認は`config.model.backend=mock`（合成クリップを返すダミーの生成エンジン）経由**。これはGPUを占有しない安全策として意図的な選択であり、API・スキーマ・ジョブ管理の疎通確認が目的。
- **オーナー自身は当初から`Nz-LTX23-backend`を`./run.ps1`で起動して検証してきた**。`model.backend: "auto"`（既定値）により、モデル一式が揃っている本機では自動的に`real`バックエンドが選ばれるため、オーナーの検証は最初から実GPUでの本物の生成エンジンを使用している。

2026-07-18、オーナーから「realエンジンで検証済みの機能」の申告があり、Create/Chain両タブの主要機能（T2V・I2V・IC-LoRA・単発A2V・バッチA2V・Chain Clips連結・Chain V2V・chunked_upsample常用）についてrealバックエンドでの合格が確認された（詳細は第4節・第6節、[`DEVLOG.md`](DEVLOG.md) §19・§21、[`PENDING_TASKS_CLOSED.md`](PENDING_TASKS_CLOSED.md) §3-14）。同日、モデル管理（4.8）についても、オーナーの実機ロード操作をサーバーログ（`POST /api/v1/pipeline/load 200`→モデル切替→`LTX worker ready`→切替後モデルでのt2v生成完走）で裏付けて合格を追加した。これによりマトリクス9項目は**9/9すべてで少なくとも基本パスが合格**した。

本書の位置づけは「realでの検証が未実施であることを示す文書」ではなく、**「第4節の実GPU通しマトリクスのうち、オーナー申告・ログ証跡でまだカバーされていない各項目内の個別技術チェックを洗い出し、潰していくための文書」**へ改める。個別技術チェックのうち6点は、オーナーが「後日確認する」と明言した（2026-07-18決定）ため、ブロッカー扱いではなく`Docs/PENDING_TASKS.md` §2-1の「実装済み・テスト待ち」区分に整理していた。**2026-07-19、この6点のうち4点（IC-LoRA強度スライダー・wav自動調整細目・冒頭キーフレームなし品質確認・バッチのnoClobber付番）は「以前のセッションで確認済み」とオーナーが追加申告して合格し、あわせて開始前チェック・予想出力尺一致・reuseIfPresent通常再挿入・出力フォルダ自動作成も合格した**（詳細は`Docs/PENDING_TASKS_CLOSED.md` §3-16・§3-17）。後日確認明言分として残っていたのはChain V2VのcutoutRange併用と短いクリップでのoverlap_frames制約（`kv>=L`）の2点だったが、後者は2026-07-22に機能面合格でクローズし（`Docs/PENDING_TASKS_CLOSED.md` §3-33）、前者は**2026-07-27に「製品UIから実行する経路が無い（未配線）」と判明して検証対象外で決着した**（§4.5、`Docs/PENDING_TASKS_CLOSED.md` §3-48）。**あわせて、後日明言のなかった残り6細部（IC-LoRA 128グリッドUI・A2Vの5スロット全体併用・スタイルLoRA併用・短すぎる音声の事前ブロック・モデル管理の409排他/危険ゾーンunload→再ロード・chunked_upsampleの768p級ストレステスト）も2026-07-19のオーナー最終判定ですべて合格した**（詳細は`Docs/PENDING_TASKS_CLOSED.md` §3-18）。α版リリース前に残項目を解消すべき**最終ゲート**という位置づけ自体は変わらない。

### 1.2 前提条件

本チェックリストの実行前に、以下が満たされていることを確認する。

- [ ] フロントエンドのbuildとAviUtl2向けdeployが完了している（`webui`のビルド成果物が`aux2`へ埋め込み済み、`scripts/deploy.ps1`でプラグインディレクトリへ配置済み）。
- [ ] AviUtl2 **v2.0.54ポータブル**（`D:\For_Videos\AviUtl2\aviutl2_v2.0.54\`）で起動し、初回ロード時の信頼承認ダイアログ（「このプラグイン・スクリプトを信頼して使用する」）を承認済み（`Docs/DEVLOG.md` §4-1参照）。
- [ ] AviUtl2上でNzLTX23パネルが正常に表示され、mockバックエンド経由での基本動作（Create/Chain/Library/設定パネル）が一通り承認済みである。

以上が未完了の場合は、実バックエンド確認より先にそちらを済ませること。

## 2. 起動手順（バックエンド既存手順の引用）

**バックエンド側の手順は変更しない。以下は`Nz-LTX23-backend/README.md`の該当節への案内であり、丸写しはしない。**

### 2.1 backendをrealへ切り替える

`Nz-LTX23-backend/config.yaml`の`model.backend`を確認する（README §1「必要なモデル」表の直後の説明）。

- [ ] `model.backend: "auto"`（既定値）のままでよい場合: `./.venv-engine`のpythonと`engine/worker.py`、および下記モデル一式が揃っていれば自動的に`real`が選ばれる（揃っていなければ`mock`にフォールバックする）。
- [ ] 明示的に`real`を強制したい場合: `config.yaml`の`model.backend`を`"real"`に書き換える（未整備なら起動時にロード失敗する点に注意）。

### 2.2 モデル配置の確認

README §1「必要なモデル（`config.yaml`の`model:`が参照）」の表を参照し、以下9点が`models/`配下に揃っているか確認する（2026-08-03更新。`setup.bat`＝`scripts/install_ltx.ps1`が**3リポジトリ・5回のダウンロード・合計23ファイル・32,912,043,043バイト＝約30.7GiB**を自動取得するため、通常は手動配置は不要）。

**生成の中核（欠けると`_real_available()`がFalseになり`mock`へ落ちる）— 合計 ~28GB**:

- [ ] GGUF transformer (Q4_K_M)（`models/ltx-2.3-gguf/` 直下、~17GB）
- [ ] GGUF Gemma (Q4_K_M)（`models/gemma-3-12b-it-gguf/`、~7.3GB）
- [ ] component VAE / audio / text-projection（`models/ltx-2.3-components/`、~3.9GB）
- [ ] spatial upsampler（`models/ltx-2.3/`、~0.95GB）
- [ ] Gemma tokenizer dir（`models/gemma-3-12b-it-tokenizer/`、~40MB）

**IC-LoRAと前処理器（欠けても`mock`には落ちないが、UIから選んだ瞬間404になる）— 合計 ~2.58GiB**:

- [ ] IC-LoRA 2点（`models/ltx-2.3-ic-lora/{pixel-spatial-upscaler,union-control}/`、~1.22GiB）。`config.yaml`の`ic_loras:`が登録する5アダプタ（`pixel-spatial-upscaler-x2`／`canny-control`／`pose-control`／`depth-control`／`deblur`。中3つは同一のunion-controlファイルを3つの名前で公開したもの）のうち4つの実体。
- [ ] DWPose前処理器 2点（`models/preprocessors/{yolox_l,dw-ll_ucoco_384_bs5}.torchscript.pt`、~0.33GiB）。`pose-control`が参照動画から骨格を起こすときに使う。`engine/preprocess/dwpose.py`が絶対パスで読むため配置は変更不可。
- [ ] IC-LoRA Deblur 1点（`models/ltx-2.3-ic-lora-deblur/ltx-2.3-22b-ic-lora-deblur-0.9.safetensors`、~0.91GiB）。`deblur`アダプタの実体で、前処理は不要。
- [ ] VDA深度前処理器 2点（`models/preprocessors-vda/{video_depth_anything_vits.pth,LICENSE}`、~0.12GiB）。`depth-control`が参照動画から深度を起こすときに使う。`engine/preprocess/depth.py`が絶対パスで読むため配置は変更不可（`LICENSE`はApache-2.0の全文で`.pth`と必ず一緒に置く）。

**取得総量は約33GB（30.7GiB）。** `install_ltx.ps1`の最後に出るPASS/MISSING検証テーブルは**16項目**で、上記4種類も名指しで表示する。ここが全項目PASSであることが、本節の確認としてはもっとも確実である（[`PENDING_TASKS_CLOSED.md`](PENDING_TASKS_CLOSED.md) §3-36）。詳細な配置規約・自動認識の仕組みはREADME §1「追加のtransformer GGUF / LoRAを配置する」節を参照。

### 2.3 GPU要件

README §1「必要なもの」に記載のとおり、NVIDIA CUDA GPU（VRAM 16GB推奨・CUDA 12.8/cu128対応ドライバ）が前提。詳細な省メモリ技術（block-swap、GGUF Gemma逐次オフロード、DiT CPU構築、VAEタイリング）はREADME §3「アーキテクチャ」を参照。

### 2.4 起動

- [ ] **標準手順**: `Nz-LTX23-backend`ディレクトリ直下の`setup.bat`で導入し（初回のみ）、`run.bat`をダブルクリックして起動する（README §1「かんたんインストール」・§2「起動」）。`run.bat`は`run.ps1`を呼ぶだけの薄いラッパーである。
- [ ] 開発時は`Nz-LTX23-backend`ディレクトリで`./run.ps1`を直接実行してもよい（README §2）。real backendが選ばれると、アプリが`./.venv-engine`のワーカーを自動でsubprocess起動する（手動起動は不要）。
- [ ] 起動後、`http://127.0.0.1:18620/api/v1/config`にアクセスし、応答内の`model.backend`が`real`になっていることを確認する（`/status`応答には`backend`に相当するフィールドが存在しないため、`/config`側で確認する）。`mock`のままの場合は2.1〜2.3のいずれかが未整備。

```powershell
curl http://127.0.0.1:18620/api/v1/config
```

### 2.5 フロント側の接続先変更

- [ ] **不要**。ネイティブブリッジの既定接続先（`native/src/bridge_core.h`の`kBackendBaseUrl = "http://127.0.0.1:18620"`）は、バックエンドのmock/real切り替えと無関係にそのまま使える。バックエンドを`127.0.0.1:18620`以外のポート・ホストで起動する場合のみ、AviUtl2内の設定パネル（`settings.set`）でbaseUrlを変更する。

## 3. 優先再検証: mock-real挙動差2点

これまでmock検証でしか確認できていなかった、既知の挙動差。実機で最優先の確認事項とする（`Docs/DEVLOG.md` §4-7、§4-8、§5残課題3参照）。オーナーはChainをrealバックエンドで反復使用してきており（t2vクリップ連結・i2v冒頭キーフレームクリップ連結・v2v、chunked_upsample常時Enabled）、通常構成でのChain生成自体は合格しているが、以下2点は「その反復使用の中で明示的に狙って確認したか」が不明なため、勝手に合格にせず個別に status を残す。

### 3.1 chainジョブの`clip`/`clip_count`

**確認状況**: **合格（2026-07-18オーナー確認）**。オーナーがChain使用時に進捗表示（clip/clip_countに基づく値）を確認し、適切に表示されたと申告した。

- [x] mockバックエンドでは`GET /jobs/{id}`の`clip`/`clip_count`が常に`null`（`services/ltx_runner.py`の`_MockBackend.generate_chain`が`clip`/`clip_count`引数を渡さないため）。
- [x] 実バックエンドでは`engine/pipeline/chain_pipeline.py`側がstage-1で`clip=`/`clip_count=`を渡す設計になっている。**実機でChain系ジョブ（Clips連結・V2V・A2V chain等）を実行し、`clip`/`clip_count`が非nullで進行に応じて更新されることを確認する。** フロントエンドの進捗表示（`JobResponse.clip`/`clip_count`/`stage`、`Docs/DEVLOG.md` §8.6で実装済み）が実際に意味のある値を表示することを目視確認する。
  - **状況（2026-07-18）**: オーナーがChain使用時に進捗表示の値を意識して確認し、「適切に表示された」と申告した。`Docs/PENDING_TASKS.md` §2-1の後日確認リストからも本項目を除去し、合格へ整理済み。

### 3.2 `overlap_frames`の実バックエンド制約

**確認状況**: **機能面合格・クローズ（2026-07-22）**。オーナーが実機で短いクリップを含むChainに既定の`overlap_frames`を指定し、「サーバーエラーが表示されて生成が止まる」挙動を確認して機能面で合格した。あわせて第2波バッチ（X3）で追加したクライアント側ゲート（Generate無効化＋最小フレーム数の命令形ノート）の実機・目視確認も同日合格した。詳細は`Docs/PENDING_TASKS_CLOSED.md` §3-33（機能面合格）・§3-34（X3のクライアント側ゲートの実機ゲート合格）、出典は`Docs/DEVLOG.md` §47。

- [x] `chain_math.py::compute_chain_layout`は`kv >= L`（`kv`=`overlap_frames`、`L`=各クリップの stage-1 latentフレーム数 ≈ `(num_frames-1)//8+1`）の場合に例外を送出する制約を持つが、**mockバックエンドはこの検証を通らないため再現しない**（`Docs/DEVLOG.md` §4-8）。実バックエンドでこの制約が実際に働くこと自体は、下記の実機確認により「サーバーエラーが表示されて生成が止まる」挙動として裏付けられた（**合格（2026-07-22オーナー実機確認）**。`Docs/PENDING_TASKS_CLOSED.md` §3-33）。
- [x] 実機で、短い`num_frames`のクリップ（例: 9〜16フレーム）を含むChainに既定の`overlap_frames`（既定値3）を指定し、フロント側の入力バリデーションが422エラーを未然に防いでいること、または適切なエラーメッセージが表示されることを確認する。（**合格（2026-07-22）**。サーバー側のエラー表示は機能面合格として確認済み（`Docs/PENDING_TASKS_CLOSED.md` §3-33）。あわせて第2波バッチ（X3）で追加したクライアント側の未然防止ゲート〔Generate無効化＋必要最小フレーム数を予告する命令形ノート〕の実機・目視確認も合格した（`Docs/PENDING_TASKS_CLOSED.md` §3-34）。）
- [x] 逆に、通常の長さのクリップで`overlap_frames`を含むChainが正常に生成できることは、オーナーのrealバックエンドでの反復使用（t2v/i2vクリップ連結、既定overlap_framesでの生成）により**合格**とみなせる（2026-07-18オーナー申告）。
  - **状況（2026-07-18時点の記録）**: 通常長のクリップでの動作は上記のとおり合格。ただし「短いクリップ構成での`kv >= L`制約の明示テスト」は、オーナーが意図的に短いクリップだけを使ったChainを組んで試した申告が無いため当時は未実施だった。**オーナー後日確認予定（2026-07-18決定）**として、`Docs/PENDING_TASKS.md` §2-1の「実装済み・テスト待ち」区分に整理していた。
  - **決着（2026-07-22）**: 上記の後日確認が実施され、**機能面合格でクローズ**した（オーナーが実機で「サーバーエラーが表示されて生成が止まる」挙動を確認。あわせてクライアント側の未然防止ゲート〔X3〕を追加し、その実機・目視確認も合格）。記録は`Docs/PENDING_TASKS_CLOSED.md` §3-33（機能面合格）・§3-34（X3の実機ゲート合格）へ移った。**`Docs/PENDING_TASKS.md` §2-1には既に存在しない**ため、参照先を読み替えること。

## 4. 実GPU通しマトリクス

> **§4.1〜§4.13すべて全項目合格済み（§4.10は2026-08-05にオーナー実機で合格・クローズ）。**

各項目は「生成→プレビュー→タイムライン挿入まで」を目視確認する。チェックボックスは合格時にチェックする。特記の無い限りCreate画面から実施。

### 4.1 単発 T2V（テキスト→動画）

**確認状況**: **合格（2026-07-18オーナー申告、realバックエンド）**。Create画面のt2v動画生成、およびChain画面のt2vクリップ連結動画生成の双方でrealバックエンドでの生成を確認済み。

- [x] プロンプトのみで生成が開始できる
- [x] 生成完了後にプレビューが表示される
- [x] タイムラインへの挿入（プロビジョナル→本挿入の一連の流れ）が成功する
- [x] 生成された動画の内容がプロンプトに沿っている（目視で破綻がない）
- [x] **minimal 481f・small 481fプリセット（2026-07-20追加、右クリック再設計第2段階）**: `Nz-LTX23-backend/config.yaml`の`generation_presets.minimal`（512×320）・`small`（960×576）が49／121フレームから481フレーム（20秒@24fps上限）へ引き上げられた（[`DEVLOG.md`](DEVLOG.md) §41.1）。その後のG4実機ゲートでsmall（960×576）の481f生成にわずかな共有メモリ使用が観測され、一時保守側の457フレームへ変更した（[`DEVLOG.md`](DEVLOG.md) §41.1a）が、オーナーがログを確認した結果、速度低下を伴わない無害なベースライン（`RESOLUTION_DURATION_CAPABILITY.md` §8.2）と確定したため481フレームへ復帰した（[`DEVLOG.md`](DEVLOG.md) §41.1b）。**合格（2026-07-20）**。G4実機ゲートは全9項目合格でクローズ済みで、本項の詳細な経緯は[`PENDING_TASKS_CLOSED.md`](PENDING_TASKS_CLOSED.md) §3-23（第2段階のクローズまとめ）に集約されている。本行は実GPUマトリクスの一項目として存在のみ記録する。

### 4.2 単発 I2V（画像/キーフレーム条件）

**確認状況**: Create画面のi2v（冒頭キーフレームのみ）・i2v（複数キーフレーム）・キーフレーム位置調整スライダーUIは**合格（2026-07-18オーナー申告、realバックエンド）**。Chain画面のi2v（冒頭キーフレーム）クリップ連結も合格。末尾の「冒頭キーフレームなし生成の実品質確認」も、以前のセッションで確認済みであることを**2026-07-19にオーナーが申告し合格**した。

- [x] 画像1枚（`frame_idx=0`固定の最小I2V）で生成できる
- [x] 複数キーフレーム画像（最大5枚・任意`frame_idx`）でも生成できる
- [x] 指定した画像の内容が動画の該当フレーム付近に反映されている
- [x] **冒頭キーフレームなし生成の実品質確認**（2026-07-18のキーフレーム専用枠廃止・任意位置対応〔[`KEYFRAME_TIMELINE_DESIGN_NOTES.md`](KEYFRAME_TIMELINE_DESIGN_NOTES.md)〕を受けて追加。**合格（2026-07-19オーナー申告、以前セッションで確認済み）**）: `num_frames=49`・`conditioning_images`は`frame_idx:9`の1枚のみを送り、冒頭0〜8フレームがキーフレームに縛られず自由に生成され、9フレーム付近で指定画像へ収束するかを確認する。同条件で`frame_idx:0`にキーフレームを置いた単独版も生成し、両者を目視で比較する。

### 4.3 IC-LoRA 参照動画（128グリッド、`/generate`）

**確認状況**: Create画面の「IC-LoRAでの動画生成」は**合格（2026-07-18オーナー申告、realバックエンド）**。強度スライダーの効果も、以前のセッションで確認済みであることを**2026-07-19にオーナーが申告し合格**した。128グリッド関連UIも**2026-07-19、オーナーの最終判定で合格**とした。

- [x] 参照動画（輪郭線canny・骨格pose等）をアップロードし、control系IC-LoRAを指定して生成できる（2026-07-17のIC-LoRA UI再設計〔第5波〕により、Create画面の参照動画アコーディオン内に状態持ちドロップダウン〔`config.model.ic_loras`駆動、「なし」付き・選択保持〕＋重みスライダー〔既定1.0〕がある。プロンプトタグは使わず、選択内容は送信時に`loras`配列の先頭へマージされる。プロンプトへ手打ちした制御LoRAタグは自動でパネル選択へ移行する）
- [x] `conditioning_attention_strength`等の強度スライダーの効果が確認できる（**合格（2026-07-19オーナー申告、以前セッションで確認済み）**）
- [x] 128グリッド関連のUI（該当する場合）が正しく機能する（**合格（2026-07-19オーナー最終判定）**）

### 4.4 Chain: Clips 連結（複数クリップ）

**確認状況**: **合格（2026-07-18オーナー申告、realバックエンド）**。t2vクリップ連結・i2v（冒頭キーフレーム）クリップ連結ともに合格。Chunked Upsampleは常にEnabledで使用しているとの申告のため、その状態での生成自体も合格とみなせる。進捗表示（3.1）も同日中のオーナー確認により合格。予想出力尺プレビューの一致も**2026-07-19のセッションで合格**した。

- [x] 2クリップ以上のChainが生成できる（24枠まで拡張済みだが、まずは2〜3クリップで確認）
- [x] `chunked_upsample`トグルがオンの状態で生成できる（既定オン、§4.9でも再確認。オーナーはChainで常時Enabled使用）
- [x] `overlap_frames`によるクリップ間の融合が自然につながっている（目視でシームが破綻していない）（通常長クリップでの反復使用実績より合格。§3.2参照）
- [x] 3.1の`clip`/`clip_count`進捗表示が機能する（**合格（2026-07-18オーナー確認）。§3.1参照**）
- [x] 8.6で実装した「予想出力尺」プレビューの値が、実際の出力尺と一致する（オーバーラップ融合・V2Vのcontext置換による減算を考慮した値と実測が近いこと）（**合格（2026-07-19セッション）**）

### 4.5 Chain: V2V（動画の続き）

**確認状況**: **合格（2026-07-18オーナー申告、realバックエンド）**。「v2vで動画の続きを生成」する基本フローは合格。`timeline.cutoutRange`（範囲選択切り抜き）と組み合わせた使用フローは、当初「オーナーが後日確認する」としていたが、**2026-07-27の調査でこの組み合わせを製品UIから実行する経路が存在しない（未配線）と判明し、検証対象外として決着した**（下記）。

- [x] 既存動画（`source_video`）をアップロードし、続きを生成できる
- [x] 元動画との接続部分（junction）が自然につながっている
- [x] ~~`timeline.cutoutRange`（範囲選択切り抜き）と組み合わせた実際の使用フローが成立する~~（**未配線につき検証対象外（2026-07-27決着）**: `CutoutRangeWorker`・`cutoutRangeAndUpload`は実装済みだが製品UIから呼ぶ配線が無く、実機で発火させる手段が無い。α版では未配線のままとし、実装済みコードは残置するとオーナーが決定した。経緯・将来配線するときの判断材料は[`PENDING_TASKS_CLOSED.md`](PENDING_TASKS_CLOSED.md) §3-48、機能自体の管理は[`RIGHTCLICK_REDESIGN_SPEC.md`](RIGHTCLICK_REDESIGN_SPEC.md)第9節のスコープ外一覧）

### 4.6 単発 A2V（グループ3でCreateへ移設・音声添付欄・distilled・`/generate/chain`）

**確認状況**: **合格（2026-07-18オーナー申告、realバックエンド）**。「a2v動画生成」「a2v+冒頭キーフレーム動画生成」が合格しており、基本フローと冒頭キーフレーム1枚との併用は確認済み。wav長自動調整の細部も、以前のセッションで確認済みであることを**2026-07-19にオーナーが申告し合格**した。短すぎる音声の事前ブロック・5スロット全体での併用・スタイルLoRA併用も、**2026-07-19のオーナー最終判定で合格**とした。ただしA2V＋参照動画付きcontrol系IC-LoRAの併用は、2026-07-15確定の「音声⇔参照動画の相互排他」設計によりUIに入口が存在しないため**確認項目から削除（スコープ外）**とした（チェックリストがGradio側の能力を誤って転記していたことが判明）。

- [x] Create画面の音声添付欄から音声ファイルをアップロードし、A2V生成が開始できる
- [x] wav長→Frames自動調整（契約v6`fs.probeAudioDuration`、`Docs/DEVLOG.md` §9.5）が実機で正しく機能する（**合格（2026-07-19オーナー申告、以前セッションで確認済み）**）
- [x] 音声が短すぎる場合の事前ブロック（送信前チェック）が実機で機能する（**合格（2026-07-19オーナー最終判定）**。Generate無効化とDURATION自動調整の両方の挙動を確認）
- [x] キーフレーム画像とA2Vの併用（5スロット）が機能する（**合格（2026-07-19オーナー最終判定）**。冒頭キーフレーム1枚との併用に続き、5枚全部を使った併用も合格）
- [x] A2V＋スタイルLoRAタグの併用が機能する（**合格（2026-07-19オーナー最終判定）**）
- ~~A2V＋参照動画付きcontrol系IC-LoRAとの併用（α版）~~（**スコープ外（2026-07-19確定）**: 2026-07-15確定の「音声⇔参照動画の相互排他」設計により、この組み合わせを送信するUI上の入口が存在しない。本ツールのスコープ外のため確認対象から削除）
  - **その後の訂正（2026-07-21）**: 上記2026-07-19の判断は事実誤認と判明した（`Docs/PENDING_TASKS_CLOSED.md` §3-18の訂正注記参照）。併用は2026-07-21に実装解禁され、同日real GPUでの併用生成ゲートに合格した（`Docs/PENDING_TASKS_CLOSED.md` §3-25・§3-27、[`DEVLOG.md`](DEVLOG.md) §44）。取り消し線の記述は経緯の記録として残すが、現状は実装済み・合格済みである。
- [x] 生成された動画の口パク・動きが音声と同期している（目視）

### 4.7 バッチ A2V（Create画面内の一括生成節、フォルダスキャン・同名mp4付番）

**確認状況**: **合格（2026-07-18オーナー申告、realバックエンド）**。「バッチa2vの機能テスト合格」と申告されており、本日の新Shared仕様デプロイ（§20）後に実施された点も明記されている。`noClobber`付番も、以前のセッションで確認済みであることを**2026-07-19にオーナーが申告し合格**した。開始前チェックも**2026-07-19のセッションで合格**した。

**注記（2026-07-18、設計判断でステートレス化）**: 本項目は元々`batch_a2v_manifest.csv`による行状態の読み書きとアプリ再起動後の中断再開機能を前提にしていたが、2026-07-18の設計判断（[`DEVLOG.md`](DEVLOG.md) §26.3）により、この状態保存・中断再開機能自体が廃止された。以降のバッチA2Vはフォルダスキャン→メモリ内行リスト→実行のみのステートレス設計で、CSVマニフェストは生成されず、ウィンドウを閉じると行状態は消える。下記の「アプリ再起動後の再開シナリオ」は当時（再開機能があった時点）の合格記録として残すが、その機能自体はもう存在しない。

- [x] 音声フォルダ・画像フォルダ・出力先を指定し、「Set audios」で行が生成される
- [x] バッチ実行中、Generateボタンが無効化される（二重投入防止）
- [x] 出力フォルダ規約（`{音声フォルダ名}_a2v_out`）どおりに動画が出力される
- [x] 出力フォルダが存在しない状態から自動作成される（**2026-07-19オーナー実機実証**。native `bridge.cpp`の`EnsureDirectoryTree`＋`http_client`側の親ディレクトリ再帰作成の二重実装、[`DEVLOG.md`](DEVLOG.md) §28.3・§29）
- [x] ファイル名対応規約（`音声名.wav`→同名`音声名.mp4`）が守られる
- [x] 同名ファイルが既にある場合、`noClobber`付番（`_2`, `_3`...）で上書きされない（**合格（2026-07-19オーナー申告、以前セッションで確認済み）**）
- [x] ~~アプリ再起動・電源断相当のシナリオ後、「Set audios」→「Start a2v batch」で続きから再開でき、Done行が再実行されない~~（**合格（2026-07-18オーナー確認、CSVマニフェストによる中断再開機能があった当時の記録）。同日中の設計判断で中断再開機能自体が廃止されたため、本項目は前提消滅によりクローズ**）
- [x] 開始前チェック（プロンプト・画像の入力忘れ検知）が実機で機能する（**合格（2026-07-19セッション）**）
- [x] **reuseIfPresent実機検証（通常経路）**: ジョブカードのInsert（🎞️）→✅→✅クリックで🎞️へリセット→再度🎞️の再挿入サイクルで、`backend.downloadVideo`の`reuseIfPresent`（plain動画限定のオプトイン）により2回目以降の再挿入が再ダウンロードをスキップして成功することを、**2026-07-19朝のオーナー目視で確認済み（合格）**（[`DEVLOG.md`](DEVLOG.md) §26.2）。
- [x] **reuseIfPresent実機検証（リロード後の保険経路）**: WebView2のリロード（ページ再読み込み）またはAviUtl2再起動でwebui側の記憶が消えた状態でも、完了ジョブの🎞️でnative側の`reuseIfPresent`がダウンロード済みファイルを再利用して再挿入できることを確認した（**合格（2026-07-19オーナー最終判定）**。[`DEVLOG.md`](DEVLOG.md) §32、`Docs/PENDING_TASKS_CLOSED.md` §3-19）。これで通常経路・保険経路とも合格し、Insert再挿入まわりの確認はすべて完了した。

### 4.8 モデル管理（`GET /models`一覧取得 → `POST /pipeline/load`で切替、ロードは数分・409 JOB_BUSY排他）

**確認状況**: **合格（2026-07-18、オーナー実機・サーバーログ証跡あり／409排他・unload→再ロードは2026-07-19オーナー最終判定で合格／DangerZonePanelの表示有無も2026-07-19に合格）**。SettingsPanelのモデル管理UIからLoadを実行し、サーバーログで`POST /api/v1/pipeline/load 200`→`Model-management overrides: {'transformer': 'models\ltx-2.3-gguf\Sulphur-2-base-distil-Q4_K_M.gguf'}`→`LTX worker ready`という切替完了を確認した。その後投入したt2v生成ジョブは`base=Sulphur-2-base-distil-Q4_K_M.gguf`で実行され264.7秒・peak_vram=10637MBで完走、動画DLも成功した（2026-07-18 15:04-15:11のログ）。2026-07-19、生成中のLoad試行が「Cannot switch models while a generation job is running.」で拒否されること、生成完了後のモデル切替→t2v/a2v成功をオーナーが確認し、409排他・unload→再ロードのサイクルもあわせて合格とした。なお、オーナーから「危険ゾーンが見当たらない」との申告があった件は、2026-07-19最終判定でオーナーの記憶違いと判明し、生成中のunloadでbusy表示が出る想定どおりの挙動であることを確認した（表示バグの疑いは解消。[`DEVLOG.md`](DEVLOG.md) §32、`Docs/PENDING_TASKS_CLOSED.md` §3-19）。

- [x] SettingsPanelのモデル管理UIで4カテゴリのドロップダウンにモデル一覧が表示される（Refreshボタン含む）（切替対象を選択できた実績から妥当と判断。ドロップダウンの見た目自体は`PENDING_TASKS.md`旧§2-2の目視確認対象だった）
- [x] Loadボタンでモデル切替が実行でき、ロード完了まで（数分単位）待機できる（`timeoutMs=600000`が実機で十分か確認）（サーバーログで`POST /pipeline/load 200`→`LTX worker ready`の完了を確認。ロード単体の所要時間の内訳はログから未分離だが、タイムアウトに達さず完了している）
- [x] ロード中に別ジョブを投げた場合、または既にロード中の別リクエストがある場合に409 `JOB_BUSY`が返り、フロントに専用メッセージが表示される（**合格（2026-07-19オーナー最終判定）**。生成中のLoad試行が「Cannot switch models while a generation job is running.」で拒否されることを確認）
- [x] 切替後のモデルで実際に生成が成功する（`base=Sulphur-2-base-distil-Q4_K_M.gguf`でのt2v生成が264.7秒・peak_vram=10637MBで完走し、動画DLも成功）
- [x] 設定パネルの危険ゾーン（N4、`Docs/DEVLOG.md` §12.8）から実行する**パイプラインunload → 再ロード**が正常に機能する（**合格（2026-07-19オーナー最終判定）**。生成完了後のモデル切替→t2v/a2v成功を確認。DangerZonePanelの表示有無自体も2026-07-19に合格（表示バグの疑いは記憶違いと判明し解消。`Docs/PENDING_TASKS_CLOSED.md` §3-19））

### 4.9 chunked_upsample がオンで送られ省メモリ経路になること

**確認状況**: **合格・決定的証跡あり（2026-07-19オーナー最終判定）**。オーナーはChain画面で`chunked_upsample`を常にEnabledのまま使用しており、chunked_upsample経路自体は実GPUで反復検証済み。加えて768p級ストレステストとして、`Nz-LTX23-backend/outputs/a2472b84-c8a6-4f04-b84f-6b505f8f5cce/metadata.json`を実読し、768p（`width`1280×`height`768、`crop_output`1280×720）・24クリップ×257フレーム＝総`5777`フレーム（出力`240.708`秒、約4分）・`chunked_upsample: true`・`vram_optimization.peak_vram_mb: 9780`（16GB内・スピルなし。机上推定10.4GBと整合）で完走したことを確認した（2026-07-14生成ジョブ、GPU: RTX 4070 Ti SUPER）。

- [x] Chain画面の`chunked_upsample`トグルが既定オンになっている
- [x] トグルオンの状態で768p級の長尺Chainを生成し、VRAM溢れ（OOM）が発生しないことを確認する（**合格・決定的証跡あり（2026-07-19）**。ジョブ`a2472b84-c8a6-4f04-b84f-6b505f8f5cce`のmetadata.json実読により、768p・24クリップ×257f＝総5777フレームで`peak_vram_mb 9780`〔16GB内・スピルなし〕を確認。バックエンド側の実測チャンク版9.4GB vs 一括16.1GBの差とも整合）
- [x] `Docs/DEVLOG.md` §9.9-1に記録のとおり、Gradio側バッチランナーは`chunked_upsample`を明示送信しない既知のギャップがあるが、**フロントエンドのバッチA2Vは常に明示送信する実装**になっている。バッチA2V（4.7）でもこの経路が効いていることを併せて確認する（**合格（2026-07-19オーナー最終判定）**）。

### 4.10 ドラッグ＆ドロップ（ブリッジ契約v7、`ui.resolveDroppedFiles`）

**確認状況**: **全5項目合格（2026-08-05オーナー実機確認）**。フロントエンド台帳[`PENDING_TASKS.md`](PENDING_TASKS.md) §2-1に起票していたチェックリストは、合格に伴い同書からは除去済み（跡地の記録は本節）。

- [x] 参照動画欄（IC-LoRA、Create画面）へ動画ファイルをドラッグ＆ドロップすると、実パスが解決されて添付される
- [x] A2V音声欄（Create画面）へ音声ファイルをドラッグ＆ドロップすると、実パスが解決されて添付され、wav長からのFrames自動調整も発火する
- [x] Chain統合ソース入力欄（`SourceInputPanel`）へ画像/動画ファイルをドラッグ＆ドロップすると、実パスが解決されて添付される（種別に応じてV2V/from-scratchのモードが正しく切り替わることも確認する）
- [x] 対象外のファイル種別をドロップした場合にエラーメッセージが表示される
- [x] ドロップゾーン外（WebView2の表示領域外を含む）に落としてもWebUIが壊れない

これら5項目は、実際のOSファイルドロップ→`window.chrome.webview`の`postMessageWithAdditionalObjects`→ネイティブ側`ExtractDroppedFilePaths`（`native/src/webview_host.cpp`）→`ui.resolveDroppedFiles`という経路をすべて実際に通す必要があり、**WebView2のドラッグ＆ドロップイベント自体に依存するため、doctestやmockバックエンドでは検証できない**（REALDEVICE-VERIFY対象）。mockバックエンド環境でのユニット/統合テストは、ドロップされたファイルパスの注入機構（`__droppedPaths`）自体をテストコード側でエミュレートして`ui.resolveDroppedFiles`より先のロジックのみを検証しており、実際のOSレベルのドラッグ操作は再現していない点に注意（§3系の「mock-real挙動差」と同種の限界）。

### 4.11 NAG（Normalized Attention Guidance、ネガティブプロンプトの効き、2026-07-28追加）

**確認状況: 全項目合格・クローズ（2026-07-29）**。オーナー実機確認で下記3項目とも合格し、あわせてNAGアコーディオンUIの目視ゲートも合格した。クローズ記録は[`PENDING_TASKS_CLOSED.md`](PENDING_TASKS_CLOSED.md) §3-51・§3-54、バックエンド側のゲートG0〜G7の記録は[`Nz-LTX23-backend/Docs/VERIFICATION_LOG.md`](../../Nz-LTX23-backend/Docs/VERIFICATION_LOG.md) §38。

NAGは4.1〜4.9のような特定の生成モード専用ではなく、Create/Chain/Batchの3経路すべてに横断的に乗る追加パラメータのため、単独項目として本節に置く（4.9のchunked_upsample・4.10のドラッグ＆ドロップと同じ扱い）。

- [x] 同一プロンプト・同一シードで、ネガティブプロンプトを無効（チェックOFF）にした生成と、有効＋具体的な本文（既定文`blurry, low quality, distorted, watermark, text`のままでよい）を入れた生成を見比べ、ネガティブに書いた要素（ぼやけ・低品質・歪み・透かし・字幕混入）が後者で目に見えて減っていることを確認する
- [x] scaleスライダーを既定11.0から動かし、値を上げるほど効果が強まる（下げるほど弱まる）方向性が体感できることを確認する
- [x] Create・Chain・Batchの3経路それぞれでNAGを有効化して生成し、いずれも問題なく完走することを確認する（Batchは3-1で確定済みの「Create設定を無言で自動適用」仕様どおりに効くことも併せて確認する）

対応していた台帳側の項目は`PENDING_TASKS.md` §2-1「⑧NAGで実GPUのネガティブプロンプトが実際に効くこと」で、合格に伴い[`PENDING_TASKS_CLOSED.md`](PENDING_TASKS_CLOSED.md) §3-51へ移設済みである（**同書§2-1そのものが2026-08-10に節ごと削除された**ため、現在の参照先は[`PENDING_TASKS_CLOSED.md`](PENDING_TASKS_CLOSED.md)である）。

### 4.12 V2Vリボン範囲トリム（2026-07-30追加、§1-6）

**確認状況: 全項目合格・クローズ（2026-08-01）**。①採取解析・②非退行（2026-07-31）・③短尺ゲート・④トリム後Joinの中身連続性・⑤IC-LoRA参照動画の範囲反映の5項目すべてがオーナー実機で合格した（⑤は同日に右クリック#2「IC-LoRA参照動画」へ同じトリムを広げた拡張分）。**経緯・確定事実・実装記録の正本**: [`V2V_RIBBON_TRIM_WORKORDER.md`](V2V_RIBBON_TRIM_WORKORDER.md) §0（状態）・§4（`再生位置`の確定事実）・§7-3（⑤の拡張）、[`DEVLOG.md`](DEVLOG.md) §56、[`PENDING_TASKS_CLOSED.md`](PENDING_TASKS_CLOSED.md) §3-59（クローズ記録。起票時は`PENDING_TASKS.md` §1-6。**§3-58はAccelerationなので混同しないこと**）、[`Nz-LTX23-backend/Docs/VERIFICATION_LOG.md`](../../Nz-LTX23-backend/Docs/VERIFICATION_LOG.md) §42（バックエンド側）。

タイムライン上の動画オブジェクト（リボン）が元動画ファイルの一部しか占めていないとき、その範囲だけを切り出したmp4を冒頭クリップとして送る機能。§4.5（Chain: V2V）の内側の細目だが、既存項目のチェック済み欄を書き換えないよう独立した節として置く。**①が済むまで②〜④は発火しなかった**（判定関数`decideSourceTrim`はリボンの再生位置が読めないかぎり必ず「トリムしない」を返すため）点だけ、下の各項の日付を読むときの前提として書き添えておく。

- [x] **①`enum_effect_item`の実機採取（前提条件・最優先）（2026-08-01合格——採取・解析とも完了）**: [`V2V_TRIM_PROBE_GUIDE.md`](V2V_TRIM_PROBE_GUIDE.md)の手順で調査用ビルドを作り、同書第4節の**R1〜R8のケース**（各ケースで`plugin.log`・エイリアスファイル・「選択オブジェクトの詳細」のスクリーンショット）を採取した（R7はUI上そもそも作れないため未実施、R8も未実施だがその影響は判定条件の1フレーム余裕が吸収する）。→ **結果: `再生位置`の生値は4フィールドのCSV`開始,終了,再生範囲,0`で、先頭2値は「素材時間軸の秒」（プロジェクトfpsに依存しない）と確定**。あわせて`get_object_track_value`が第1値しか返さないこと・`再生速度`が百分率の小数2桁文字列であること・`ループ再生`項目が存在すること・R4型のはみ出しが実在することも判明した。確定事実の全文は[`V2V_RIBBON_TRIM_WORKORDER.md`](V2V_RIBBON_TRIM_WORKORDER.md) §4。調査コード（`-ProbeVideoItems`スイッチとプローブA/B）は解析完了後にすべて撤去済み。
- [x] **②リボン＝元動画全体のとき、従来と同一の結果になる（2026-07-31合格——オーナー実機で右クリックV2Vが従来どおり動作することを確認）**: 元動画を丸ごと（加工なしで）タイムラインへ置き、右クリック→V2Vで生成する。→ 合格は、トリムが走らず（アップロードが従来どおり全体）生成結果・Join結果が改修前と変わらないこと。**この「無トリム時バイト等価」が本改修の退行防止線**であり、自動テストでも機械的に固定してあるが（バックエンド`tests/test_upload_video_trim.py`、WebUI `ChainScreen.prefill.test.tsx`）、実機でも1回は通す。
- [x] **③短いリボンでGenerateが無効化され、案内が出る（2026-08-01合格——短すぎるリボンのV2Vで「This video is too short. A video of about 4 seconds or longer is required.」が表示され、冒頭動画が挿入されないことをオーナー実機で確認）**: `context_frames`を供給できない短いリボン（またはリボンは足りているが後から`contextFrames`を上げた状態）でChain画面を開く。→ 合格は、Generateボタンが無効になり、理由ノートに「ソース動画が短い」旨の案内が表示されること（クライアント側ゲート。**サーバーの422を出さずに未然に止める**設計で、`PENDING_TASKS.md` §3-32の「製品UIから422を起こす操作手段が無い」状態を維持するためのもの）。あわせて、トリムを要求したのにサーバーが`trimmed: true`を返さなかった場合もGenerateが止まること（理由コード`sourceTrimFailed`。ファイルを選び直せば全体として使える案内が出る）を、可能なら確認する。
- [x] **④トリム後V2V→Join→🎞挿入で「中身の連続性」を確認する（本改修の決め手）（2026-08-01合格——末尾を削ったリボンのV2Vで「切られた位置から続きが生成される」ことをオーナー実機で確認）**: 元動画の中ほどだけを占めるリボン（頭を数秒削り、尾も余らせた状態）を右クリック→V2V→生成→Join→🎞でタイムラインへ挿入する。→ 合格は、**挿入されたJoin結果の継ぎ目の直前の絵が、リボンで選んでいた範囲の末尾の絵になっていること**。
  - **判定は必ず「中身」で行う（旧実装の症状の訂正）**: 改修前の症状は「挿入位置がずれる」ではない。`joinedInsertFrame`（`webui/src/jobs/fpsConvert.ts`）はリボンの`frameStart`/`frameEnd`から挿入位置を計算し、`frameStart`より前へは行かないようクランプしているため、**位置はもっともらしい値に収まっていた**。ずれていたのは中身である——サーバーには元動画の全長が送られていたため、Joinが残す末尾（`source_tail_seconds`、既定5秒）と生成の文脈（context）が**リボンで選んだ範囲の末尾ではなく元動画ファイルそのものの末尾**になっていた。したがって「位置が合っているから合格」と判断してはならず、継ぎ目の直前に映っている絵がどこのフレームかを目視で確かめる必要がある。
- [x] **⑤IC-LoRA参照動画のリボン範囲反映（2026-08-01追加・拡張実装分。同日合格——リボンをクリップした動画オブジェクトからのIC-LoRAで、正しくクリップ区間を参照した動画が生成されることをオーナー実機で確認）**: 321フレームの動画素材をタイムラインへ置き、リボンを**31〜120フレーム**（90フレーム）に詰める。そのオブジェクトを右クリック→「IC-LoRA参照動画」でCreate画面を開き、制御LoRAを選んで生成する。→ 合格は、**参照として効いている絵が元動画の31〜120フレーム相当になっていること**（拡張前の症状は「参照が元動画の1〜90フレームになる」で、原因はアップロードがフル尺だったためパイプラインがファイル先頭から`frame_cap`分を読んでいたこと）。あわせて次の2点も確認する:
  - **切り出し後のファイル長**: 参照スロットに載ったファイルの尺が、リボンの尺（この例なら90フレーム＝プロジェクトfps 30なら3.0秒）と一致していること。元動画の10.7秒のままなら切り出しが効いていない。
  - **DURATIONシードとの整合**: Create画面のフレーム数（DURATION）の初期値が、フル尺ではなくリボンの尺から導かれた値になっていること（右クリック時の`spanDurationSec`基準のシードと、実際に上がる素材の尺が同じ範囲を指しているか、という整合確認）。
  - あわせて、切り出しを頼んだのにサーバーが`trimmed: true`を返さなかった場合にGenerateが止まること（理由コード`referenceTrimFailed`。「ファイルを選び直せば全体として使える」案内が出る）を、可能なら確認する。Chain側`sourceTrimFailed`（③の末尾）と同型。
  - **手動📁ピックは無トリムのまま**が仕様である（タイムライン情報を持たない経路なので従来どおり全体が上がる）。参照カードから📁で選び直したときに範囲が効かないのは故障ではない。

### 4.13 バッチi2v-long（2026-07-30追加、§1-7）

**確認状況: 全項目合格・クローズ（2026-07-31）**。基本フロー5点は2026-07-30合格。行ごとプロンプト・STATUS修正（[`DEVLOG.md`](DEVLOG.md) §54.8）・中止挙動・相互ロック両方向（§54.9の修正後）・バッチ完走は2026-07-31合格。JobLedger 50件超のみ実運用トリガー待ちへ分離（[`PENDING_TASKS.md`](PENDING_TASKS.md) §3-48）。クローズ記録は[`PENDING_TASKS_CLOSED.md`](PENDING_TASKS_CLOSED.md) §3-57。

Chain画面の下部にある「バッチi2v-long（プロトタイプ）」節から、画像フォルダを指定して**画像1枚ごとにClip Chain（`POST /generate/chain`）を1本ずつ直列生成**する機能。生成パラメータはすべてChain画面の現在の設定を無言借用する（この節自身はサイズ・尺・seedの欄を持たない）。バックエンドは無改修。実装の正本は[`BATCH_I2V_WORKORDER.md`](BATCH_I2V_WORKORDER.md)、台帳は[`PENDING_TASKS_CLOSED.md`](PENDING_TASKS_CLOSED.md) §3-57（起票時は`PENDING_TASKS.md` §1-7 →同§2-1。クローズで移設した）。

- [x] **スキャン**（2026-07-30合格）: 画像フォルダを📁または手打ち＋フォーカス外し（onBlur）で指定し、Scanを押す。→ 合格は、`GET /config`の`upload.allowed_image_extensions`に一致する画像だけが**ファイル名昇順**（`img2.png`が`img10.png`より前に来る自然順）で行になり、`#`が1から連番で振られること。`upload.max_image_size_mb`を超える画像は、開始前から`Failed`＋サイズ超過の説明文で並ぶこと（実行の何十分後ではなく事前に分かること）。
- [x] **直列生成**（2026-07-30合格）: Chain画面のクリップ構成（2本以上）とプロンプトを整えてから開始する。→ 合格は、行が1つずつ`Generating`→`Done`と進み、**同時に2本以上のジョブが投入されないこと**（1枚ごとに`POST /generate/chain`→`GET /jobs/{id}`ポーリング完了→ダウンロードの順で進む）。409（サーバー混雑）は最大3回試行（あいだに3秒待つ）まで自動でやり直し、それでも通らなければその行だけが`Failed`になり、後続は続行される。
- [x] **出力フォルダ**（2026-07-30合格）: 出力先を触らなければ、画像フォルダの兄弟に`{画像フォルダ名}_i2vlong_out`が自動導出される（自分で選んだ／打ち込んだ時点で自動追従は止まり、以後戻らない）。→ 合格は、存在しないフォルダが自動作成され、各行の出力が**画像名の拡張子をmp4へ置き換えた名前**で保存されること。同名ファイルが既にある場合は`noClobber`付番（`_2`, `_3`…）で上書きされず、表の`output`列には**実際に書かれた名前**が出ること。
- [x] **行ごとプロンプト（2026-07-30の再設計分。2026-07-31合格——①②を実機確認、③④⑤は未個別確認だが自動テストでピン済み）**: Scan後、表の`プロンプト`列に**画像ごとに違う文**を入力する（📝を押すと、その行にChain画面の共通プロンプトがそのまま流し込まれる）。add/replaceのラジオはバッチ全体で1つ。→ 合格は、①出来上がった動画が**行ごとに違うプロンプトの内容**になっていること（addなら共通プロンプト＋その行の文、replaceならその行の文だけ）、②行の文が空の行は共通プロンプトだけで生成されること、③共通プロンプトも行の文も空の行があるとStartが無効になり、理由に**その行番号（`#3`のような形）**が出ること、④走行中とScan中は入力欄と📝が押せないこと、⑤再Scanすると入力した行プロンプトが消えること（ステートレス設計。仕様どおりで故障ではない）。
- [x] **中止挙動（現在の1枚は完走保存）（2026-07-31合格——現在生成中の1枚の完了時に停止・再Startで中断直後の画像から再開を実機確認）**: 生成中にStopを押す。→ 合格は、**いま生成中の1枚はそのまま完走してDoneとして保存され**、それ以降の行は`Waiting`のまま残ること。`DELETE /jobs/{id}`は送られない設計である（バックエンドは実行中のchainジョブを途中で止められず、DELETEしてもGPU時間は丸ごと消費した末に完成品を捨てるだけになるため）。したがって**Stopの反応には現在の1枚が終わるまでの時間（数十分になり得る）がかかる**のが仕様どおりで、故障ではない。後で再度Startすれば`Waiting`/`Failed`の行だけが処理される（`Done`行は再実行されない）。
- [x] **バッチA2Vとの相互ロック（2026-07-31合格——[`DEVLOG.md`](DEVLOG.md) §54.9の実バグ2件修正後、両方向とも案内文つきでStart無効化を実機確認）**: Create画面のバッチA2Vを走らせたまま、Chain画面のバッチi2v-longのStartを見る（および逆方向）。→ 合格は、**Startが無効化され、理由ノートに「もう一方のバッチが走っている」旨の行が表示されること**（黙って何も起きないのではなく、理由が読めること）。片方が終われば他方のStartが自動で押せるようになる。
- [x] **JobLedger 50件超での描画確認（2026-07-31クローズ——実運用トリガー待ちへ移行）**: バッチのScan表側は50件超の画像フォルダでスクロール含め問題なしをオーナー実機で確認済み。画面右のジョブ一覧（JobLedger）側は「実運用で50枚級バッチを回したとき、もたつく・崩れると感じたら対応する」形でクローズ（専用の検証時間を取る価値がない軽微事項というオーナー判断）。追跡は[`PENDING_TASKS.md`](PENDING_TASKS.md) §3-48。

## 5. 既知のブロッカーと解消状況

- **`scripts/deploy.ps1`・`scripts/run-aviutl.ps1`のデフォルトパス**: 2026-07-15グループ3のセッションで、両スクリプトの既定値を実機構成（AviUtl2 v2.0.54ポータブル、`D:\For_Videos\AviUtl2\aviutl2_v2.0.54\`）に修正済みであることを確認した。
  - `deploy.ps1`の`-PluginDir`既定値: `D:\For_Videos\AviUtl2\aviutl2_v2.0.54\data\Plugin`
  - `run-aviutl.ps1`の`-ExePath`既定値: `D:\For_Videos\AviUtl2\aviutl2_v2.0.54\aviutl2.exe`
  - **実行時に`-ExePath`/`-PluginDir`を明示的に上書きする必要はない**（デフォルトのままで現行実機構成に合う）。
- **バックエンドvenvの`pyvenv.cfg`破損（解消済み）**（`Docs/DEVLOG.md` §4-4）: `Nz-LTX23-backend/.venv`・`.venv-engine`がフォルダ移動後、`pyvenv.cfg`の`home`が旧パスを指したままになっている、という既知の問題があった。**2026-07-27に両venvの`pyvenv.cfg`を実測したところ、`home`はいずれも現行の`Nz-LTX23-backend/.python/cpython-3.12.9-windows-x86_64-none`を指しており、この問題は解消していることを確認した**（インストーラによるvenv再構築を経て正しい値になったもの）。フロントエンド開発中に使っていた`PYTHONPATH`設定＋基盤Python直接起動という回避策も、もはや不要である。

## 6. 記録欄

実行日・実行者・結果をこの節に追記していく。各マトリクス項目について、合否・気づき・スクリーンショットの置き場を記録する。

### 2026-07-18オーナー申告の反映（本節・項目別結果表の先取り記録）

2026-07-18、オーナーから「realエンジンで検証済みの機能」の申告があった。これは`./run.ps1`起動＋`model.backend="auto"`により本機で自動的にrealが選ばれた状態での、オーナー自身による日常的な実GPU生成の積み重ねに基づく申告であり、本節冒頭の「実行ログテンプレート」に沿った単発の実行記録ではないが、下記「項目別結果」表には申告内容をそのまま反映する。スクリーンショットの置き場は指定されていない（オーナー申告ベースのため）。

- Createタブ: プリセット選択／CROP OUTPUT／t2v／i2v（冒頭キーフレームのみ）／i2v（複数キーフレーム）／キーフレーム位置調整スライダーUI／IC-LoRA動画生成／a2v／a2v+冒頭キーフレーム／バッチa2v機能テスト（新Shared仕様デプロイ後）——すべて合格。
- Chainタブ: プリセット選択／CROP OUTPUT／t2vクリップ連結／i2v（冒頭キーフレーム）クリップ連結／v2v続き生成——すべて合格。chunked_upsampleは常時Enabledで使用。
- モデル管理: 追加でオーナーの実機ロード操作をサーバーログ（`POST /pipeline/load 200`→`LTX worker ready`→切替後モデルでのt2v生成完走）で確認し合格。
- 各カテゴリ内の細部7点（IC-LoRA強度スライダー・Chain V2Vのcutoutrange併用・wav自動調整の細部・バッチの再開/noClobber・冒頭キーフレームなし品質確認・短いクリップでのoverlap_frames制約・Chainのclip/clip_count進捗表示）は、オーナーが「後日確認する」と明言（2026-07-18決定）。ブロッカーではなくテスト待ち項目として整理。
- 出典: オーナーからの2026-07-18申告、[`DEVLOG.md`](DEVLOG.md) §19・§21、[`PENDING_TASKS.md`](PENDING_TASKS.md) §2-1。

### 実行ログテンプレート

```
実行日: 2026-__-__
実行者:
GPU/環境:
config.yaml model.backend:
起動方法（run.ps1 / 手動起動 / オプション）:
```

### 項目別結果

| 節 | 項目 | 合否 | 気づき | スクショ置き場 |
|---|---|---|---|---|
| 3.1 | chain clip/clip_count非null確認 | 合格（2026-07-18） | オーナーがChain使用時に進捗表示の値を確認し、適切に表示されたと申告 | なし |
| 3.2 | overlap_frames実バックエンド制約 | 合格（2026-07-18／2026-07-22決着） | 通常長クリップでの生成は合格。短いクリップでの`kv>=L`制約は2026-07-22にオーナー実機確認で機能面合格（サーバーエラー表示で生成停止）、あわせてクライアント側ゲート（X3）の実機ゲートも合格（`PENDING_TASKS_CLOSED.md` §3-33・§3-34） | なし |
| 4.1 | 単発T2V | 合格（2026-07-18） | Create/Chain両方でrealバックエンドでの生成を確認 | なし（オーナー申告ベース） |
| 4.2 | 単発I2V | 合格（2026-07-18／細部2026-07-19） | 冒頭キーフレームのみ・複数キーフレーム・位置調整スライダーは合格。冒頭キーフレームなし生成の品質確認も2026-07-19オーナー申告で合格 | なし |
| 4.3 | IC-LoRA参照動画 | 合格（2026-07-18／細部2026-07-19） | 動画生成自体は合格。強度スライダー効果・128グリッドUIも2026-07-19オーナー最終判定で合格 | なし |
| 4.4 | Chain: Clips連結 | 合格（2026-07-18／細部2026-07-19） | t2v/i2vクリップ連結・chunked_upsample常用・進捗表示は合格。予想出力尺の一致も2026-07-19セッションで合格 | なし |
| 4.5 | Chain: V2V | 合格（2026-07-18／2026-07-27決着） | 基本フローは合格。cutoutRange併用フローは未配線につき検証対象外で決着（`PENDING_TASKS_CLOSED.md` §3-48） | なし |
| 4.6 | 単発A2V | 合格（2026-07-18／細部2026-07-19） | a2v・a2v+冒頭キーフレームは合格。wav自動調整の細部・短すぎる音声の事前ブロック・5スロット全体併用・スタイルLoRA併用も2026-07-19オーナー最終判定で合格。A2V＋参照動画付きIC-LoRA併用はUI相互排他設計によりスコープ外で削除→2026-07-21実装解禁・real併用ゲート合格（`PENDING_TASKS_CLOSED.md` §3-27） | なし |
| 4.7 | バッチA2V | 合格（2026-07-18／細部2026-07-19） | 新Shared仕様デプロイ後の機能テスト合格。再開シナリオは中断再開機能廃止（ステートレス化）でクローズ。noClobber・開始前チェック・出力フォルダ自動作成・reuseIfPresent通常再挿入・reuseIfPresentリロード後保険経路はいずれも2026-07-19に合格 | なし |
| 4.8 | モデル管理 | 合格（2026-07-18／細部2026-07-19） | サーバーログ証跡あり（`POST /pipeline/load 200`→`LTX worker ready`→切替後モデルでt2v生成完走・264.7秒・peak_vram=10637MB）。409排他（生成中Load拒否メッセージ確認）・危険ゾーンunload→再ロード・DangerZonePanel表示有無も2026-07-19オーナー最終判定で合格（表示バグの疑いは記憶違いと判明し解消） | なし |
| 4.9 | chunked_upsample省メモリ経路 | 合格・決定的証跡あり（2026-07-18／2026-07-19） | Chainで常時Enabled使用。768p級ストレステストはジョブ`a2472b84-c8a6-4f04-b84f-6b505f8f5cce`のmetadata.json実読（24クリップ×257f、peak_vram_mb 9780）で2026-07-19に合格確定 | なし |
| 4.10 | ドラッグ＆ドロップ（契約v7） | **全項目合格（2026-08-05）** | 参照動画欄・A2V音声欄・Chain統合ソース入力欄へのドロップ、対象外種別のエラー表示、ドロップゾーン外への無事故——5項目すべてオーナー実機確認 | なし |
| 4.11 | NAG（ネガティブプロンプトの効き） | 合格（2026-07-29） | オーナー実機確認で全項目合格・クローズ（`Docs/PENDING_TASKS_CLOSED.md` §3-51・§3-54） | なし |
| 4.12 | V2Vリボン範囲トリム | **全項目合格（2026-08-01）** | ①採取解析（`再生位置`＝素材時間軸の秒と確定）②非退行（2026-07-31）③短尺ゲート（案内表示＋冒頭動画の挿入なし）④トリム後Joinの中身連続性（切られた位置から続きが生成される＝本改修の決め手。位置ではなく中身で判定）⑤IC-LoRA参照動画の範囲反映（同日追加の拡張分）。クローズ記録は`Docs/PENDING_TASKS_CLOSED.md` §3-59 | なし |
| 4.13 | バッチi2v-long | **全項目合格（2026-07-31）** | 基本フロー3項目は2026-07-30合格。行ごとプロンプト・中止挙動・相互ロック両方向は§54.8/§54.9の修正後に2026-07-31合格。JobLedger 50件超のみ実運用トリガー待ちへ分離（`Docs/PENDING_TASKS.md` §3-48）。クローズ記録は`Docs/PENDING_TASKS_CLOSED.md` §3-57 | なし |

### スクリーンショット置き場の推奨

本リポジトリはgit管理下（`main`ブランチ、活発なコミット履歴あり。`Docs/DEVELOPMENT_PLAN.md:73`も「git init済み」と明記）である。スクショは`Docs/`配下に専用フォルダ（例: `Docs/screenshots/real_backend_2026-XX-XX/`）を作成して格納するか、オーナーが別途指定する場所に置く。本書自体にはスクショを埋め込まず、パス参照のみ記録すること。

### 総合判定

- [ ] 全項目合格。グループ3・項目13（実バックエンド通し確認）を完了とし、`Nz-LTX23-backend/Docs/FRONTEND_CATCHUP_WORKORDER.md`および`Docs/DEVLOG.md`に完了の歴史記録を追記する。
- [x] **基本パスは9/9合格（2026-07-18時点）**。マトリクス9項目（4.1〜4.9）すべてでオーナー申告・ログ証跡によりrealバックエンドでの基本パス合格が確認された（4.8モデル管理は2026-07-18のサーバーログ証跡により追加合格）。個別技術チェックのうち`clip`/`clip_count`進捗表示（3.1・4.4）とバッチA2Vの再開シナリオ（4.7）は、同日中のオーナー確認により追加で合格した。**2026-07-19のオーナー追加申告・セッションにより、IC-LoRA強度スライダー・wav自動調整細目・冒頭キーフレームなし品質確認・バッチのnoClobber付番・バッチの開始前チェック・Chainの予想出力尺一致・reuseIfPresent通常再挿入・出力フォルダ自動作成も合格した**（詳細は`Docs/PENDING_TASKS_CLOSED.md` §3-16・§3-17）。**続く2026-07-19の最終判定により、後日明言のなかった残り6細部（IC-LoRA 128グリッドUI・A2Vの5スロット全体併用・スタイルLoRA併用・短すぎる音声の事前ブロック・モデル管理の409排他/危険ゾーンunload→再ロード・chunked_upsampleの768p級ストレステスト〔ジョブ`a2472b84-c8a6-4f04-b84f-6b505f8f5cce`のmetadata.json実読でpeak_vram_mb 9780を確認〕）もすべて合格した**（詳細は`Docs/PENDING_TASKS_CLOSED.md` §3-18）。A2V＋参照動画付きcontrol系IC-LoRA併用は、音声⇔参照動画の相互排他設計によりUI上の入口が無いため確認対象から削除（スコープ外）とした。**続く2026-07-19の最終判定で、reuseIfPresentのリロード後保険経路・危険ゾーン（N4）の表示有無もいずれも合格し、APIキーバッジ（N13）はα版スコープ外（ループバックバインドのためAPIキー検証の実益なし）としてクローズした**（詳細は`Docs/PENDING_TASKS_CLOSED.md` §3-19、[`DEVLOG.md`](DEVLOG.md) §32）。**残っていた個別チェックのうち、短いクリップでのoverlap_frames制約（`kv>=L`）は2026-07-22に機能面合格でクローズし（`Docs/PENDING_TASKS_CLOSED.md` §3-33）、Chain V2VのcutoutRange併用は2026-07-27に未配線と判明して検証対象外で決着した（§4.5、`Docs/PENDING_TASKS_CLOSED.md` §3-48）。** これにより台帳側の実GPU細部リスト（`Docs/PENDING_TASKS.md` §2-1）に残るのは、本書§4.10のドラッグ＆ドロップ5項目（ブリッジ契約v7 `ui.resolveDroppedFiles`）だけになった。**この5項目は2026-07-27の台帳再編で§2-1へポインタとして掲載済みであり、「台帳へ載せずに本書だけで管理する未実施項目」は現在ひとつも無い**（実体のチェック欄と確認手順は引き続き本書§4.10・第4節で管理し、台帳側は所在を指すだけ、という役割分担）。オーナー目視の残項目は`.aul2`日本語フォールバック文言（`Docs/PENDING_TASKS.md` §2-2）のみ。全項目・全細目の完全合格の判定は、残項目の解消後に改めて行う。**2026-07-28追記**: NAGフロントエンド追随に伴い§4.11（NAGの効き確認3項目）を新設した。これも`Docs/PENDING_TASKS.md` §2-1へポインタとして掲載済み。**2026-07-29追記**: NAGの実機ゲートはオーナー実機確認で全項目合格しクローズした（`Docs/PENDING_TASKS_CLOSED.md` §3-51・§3-54）。**2026-07-30追記**: V2Vリボン範囲トリム（§1-6）とバッチi2v-long（§1-7）の実装完了に伴い§4.12（4項目。①の実機採取が前提）・§4.13（7項目）を新設した。いずれも`Docs/PENDING_TASKS.md`側にポインタを置いている（§1-6は同書§1-6の残作業として、§1-7は同書§2-1として）。**同日さらに、オーナー実機確認で§4.13の基本フロー3項目（スキャン・直列生成・出力フォルダ）が合格し、同時に受けたフィードバックでプロンプトを行ごと指定へ再設計した**（バッチ全体プロンプト欄・長い説明文の撤去を含む。経緯は[`DEVLOG.md`](DEVLOG.md) §54）。よって未実施の実GPU項目は、§4.10のドラッグ＆ドロップ5項目＋§4.12の4項目＋§4.13の残り4項目（行ごとプロンプト・中止挙動・相互ロック・50件超描画）である。**2026-07-31追記**: §4.13は残り項目もオーナー実機で合格し全項目合格・クローズした（JobLedger 50件超のみ「実運用でもたつくと感じたら対応する」形で`Docs/PENDING_TASKS.md` §3-48へ分離。クローズ記録は`Docs/PENDING_TASKS_CLOSED.md` §3-57）。**2026-08-01追記**: §4.12もオーナー実機で全項目合格・クローズした（①採取解析②非退行③短尺ゲート④トリム後Joinの中身連続性⑤IC-LoRA参照動画の範囲反映。⑤は同日追加した拡張分。クローズ記録は`Docs/PENDING_TASKS_CLOSED.md` §3-59、実装記録は[`DEVLOG.md`](DEVLOG.md) §56）。あわせてAcceleration（生成の高速化）の実機ゲートも同日に全項目合格した（記録はバックエンド[`VERIFICATION_LOG.md`](../../Nz-LTX23-backend/Docs/VERIFICATION_LOG.md) §43、クローズは`Docs/PENDING_TASKS_CLOSED.md` §3-58。こちらは本書ではなくバックエンド側で管理している項目である）。**したがって2026-08-01時点で未実施として残る実GPU項目は、§4.10のドラッグ＆ドロップ5項目だけ**であり、オーナー目視の残項目は`.aul2`日本語フォールバック文言（`Docs/PENDING_TASKS.md` §2-2）のみである。**2026-08-03追記**: `keep_resident`（モデル骨格のジョブ間キャッシュ）の実GPU項目・目視項目を`Docs/PENDING_TASKS.md` §2-1／§2-2へ追加した（本書にはマトリクス行を設けず、**実体はそちらで管理する**）。**2026-08-05追記**: §4.10のドラッグ＆ドロップ5項目がオーナー実機で全項目合格しクローズした（`Docs/PENDING_TASKS.md` §2-1からは除去済み）。これにより本書§4.1〜§4.13は全項目合格となった。台帳側`Docs/PENDING_TASKS.md` §2-1に残る未確認は、本書ではなくバックエンド[`VERIFICATION_LOG.md`](../../Nz-LTX23-backend/Docs/VERIFICATION_LOG.md) §39.6（MCP承認導線）・§48.9（`keep_resident`4経路）で管理している2項目のみである。**2026-08-06追記**: その2項目もオーナー確認で合格し、`Docs/PENDING_TASKS.md` §2-1は空になった（合格記録は`Docs/PENDING_TASKS_CLOSED.md` §3-67）。**2026-08-10追記**: 台帳の運用変更（全項目が合格して空になった節は見出しごと削除する）により、**`Docs/PENDING_TASKS.md` §2-1は節ごと削除された**。本書の上記各追記に出てくる「§2-1」はすべて当時の記録であり、**現在の参照先は`Docs/PENDING_TASKS_CLOSED.md` §3-67**である。**2026-08-11追記**: 同じ運用により**`Docs/PENDING_TASKS.md` §2-2（オーナー目視）も節ごと削除された**。上記各追記に出てくる「§2-2」（`.aul2`日本語フォールバック文言・`keep_resident`の目視項目）も当時の記録であり、**現在の参照先は`Docs/PENDING_TASKS_CLOSED.md` §3-67・§3-64**である。
