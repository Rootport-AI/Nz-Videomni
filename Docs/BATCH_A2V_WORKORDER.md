# バッチA2V機能 ワークオーダー — 就寝中一括生成のためのGUI機能

> **（2026-07-12 実装完了・歴史記録注記）本書§4の未決5論点はすべて決着し、実装完了した。** 決着内容・実装ファイル・テスト結果・残タスクは以下の「実装完了の要約」ブロックを参照。本文（§1〜§5）はワークオーダー作成時点（実装着手前）の記録としてそのまま残す。現在の正本＝[`NEXT_SESSION_HANDOFF.md`](NEXT_SESSION_HANDOFF.md)冒頭ブロック／[`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §36／CSVマニフェストの共通仕様の正本＝[`BATCH_A2V_CSV_SPEC.md`](BATCH_A2V_CSV_SPEC.md)。

## 実装完了の要約（2026-07-12・§4の決着内容を含む）

### §4の未決5論点の決着

1. **§4.1 Replace切替時の挙動**: オーナー原案（切替時に共通プロンプトを全行へ転記＋Undo必須）ではなく、**対案（非破壊方式）を採用**。行のテキストは常に「個別プロンプトのみ」を保持し、送信時にAdd（共通の末尾に追記）／Replace（行が共通を置換。行が空なら共通にフォールバック）のグローバル切替に応じて合成する。個別プロンプトが空の行を下敷き編集したいニーズには「共通をこの行にコピー」ボタンで対応した。
2. **§4.2 Prompt列の編集方式**: §3で挙げた折衷案（Prompt列のみセル直接編集を許し、image列はドロップダウンが必要なため下部パネル経由）を採用。実装は既存Jobsタブの`.select`パターンを踏襲。
3. **§4.3 Skipの閾値**: 有力案どおり「快適上限は警告のみで表示し、Skip自体は481フレーム基準にする」で確定。解像度別の快適上限（VRAMあふれの無い目安）超過は警告のみでSkipしない。
4. **§4.4 バッチAPIの実装層**: 第一候補（GUI側ループが既存の`POST /generate/chain`を1件ずつ叩く・API側無改修）を採用。ただし、ループ自体はGradioの画面イベント内ではなく、**サーバープロセス内のバックグラウンドスレッド（`BatchRunner`）**に持たせる設計にした。ブラウザを閉じてもタブがスリープしても生成は止まらない（就寝中運用の核心）。画面は2秒間隔の`gr.Timer`が表を再描画するだけの読み取り専用クライアントで、翌朝ブラウザを開き直せば現状がそのまま表示される。
5. **§4.5 CSVマニフェストの形式**: 10列・UTF-8 BOM付き（Excelでダブルクリックしても文字化けしない）・CRLF改行・音声フォルダ直下に`batch_a2v_manifest.csv`として配置、と確定。WebView2フロントエンド（AviUtl2拡張・Reactベース）との共通仕様として、詳細を新設の[`BATCH_A2V_CSV_SPEC.md`](BATCH_A2V_CSV_SPEC.md)に正本化した。

### 実装ファイル

- `gradio_ui/manifest.py`（新規・CSVマニフェストの純ロジック。Gradio/スレッド/HTTPに非依存）
- `gradio_ui/batch.py`（新規・`BatchRunner`。サーバープロセス内デーモンスレッドで動く実行本体）
- `gradio_ui/handlers.py`（既存の単発A2V送信ロジックから`build_a2v_chain_payload`を関数抽出し、バッチ側と共有。凍結A2V送信とバイト等価であることを維持）
- `gradio_ui/ui.py`（Batch A2Vアコーディオンの配線・2秒間隔`gr.Timer`によるポーリング表示）
- `gradio_ui/i18n.py`（EN/JA文言48キー追加）

### テスト結果・品質記録

- **pytest 574 passed / 1 skipped**（着手前基準の526+1=527 → +48・退行ゼロ）。新規テストファイル3本（`tests/test_gradio_batch_manifest.py`・`tests/test_gradio_batch_runner.py`・`tests/test_gradio_batch_fullstack.py`）＋既存`tests/test_gradio_handlers.py`の更新。
- **独立レビュー実施済み**: must-fix（必須修正）1件（画像フォルダの解決処理の不具合）を修正済み。should-fix（推奨修正）3件のうち2件（一時ファイルの掃除漏れ・Set audios実行中のロック不足）を修正済み。残り1件（サーバー側`outputs`/`uploads`フォルダの自動削除）はオーナー判断待ちとして記録のみ（下記「残タスク」参照）。
- フルスタックテスト（実サーバー・mockバックエンド経由）で、失敗継続・再開・出力ファイルの付番・Add/Replaceモードの実送信を検証済み。

### 残タスク（2026-07-12更新）

1. ~~**GPU実機目視ゲート（オーナー立ち会い）**~~ → **合格・完了（2026-07-12）**。詳細は下記「GPU実機目視ゲート合格・オーナー決定2件・修正ラウンド」節。
2. ~~**オーナー判断待ち2件**~~ → **決定済み（2026-07-12）**。詳細は下記節。
3. **WebView2フロントエンド（AviUtl2拡張・別リポジトリ）側のCSVマニフェスト対応**は本リポジトリのスコープ外（別リポ課題）。[`BATCH_A2V_CSV_SPEC.md`](BATCH_A2V_CSV_SPEC.md)を正本として実装すること。
4. ~~**git commit**＝オーナー指示待ち~~ → **2026-07-13 コミット＆プッシュ完了（18fb08e／2ccf8fb、origin/main）。本ワークオーダーの課題は全て完結。**

### GPU実機目視ゲート合格・オーナー決定2件・修正ラウンド（2026-07-12追記）

**GPU実機目視ゲート＝合格（オーナー確認済み）。** 実ログで確認された動作: 3行バッチ（うち1行はSkip）の2行連続生成→就寝想定の完走、ガチャ再生成（同じセリフの撮り直し）で`_2`〜`_4`の自動付番（既存mp4は無傷）、停止ボタンで「実行中1件は完走→次行を投入せず停止」という設計どおりの挙動（ログに`stop requested, halting before row 2`）、再開時にDone/Skip行を正しくスキップ、日本語ファイル名の音声で正常動作、解像度変更（1280x768→896x1152）がスナップショットへ正しく反映されること。オーナーの評価は「事前に期待していた要件はいずれも満たしている」。

**オーナー決定2件（上記「残タスク」2番の決着）**:

1. サーバー側`outputs`/`uploads`フォルダの掃除は**ユーザーの手動削除で運用**と決定。UI側に削除機能は追加しない。理由＝ローカル専用ツールでありユーザーは日常的にエクスプローラーで`\outputs`を直接操作するため、直接操作のほうが便利という判断。
2. 行の個別プロンプトでの`<lora:名前:強度>`トークン指定は**将来の研究課題へ格下げ**。ユーザーから要望が出た時点で検討する。現状どおりLoRAは共通プロンプト側／バッチ開始時のGenerateタブ設定（スナップショット）で全行共通のまま。

**実機フィードバックに基づく修正5点（実装済み・pytest 574 passed / 1 skipped＝退行ゼロ）**:

1. バッチ実行中はGenerateボタンが「Batching a2v...」表示に変わり押下不可になり、完了で「Start a2v batch」に復元される（誤操作による二重投入の防止。i18nキー`batch_running`を新設）。
2. Batch A2Vアコーディオン内の要素を実際の操作順に並べ替え: Enableパネル（`gr.Group`にEnable／音声フォルダ／画像フォルダ／出力先〔Auto/Custom〕を集約）→ Prompt mode（Add/Replace）はSet audiosボタンの直上 → Set audios → 表 → 表の直下にmax durationとサマリ（Done/Failed/Skip/Waiting）を横並びで集約 → 行編集パネル（画像ドロップダウン・行情報）→ 共通プロンプトコピー → 行再生成 → 停止ボタン。
3. 表の列名を短縮: queue#→「#」、Duration→「dur.」（表示崩れ対策）。
4. `gr.Dataframe`の`column_widths`でPrompt列の初期幅を41%確保（Set audios直後からセルをクリックして編集しやすくした）。
5. コンソールログの静音化: uvicornのアクセスログから`/ui/gradio_api/`を含む行（Gradioの内部イベント通信。2秒間隔の表更新タイマー等が出す大量ログ）を除外するフィルタを`main.py`に追加（既存の`JobPollingAccessFilter`と同方式）。GradioがStarletteの古い定数を使うことで出る`StarletteDeprecationWarning`もメッセージ・カテゴリ限定で抑制。`/api/v1/*`のアクセスログと生成進捗ログは従来どおり表示され、表更新タイマーの間隔（2秒）自体は変更なし。

詳細な検証記録＝[`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §36.6〜§36.8。次セッション引き継ぎの正本＝[`NEXT_SESSION_HANDOFF.md`](NEXT_SESSION_HANDOFF.md)冒頭ブロック。

### 第2次フィードバック対応（2026-07-12・実装済み・pytest 585 passed / 1 skipped）

上記の修正5点をオーナーが実機で使い込んだ結果、さらに6点のフィードバックが寄せられ、同一セッション内で実装・検証まで完了した。

1. **表の横スクロール復活**: 列幅を固定ピクセル（#=50px／音声=220px／dur.=70px／画像=140px／Prompt=480px／状態=90px／出力=220px、合計1270px）に変更した。Gradioの列幅は割合指定だとビューポート幅に常に収まる仕様のため、以前はPrompt列が実質的に潰れて折り返しが多発していた。固定ピクセル化により、合計幅がパネル幅を超えると表に横スクロールバーが出るようになった。なおセル内の折り返し自体は禁止していない（480pxを超える長文プロンプトは折り返して表示される）。
2. **表示位置の調整**: max durationとサマリ（Done/Failed/Skip/Waiting）の行を「Set audios」ボタンと表の間（表の直上）に移動した。
3. **Start押下時のSkip再判定**: バッチ開始時に、処理対象の全行についてフレーム数とSkip判定を、その時点のフレームレートで再計算するようにした。効果は次の2点: (a) 手動でWaitingに戻した上限超過行も、開始時に確実にSkipへ再マークされる。(b) 「Set audios」後にfpsや解像度を変更しても、フレーム数は開始時の設定で計算し直される（**注意**: 行のframes値を手で編集していても、開始時にこの再計算で上書きされる）。判定・再計算がすべて通ってからCSVへ書き込みバッチを開始する設計にしたため、開始不可のときは表もCSVも一切変化しない。
4. **Regenerateガード**: Skip行を選んで「この行を再生成」を押しても、無条件にWaitingへ戻すのではなく、理由付きの警告を出して止めるようにした。
5. **フールプルーフ（開始前チェック）**: オーナー承認済みの判定表にもとづき実装。検査対象は処理対象行（Waiting／Failed／クラッシュ遺残）のみ。
   - **プロンプト**: 共通プロンプトが入力済みなら開始可。共通プロンプトが空の場合、Addモードなら開始不可（「共通プロンプトを入力するかReplaceに切り替えて」の旨を案内）。Replaceモードなら、処理対象行のプロンプトが全部埋まっていれば開始可、1行でも空なら開始不可（空の行数を表示する）。
   - **画像**: 共通キーフレーム画像の**1枚目**（Keyframe imagesのスロット1＝先頭フレーム0。Useチェックがオン かつ 画像が入っていることが条件）が設定済みなら開始可。未設定の場合（スロット2以降だけ設定されている場合を含む）、処理対象行が全部個別画像指定になっていれば開始可、"Shared"のままの行が1つでも残っていれば開始不可（2026-07-13、判定強化。詳細後述）。
   - 狙いは、入力忘れという単純ミスで一部の行だけ走ってFailedが量産される事態を防ぐこと。全行に個別入力を済ませた使い方（Replace＋全行個別プロンプト、全行個別画像）は従来どおり問題なく開始できる。
6. **テスト**: 既存574件＋新規11件＝**585 passed / 1 skipped**。フルスタックテストのうち「音声が短すぎてサーバー側422になるシナリオ」を検証していた1本は、Start押下時のフレーム数再計算により、そのエラー自体が到達不能になった。テストの狙い（再開機能の検証）は維持したまま、失敗ベクタを「存在しない個別画像を指定した行」に差し替えて対応した。

詳細な検証記録＝[`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §36.9。次セッション引き継ぎの正本＝[`NEXT_SESSION_HANDOFF.md`](NEXT_SESSION_HANDOFF.md)冒頭ブロック。

### 画像判定の強化（2026-07-13・実装済み）

開始前チェックの画像判定を、「共通キーフレーム画像が1枚でもあればOK」から「**1枚目（スロット1・先頭フレーム0）が入っているか**」に厳密化した。ui.py側のdispatchはもともとUseチェックがオンかつ画像ありのスロットしか`shared_images`へ集めないため、「Useチェックがオンか」という条件はこの変更以前から担保済み（今回はそこに変更なし）。`_validate`（`gradio_ui/batch.py`）は`shared_images`に`frame_idx==0`のエントリが含まれるかで判定するよう変更し、含まれない場合（空、またはスロット2以降のみ）は従来どおり処理対象行が全部個別画像指定ならOK、"Shared"行が残っていれば開始不可とする。個別画像ファイルの実在チェックは今回も追加していない（オーナー決定）。文言（`batch_msg_no_common_image`）も「1枚目」が要件だと分かるよう更新した。テストは既存585 passed / 1 skippedに新規3件を追加し、**589 tests / 588 passed / 1 skipped**（0 failures）。

---

- 作成: 2026-07-12（オーナーとの合意事項を整理・次セッション着手前のワークオーダー）
- 正本: 本書。着手後の状態遷移は本書の冒頭に歴史記録ブロックを追加していく運用とする（他の `*_WORKORDER.md` と同じ体裁）。
- 併読: [`NEXT_SESSION_HANDOFF.md`](NEXT_SESSION_HANDOFF.md)（次セッションの課題としての位置付け）／[`A2V_DESIGN.md`](A2V_DESIGN.md)（A2V＝音声から動画を生成する機能。アップロードした音声の長さに合わせて映像を生成する、の設計正本）／[`RESOLUTION_DURATION_CAPABILITY.md`](RESOLUTION_DURATION_CAPABILITY.md)（481フレーム上限・解像度別の快適上限の算出根拠）／[`chain_math.py`](../chain_math.py)（尺算術の実装）

**本書の位置付け（歴史記録＝ワークオーダー作成時点の記述）**: まだ実装は開始していない。設計・要件の合意事項と、次セッション冒頭でオーナーに確認すべき未決論点をまとめたワークオーダーである。実装着手時はこのファイルの内容を歴史記録として残しつつ、実装計画・実装結果を追記していくこと。→ **上記「実装完了の要約」のとおり実施済み。**

---

## 1. 背景と動機

想定ユーザーは、ゆっくり実況・VOICEROID実況（音声合成ソフトによるキャラクター実況動画）の制作者。20分の動画1本に対して音声素材が100〜200個あり、そのすべてに i2v（画像から動画を生成する機能）による立ち絵の口パクアニメーションが必要になる。

i2v 生成は1本あたり4〜5分かかる（RTX 4070 Ti SUPER 実測）。100〜200本を1件ずつ手動で生成していては到底終わらないため、就寝中に一括投入して朝までに全件仕上げる「バッチA2V」機能が必須になる。

## 2. 確定済みのUX設計（オーナー合意・2026-07-11〜12）

### 2.1 表（マニフェスト）が正典

ファイル名から音声・画像・プロンプトを自動的に結びつける機能は**採用しない**。理由は次の2点:

- ユーザーにファイルのリネームという余計な手間を負わせたくない。
- 音声ファイル名は VOICEROID が吐き出したままの形（連番＋台詞の一部など）で使う前提であり、命名規則に依存した自動結びつけは事故りやすい。

同様の理由で、**表情プリセット機能（喜怒哀楽などを定型ボタンで選ぶ方式）も不採用**。オーナーの判断は「演技は主観的な表現であり、LTX 2.3 は自然言語プロンプトがよく効くモデルなので、表情を定型化するとむしろ表現の幅を狭める」というもの。プロンプトは自由記述のテキスト欄に任せる。

### 2.2 操作フロー

1. wav フォルダのパスを入力する。
2. 画像フォルダのパスを入力する。
3. 「Set audios」ボタンを押すと、wav フォルダをスキャンして表（マニフェスト）を生成・表示する。
4. 表の上で、必要な行だけ手動編集する（画像の割り当て・プロンプトの上書きなど）。
5. 「Generate」ボタンでバッチ生成を開始する。

### 2.3 表の列構成

| 列 | 内容 |
|---|---|
| queue# | 実行順を表す通し番号 |
| wav名 | 音声ファイル名（VOICEROIDが吐いたままの表記） |
| Duration | 音声の長さ |
| image | ドロップダウン選択。既定値は "Shared"（Generate タブで設定する共通のi2vキーフレーム画像を指す） |
| Prompt | テキスト入力（行ごとの個別プロンプト） |
| Stat | Waiting / Generating / Done / Failed / Skip のいずれか |

### 2.4 プロンプト合成モード（Add / Replace）

行ごとの個別切り替えは**α版では見送り**（要望が多ければ次段で検討）。**グローバルなラジオボタン**で全行に一括適用する:

- **Add**: 共通プロンプト（Generate タブの共通欄）の末尾に、行のテキストを追記する。
- **Replace**: 行のテキストが共通プロンプトを丸ごと置換する。

### 2.5 共通画像未設定時のガード

共通のi2vキーフレーム画像の**1枚目**（スロット1・先頭フレーム0。Useオン＋画像ありが条件）が未設定の状態で、image列が "Shared" のまま（＝行ごとの個別画像指定が無い）行が1つでもあると、生成開始時にエラーで停止する（2026-07-13、判定強化）。

### 2.6 尺の上限表示とSkip

パス入力欄の近くに「max duration: ***f(**s)」（フレーム数・秒数）を表示する。上限を超える音声はバッチから除外し、表上でグレー表示＋理由表示の **Skip** 扱いとする。算出は既存の算術（481フレーム上限・解像度別の快適上限）をそのまま流用する（[`RESOLUTION_DURATION_CAPABILITY.md`](RESOLUTION_DURATION_CAPABILITY.md)・`chain_math.py`）。

### 2.7 行の並び順

既定は**ファイル更新日時順**。VOICEROIDは台本の順番どおりにファイルを保存するため、更新日時順にしないと台本の並びと一致しない（ファイル名の五十音順ソートだと台本の順番からズレる）。

### 2.8 出力ファイル名

音声ファイルと同名にする（例: `こんにちは.wav` → `こんにちは.mp4`）。AviUtl2（動画編集ソフト）のタイムラインへ生成結果を戻す際、音声と動画の対応付けをファイル名で行えるようにするため。

### 2.9 CSVマニフェストの永続化

表はCSVとして音声フォルダの隣に自動保存し、再スキャン時に読み戻す。これにより、やり直しや追加生成（既に生成済みの行をスキップして続きだけ回す、など）に耐える設計にする。

**CSV形式はWebView2フロントエンド（AviUtl2拡張・Reactベース）と共通仕様にし、Gradio GUIとWebView2フロントエンドの両方で相互運用できるようにする。** 列定義・文字コードなど具体的な形式は未確定（§4.5参照）。

### 2.10 バッチ実行の逐次投入・耐障害性

バックエンドは同時1ジョブ制（2件目以降は409で拒否される仕様）のため、GUI側が表の上から1件ずつ逐次投入するキューを持つ。

- **1件失敗しても全体を止めず、次の行へ進む。**
- 完了時に成功／失敗の一覧を表示する。
- **途中再開（既に生成済みの行はスキップする）が必須**。就寝中に実行し、翌朝に結果を確認する運用が前提のため、電源断・アプリ再起動などがあっても再実行で続きから拾えるようにする。

## 3. 技術的裏取り済み事項（Gradio 6.19.0 実ソース確認済み）

表の実装方式を検討する上で、Gradio（Python製のGUIフレームワーク。本バックエンドのWebGUIはこれで構築されている）の `gr.Dataframe` コンポーネントの実際の機能を現物のソースで確認した。

- **`gr.Dataframe` にセル内ドロップダウンは存在しない**（`choices` に相当するパラメータが無い）。列ごとに選択式のセルを持たせる機能は無い。
- 使える機能は次のとおり:
  - `interactive=True` によるセル編集。
  - `static_columns`（特定の列だけ読み取り専用にする。ただし `col_count` を固定する副作用がある）。
  - `.select` イベント（`evt.index=(row, col)`・`evt.row_value` でクリックされたセルの位置と行データを取得できる）。
  - **`.edit` イベント**（セル単位の差分を取得できる。`index`・`previous_value`・`value`）。
  - `max_height`（スクロール可能な高さ指定。ページネーション機能は無い）。
- **推奨構成**: 表と `.select` イベントを組み合わせ、選択した行を下部の編集パネル（`gr.Dropdown` ＋ `gr.Textbox`）へロードする方式。これは**既存の Jobs タブの `jobs_table` / `on_job_select`（`gradio_ui/ui.py`・`gradio_ui/handlers.py`）と同型**であり、実装パターンを流用できる。
- **折衷案**: Prompt 列だけ `interactive=True` にして `.edit` の差分検知でセル直接編集を許し、image 列（ドロップダウンが必要）は下部パネル経由にする、という組み合わせも有力。
- **Stat 列の更新**は `gr.Timer` の `.tick` によるポーリングで実現する（既存の `config_retry_timer` に前例がある・`gradio_ui/ui.py`）。
- **WebView2側（React・AviUtl2拡張フロントエンド）は、セル内に `<select>` や `<textarea>` を自由に組み込めるため**、モック通りのドロップダウン付き表がそのまま作れる。想定行数（200行程度）であれば仮想化（画面外の行を描画しない最適化）は不要と見積もる。

## 4. 未決の設計論点（次セッション冒頭でオーナーに確認すること）

以下5点は本セッションでは決着していない。実装着手前に必ずオーナーへ確認する。

### 4.1 Replace切替時の挙動

- **オーナー原案**: Add⇔Replace の切替時に、共通プロンプトの内容を全行のテキスト欄へ転記する。この場合、誤操作で個別プロンプトが上書き消失するリスクがあるため、**Undo（元に戻す）機能が必須**になる。
- **対案（非破壊方式）**: 行のテキストは常に「個別テキストのみ」を保持し、送信時にモード（Add/Replace）に応じてサーバー側・GUI側で合成する。この方式ならモード切替でデータが壊れないため Undo は不要。個別テキストが空の行を「共通プロンプトで下敷き編集したい」というニーズには、「共通をこの行にコピー」ボタンを別途用意して対応する。

どちらを採るかは実装コストと事故りやすさのトレードオフであり、次セッション冒頭でオーナーに確認する。

### 4.2 Prompt列の編集方式

- セル直接編集（`.edit` イベントで差分検知）か、下部パネル経由か、両者の折衷か（§3参照）。UXの手触りに関わるため、実装前に方向性を決める。

### 4.3 Skipの閾値

- 481フレーム（API上限）基準か、解像度別の快適上限基準かで迷いがある。**現時点で有力なのは「快適上限は警告のみで表示し、Skip自体は481フレーム基準にする」案**だが、確定はしていない。

### 4.4 バッチAPIの実装層

- **第一候補**: GUI側のループで、既存の `POST /generate/chain` を1件ずつ叩く（API側は無改修）。凍結API契約を壊さない点で望ましい。
- サーバー側にバッチ専用エンドポイントを新設する案は、当面は不要と思われるが、要件を詰める過程で必要性が出てくる可能性もあるため要確認。

### 4.5 CSVマニフェストの形式

- 列定義・文字コード（UTF-8 with BOM か否かなど、Excelでの開きやすさも考慮）・配置場所（音声フォルダ直下か、隣接する別フォルダか）が未確定。**WebView2フロントエンド側との共通仕様にする**という方針だけは決まっているため、フロントエンド側の実装状況・都合も踏まえて次セッションで確定する。

## 5. 次のアクション

1. 次セッション冒頭で、上記§4の5論点をオーナーに確認し決定を得る。
2. 決定内容に基づき実装計画を提示し、オーナー合意のうえで着手する（本プロジェクトの運用ルール＝コード着手前に実装計画を提示して合意）。
3. 実装はGUI層（`gradio_ui/`）中心になる見込みだが、§4.4の結論次第でバックエンドAPI層に手が入る可能性もある。
4. 目視ゲート（実際に就寝中運用を想定したバッチ生成が問題なく回るか）はオーナー立ち会いで実施する。
