# クリップ連結（Clip Chain）の stage-2 と条件付け 研究ノート

本書は、クリップ連結（Clip Chain＝複数のクリップを1本の連続した動画としてつないで生成する機能）の内部構造について、2026-08-05〜06の調査で分かったことを集約する置き場である。台帳（フロントエンド[`PENDING_TASKS.md`](../../Nz-LTX23-frontend-AviUtl2/Docs/PENDING_TASKS.md)）は優先度順の一覧を保つための文書なので、調査の中身は本書に置き、台帳側は本書への相対リンクで委譲する。実装の実測値そのものの正本は[`VERIFICATION_LOG.md`](VERIFICATION_LOG.md)の各節であり、本書は「なぜ今こうなっているか」と「次に何を足せるか」を見比べるための整理用ノートという位置づけである。

## 現在地（2026-08-06時点）

クリップ連結は2段構えで動いている。前半（stage-1）は半分の解像度でクリップごとに区切って生成し、末尾の一部を次のクリップへ凍結して引き継ぐことで連続性を作る。それらをつないで1本の長い潜在表現（latent＝映像を圧縮した内部表現）にまとめ、空間方向に拡大したうえで、後半（stage-2）が**時間方向の固定窓を少しずつずらしながら**仕上げ直す。全体の設計は[`chain_math.py`](../chain_math.py)の冒頭コメントが正本である。

この構造から出てくる、直感に反する重要な帰結が2つある。

1. **stage-2の窓はクリップの長さにも本数にも関係なく22潜在フレームで固定**である。したがってチェーンの快適上限は「フレーム数」ではなく「解像度」で決まる。
2. **キーフレーム画像はstage-1とstage-2の両方へ注入されるが、プロンプトはstage-2ではクリップ0のもの1本しか使われない**。つまりプロンプトと画像で扱いが非対称である。

以下、それぞれの根拠と、そこから見える拡張候補を述べる。

---

## 1. stage-2の固定窓（22潜在フレーム）の由来と制約

### 何が固定されているか

[`chain_math.py`](../chain_math.py) 44〜50行に、次の3つの定数がある。

- `STAGE2_V_TILE = 22` — stage-2が一度に扱う窓の長さ（潜在フレーム単位）。ピクセルに直すと169フレーム。
- `STAGE2_V_ADV = 18` — 窓を次へずらす量。ピクセルに直すと144フレーム。
- `STAGE2_KT_V = 4` — 隣り合う窓の重なり（のり代）。ここは前の窓の結果で上書きして固く凍結し、そのうえで混ぜ合わせる。

この22は、クリップの長さにもクリップの本数にも一切依存しない。短いチェーンでは窓が1つに縮退するだけで、機構としては常にタイル化されている。

### なぜタイル化が必要なのか

モデルの時間方向の位置埋め込み（RoPE＝Rotary Position Embedding。トークンに「これは何秒目のどこか」を教える仕組み）が、**学習時に20秒までしか見ていない**ためである。`.venv-engine/Lib/site-packages/ltx_core/model/transformer/model.py` 78〜79行に映像側の既定値 `positional_embedding_max_pos = [20, 2048, 2048]`（先頭が時間軸の秒数）、92行に音声側の `audio_positional_embedding_max_pos = [20]` がある。

一方でクリップ1本の最大は481フレーム＝毎秒24フレームで20.04秒であり、この天井をわずかに超える。チェーンはそれを何本もつなぐのだから、天井を超えるのは確実である。**窓分割は「あれば速い」という最適化ではなく、超えたら壊れるから必須の措置**である。各窓は内部の位置を0から数え直すので、窓の中では常に天井の内側にとどまる。

### 22という数字の由来

2026-07-03のスパイク（試作実験）[`outputs/phase3_clip_concat_spike/s2_tiled_spike.py`](../outputs/phase3_clip_concat_spike/s2_tiled_spike.py) 123〜125行で決めた値を、そのまま製品へ凍結したものである。合成の内訳は次のとおり。

- 前進18潜在フレーム＝144ピクセルフレーム＝毎秒24フレームでちょうど6.000秒。音声側の潜在フレームは毎秒25なので、6.000秒はちょうど150フレームになり、**丸め誤差がゼロ**になる。映像と音声の窓割りを整数のまま揃えられるのが18を選んだ理由である。
- のり代4を足して22。

**窓サイズを振ってみる実験（スイープ）は実施していない。** 22は「動いた最初の値」であって、最適値として選ばれた値ではない。

また調査の範囲では、上流（LTX公式）にも同梱の`vendor/LTX-2`にもComfyUIにも、**時間方向にスライディング窓でdenoiseする先行実装は見つからなかった**（上流にあるタイル化はVAE＝映像の圧縮伸長のタイル化であって、denoiseのタイル化ではない）。重なりを作って混ぜるという発想だけはComfyUIのlooping sampler系の調査を踏襲しているが、この構造自体は当方の独自設計である。

### 帰結: チェーンの上限は「解像度」で決まる

VRAM（グラフィックスメモリ）から溢れて共有メモリへ退避する現象（spill）が始まるのは、経験的におよそ**40,000トークン**である。ここでいうトークン数は「空間のマス目の数 × 潜在フレーム数」で、空間のマス目は縦横それぞれ32ピクセルにつき1つである。この40,000という値は[`config.yaml`](../config.yaml) 179〜184行の実測テーブル `limits.spill_free_frames` の上側3点から逆算したものである。

| 解像度 | 空間のマス目 | 実測の快適上限フレーム | 潜在フレーム | トークン |
| --- | --- | --- | --- | --- |
| 1280x768 | 960 | 257 | 33 | 31,680 |
| 1920x1088 | 2,040 | 153 | 20 | 40,800 |
| 2560x1472 | 3,680 | 81 | 11 | 40,480 |

チェーンでは潜在フレーム数が22で固定されるので、上限は次のように出る。

- 40,000 ÷ 22 ≒ **1,818マス** → 幅×高さがおよそ **1.86メガピクセル** まで。
- 単発生成で1080p級（153フレーム＝20潜在フレーム）を選んだ場合は 40,000 ÷ 20 ＝ 2,000マス ＝ 約2.05メガピクセルまで許される。**チェーンはそれより9〜10%低い**ことになる。

実際の当てはめ（2026-08-05にオーナー実機で確認した1216x1664の事例を含む）。

| 解像度 | マス目 | ×22 | 判定 |
| --- | --- | --- | --- |
| 1152x1536 | 1,728 | 38,016 | clean |
| 1216x1472 | 1,748 | 38,456 | clean |
| 1152x1600 | 1,800 | 39,600 | 境界 |
| **1216x1664** | **1,976** | **43,472** | **8.7%超過（必ず溢れる）** |

**1216x1664は、クリップを何秒にしようと本数を何本にしようと、必ず溢れる。** 2026-08-05のオーナー実機で2〜3GBのspillが実測されている。この帯域は経験則として生成時間が2倍前後に悪化する。

### 現状のUIギャップ

フロントエンドのspill警告は、単発生成用のしきい値テーブルを「面積が最も近いキー」へ丸め込んで解決している（[`webui/src/modes/create/spillUtils.ts`](../../Nz-LTX23-frontend-AviUtl2/webui/src/modes/create/spillUtils.ts) 16〜41行の`resolveSpillFreeFrames`、および[`webui/src/timeline/deriveDuration.ts`](../../Nz-LTX23-frontend-AviUtl2/webui/src/timeline/deriveDuration.ts) 97行）。

1216x1664（面積2,023,424）に最も近いキーは1920x1088（面積2,088,960）なので、**153フレームという上限が返り、それを下回っている限り警告が出ない**。しかしチェーン経路では上で見たとおりフレーム数と無関係に溢れる。つまり**チェーン特有の下限を誰も見ていない**というギャップがある。これは台帳の§1-14として起票した。

### 窓19という選択肢

窓を19（前進15＋のり代4）にすると、次のようになる。

- 前進15潜在フレーム＝120ピクセルフレーム＝ちょうど5.0秒 → 音声側125フレームちょうどで、**丸め誤差ゼロという条件を保てる**。
- 窓19のピクセル長は145フレーム。これは動画から動画への継続生成（V2V）が凍結できる頭の最大長145フレームとちょうど一致するので、[`api/models.py`](../api/models.py) 379〜383行の契約（凍結した頭はstage-2の最初の窓の内側に収まらなければならない）も**そのまま保てる**。
- 1216x1664のトークンは 1,976×22＝43,472 から **1,976×19＝37,544** へ下がり、40,000を6%下回ってcleanになる見込みである。

代償は窓の数が増えることである。窓が増えれば継ぎ目も増えるので、継ぎ目のずれ（drift）が悪化しないかを実写のA/B比較で確かめることが前提になる。台帳の§3-57として起票した。

---

## 2. キーフレームと条件付けの機構（単発の2段生成）

### プロンプトと画像は扱いが非対称

- **プロンプト**: stage-1で解釈され、stage-2は単一のコンテキストを使い回す。チェーンではクリップごとに別のプロンプトを書けるが、[`engine/pipeline/chain_pipeline.py`](../engine/pipeline/chain_pipeline.py) 788行が `stage2_vctx, stage2_actx = seg_ctx[0]` としているとおり、**stage-2で使われるのはクリップ0のプロンプト1本だけ**である（548行でクリップごとの符号化は作ってある）。
- **画像条件**: [`vendor/LTX-2/packages/ltx-pipelines/src/ltx_pipelines/distilled.py`](../vendor/LTX-2/packages/ltx-pipelines/src/ltx_pipelines/distilled.py) 118〜127行と145〜152行が、**同じ`images`から半分の解像度用と最終解像度用の条件を別々に作り、stage-1とstage-2の両方へ渡している**。チェーン側も同じで、[`engine/pipeline/chain_pipeline.py`](../engine/pipeline/chain_pipeline.py) 671〜672行（stage-1・半解像度）と831〜832行（stage-2・全解像度）で二度注入している。

### frame 0 と frame>0 で意味が違う

[`vendor/LTX-2/packages/ltx-pipelines/src/ltx_pipelines/utils/helpers.py`](../vendor/LTX-2/packages/ltx-pipelines/src/ltx_pipelines/utils/helpers.py) 132〜166行の`combined_image_conditionings`が分岐している。

- **frame 0 の画像**: 潜在表現の先頭を符号化した画像で直接置き換え、マスクで固定する。実質的に凍結であり、stage-2でも忠実度が保たれる。
- **frame>0 のキーフレーム**: `.venv-engine/Lib/site-packages/ltx_core/conditioning/types/keyframe_cond.py` 10〜45行のとおり、**誘導用のトークンを系列の末尾に付け足し、その位置情報（RoPE）を指定フレームぶんずらす**方式である。ピクセルを固定するわけではない。したがって「そのフレームがその絵になる」ではなく「そのあたりでその絵に寄っていく」という**ソフトな誘導**として理解するのが正しい。

### stage-2は「拡大するだけ」ではない

stage-2のノイズ量は `STAGE_2_DISTILLED_SIGMA_VALUES = [0.909375, 0.725, 0.421875, 0.0]`（[`vendor/LTX-2/packages/ltx-pipelines/src/ltx_pipelines/utils/constants.py`](../vendor/LTX-2/packages/ltx-pipelines/src/ltx_pipelines/utils/constants.py) 19行）である。**σ＝0.909という高いノイズ量から3ステップかけて生成し直している**のであって、単なるアップスケールではない。ここで構図が動きうるという事実が、次節の設計判断（stage-1だけに注入しても足りない）の根拠になる。

---

## 3. クリップごとに指定できる入力の現状

| 入力 | 現状 | 根拠 |
| --- | --- | --- |
| プロンプト | クリップごとに指定可。ただしstage-2はクリップ0の1本のみ | `chain_pipeline.py` 548・788行 |
| キーフレーム画像（`conditioning_images`） | **クリップ0のみ**・最大5枚。frame>0もクリップ0の中でなら可（UIは1枚・frame 0固定） | [`api/models.py`](../api/models.py) 624〜633行 |
| 参照音声（A2V＝音声から動画を作る機能） | **クリップが1本のときだけ**。全長を凍結する | [`api/models.py`](../api/models.py) 575行 |
| 参照動画（IC-LoRA control系＝参照動画から輪郭線や骨格を読み取って条件付けするアダプタ） | **クリップが1本のときだけ**（2本以上は422 `LORA_CONTROL_UNSUPPORTED_IN_CHAIN`）。参照はstage-1にのみ注入する（単発生成も同じ意味論） | [`api/models.py`](../api/models.py) 669行、[`api/generate_chain.py`](../api/generate_chain.py) 112行、[`api/errors.py`](../api/errors.py) 221行、`chain_pipeline.py` 675〜676行 |
| スタイルLoRA | チェーン全体に一律。クリップごとの強度指定はv1では採らないというオーナー裁定 | — |

なお`chain_pipeline.py` 719行が `conds = []` としているとおり、**クリップ1本目より後ろのstage-1には画像条件が一切入っていない**。次節の拡張はここが起点になる。

---

## 4. 拡張候補の実現性（2026-08-05の調査）

### (1) 中間クリップのキーフレーム

2本目以降のクリップにもキーフレーム画像を指定できるようにする案。

- 触る場所は2つ。stage-1の `i >= 1` の分岐（`chain_pipeline.py` 719行、現状は空リスト）へ注入することと、stage-2の `clips[0].images` という決め打ち（同831〜832行）を外すこと。
- **新規に書き起こすロジックは1つだけ**で、「クリップの中でのローカルなフレーム位置」を「つなげたあとのタイムライン上のグローバルな位置」へ変換する処理である。それ以外は既存の配管の流用で済む。
- **stage-1だけに入れる案は推奨しない。** 前節のとおりstage-2はσ＝0.909から作り直すので、クリップ0だけがstage-2でも条件を持ち、他のクリップは持たないという非対称な状態になると、2本目以降だけ構図が緩む。両段へ対称に入れるべきである。
- 台帳の§3-58として起票した。

### (2) クリップごとの参照動画（長尺IC-LoRA）

チェーン全体を1本の参照動画で制御できるようにする案。

- 参照はstage-1にのみ注入するという**既存の意味論をそのまま `i >= 1` へ広げるだけ**でよい。単発生成と意味論が揃うので、説明も一貫する。
- つなぎ目の整合は自動的に成り立つ。元の1本の参照動画を、クリップの重なり幅ぶんだけオーバーラップさせて切り出せば、引き継ぎ（carry）の対象になる区間の参照映像が前後のクリップで一致するためである。
- **切り出しはサーバー側で自動的に行うべき**である。8n+1というフレーム数の刻みや、重なり幅の計算をユーザーにさせるのは現実的でない。
- **VRAMの上限は悪化しない。** 参照はstage-2には入らないので、前節で見た22窓のトークン計算には影響しない。
- 台帳の§1-15として起票した。

### (3) クリップごとの参照音声（長尺A2V）

2本目以降のクリップでもA2Vを使えるようにする案。

- チェーンは**もともと音声を映像と同時に生成してつなぐ機構を持っている**（セグメントごとの音声窓の計算、つなぎ目のクロスフェード、stage-2の音声タイル）。A2Vはそこへ「生成する代わりに、符号化済みの音声で置き換えて凍結する」という差し替えを入れたもので、クリップ1本の場合はすでに実装済みである（`chain_pipeline.py` 720〜731行がstage-1、822〜829行がstage-2）。しかもstage-1側の差し替えは**すでにセグメントごとの音声窓 `a_seg_windows[i]` を見て書かれている**ので、複数クリップへ広げる下地はコード上に残っている。実際に塞いでいるのはAPI層のガード1行（[`api/models.py`](../api/models.py) 575行）である。
- 複数クリップ化は、つなげた音声潜在表現を窓ごとに凍結するという形になる。**凍結した領域は重なり部分でも中身が同一**なので、接合の整合は自明に成り立つ。
- 配管の長さは参照動画（(2)）より短くて済む見込みである。
- 台帳の§1-16として起票した。

---

## 5. UI設計の方向性（2026-08-06のオーナー方針・実装時に確定）

- **参照動画・参照音声の入力欄はChainタブ専用に新設する。** Createタブの入力欄を流用せず分離し、クリップ連結はChainタブ側の入力だけを見る。どちらの欄が効いているのか分からなくなる混乱を避けるためである。
- **中間クリップのキーフレームは、クリップカードの中に「Keyframes／キーフレーム制御」のオン・オフ切替を置き、オンのときだけスライダー等を表示する。** クリップ枠は最大24あるので、常時表示にすると画面が冗長になる。

---

## 次の一歩

台帳（[`PENDING_TASKS.md`](../../Nz-LTX23-frontend-AviUtl2/Docs/PENDING_TASKS.md)）へ起票した5件が本書の出口である。

- §1-14 チェーン経路の解像度警告（現状のギャップ解消。上記1節）
- §1-15 クリップごとの参照動画（長尺IC-LoRA。上記4-(2)）
- §1-16 クリップごとの参照音声（長尺A2V。上記4-(3)）
- §3-57 stage-2の窓サイズ最適化（上記1節の窓19）
- §3-58 中間クリップのキーフレーム（上記4-(1)）

着手するときは、**まず窓サイズ（§3-57）の可否を決めるのが順序として良い。** 窓を19へ変えると1節の上限計算がまるごと動くので、§1-14の警告しきい値もそれに追随させる必要があるためである。
