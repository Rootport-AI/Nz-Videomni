# Clip Chain拡張 ワークオーダー — 折りたたみ式UIとクリップ連結上限の引き上げ

- 作成: 2026-07-13（オーナーとの合意事項を整理・次セッション着手前のワークオーダー）
- 正本: 本書。着手後の状態遷移は本書の冒頭に歴史記録ブロックを追加していく運用とする（他の `*_WORKORDER.md` と同じ体裁）。
- 併読: [`NEXT_SESSION_HANDOFF.md`](NEXT_SESSION_HANDOFF.md)（次セッションの課題としての位置付け）／[`PHASE3_CLIP_CONCAT_DESIGN.md`](PHASE3_CLIP_CONCAT_DESIGN.md)（クリップ連結＝Clip Chainの設計正本）／[`RESOLUTION_DURATION_CAPABILITY.md`](RESOLUTION_DURATION_CAPABILITY.md)（481フレーム上限・解像度別の快適上限の算出根拠）／[`chain_math.py`](../chain_math.py)（尺算術の実装）

> **（2026-07-13 実装完了・歴史記録注記）本書の要件はすべて実装完了した（未コミット・レビュー中）。** 着手前調査の結論・実装された確定仕様・オーナー決定事項は以下の「実装完了の要約」ブロックを参照。本文（§1〜§4）はワークオーダー作成時点（実装着手前）の記録としてそのまま残す。

> **（2026-08-31 追記）本文中の「768pでは単発257フレームまでがスピルなしの目安」は、当時の`spill_free_frames`の値である。** 現在の値は`config.yaml`の`limits.spill_free_frames`（説明の正本は[`COMFORT_LIMIT_TABLE.md`](COMFORT_LIMIT_TABLE.md) §付記）を参照のこと。**本文は当時の記録としてそのまま残す。**

## 実装完了の要約（2026-07-13）

### 着手前調査の結論（§4の手順1・手順2の決着）

- **VAEデコード（latent＝ピクセルに戻す前の圧縮データを実際の映像へ戻す処理）は、タイル分割の逐次処理＋ディスクへのストリーミング書き出しで実装されており、VRAM（GPUのメモリ）使用量はチェーンの総尺に依存しないと確定した。** オーナーがログで観察した「デコードが1回だけ」という挙動と、コードコメントの「常にタイル処理」という記述は両立していた（1回のデコード呼び出しの内部がタイル分割されている）。
- **ただし、アップサンプル（全体の高解像度化）の段階は全タイムラインを一括でGPU処理し、その中間latentも処理中は解放されないため、総尺に比例して増えるVRAM消費の項が存在する。** 総尺上限（`MAX_CHAIN_TOTAL_PIXEL_FRAMES`）はこの項を抑える安全上限として機能する。`api/models.py` の当該コメントは実態に合わせて書き直した。
- 手順3の実測（短いクリップ×24本を1回通してVRAM/RAM使用量を確認する作業）は、**実装完了後にオーナーが手動で実施する予定**として残っている。

### 実装された確定仕様の要点

1. **クリップ連結の上限を8→24に引き上げた。** サーバー側のAPIバリデーション（`api/models.py` の `clips` 上限）も24に、チェーン総尺の上限 `MAX_CHAIN_TOTAL_PIXEL_FRAMES` も 24×481＝11544 ピクセルフレームに更新した。
2. **Clip Chainタブは±ボタン式の折りたたみUIになった。** デフォルトはクリップ2まで表示、＋で次の枠を開き、−で最後の枠を閉じる（最低2枠）。−は「枠の非表示化」と「その枠のUseチェックのオフ」をセットで行う非破壊方式（入力済みのテキスト等は消えず、＋で開き直せば残っている）。
3. **Generateタブのキーフレーム欄（5スロット）も同じ±ボタン式にした。** デフォルトは1枚目のみ表示、上限5は変更なし（オーナー確定事項どおり）。
4. **ジョブ監視（ポーリング）のタイムアウト既定値を60分→120分に延長した。** Settingsタブの初期値・ハンドラのフォールバック値とも120分。
5. **「Clip list」見出しの直下に、連結後の推計秒数を常時表示するようにした。** 有効なクリップの合計を `chain_math.py`（尺算術の共通実装）で計算し、総尺上限を超える場合は警告文言に切り替わる。入力変更・プリセット適用・言語切替のいずれにも追随する。

### オーナー決定事項（2026-07-13）

1. **タイムアウトは単純延長のみ**とする（監視の仕組み自体の変更はしない）。
2. **バッチA2V実行中にタイムアウトが発生した場合の孤児ジョブ（orphan＝画面側の監視が切れた後もサーバー側で走り続けるジョブ）の挙動は、仕様として受容する。** バッチ側のタイムアウトはSettings値が配線済みであり、追加対応は不要と確認した。

### 実装ファイルとテスト

- 変更: `api/models.py`（上限定数とコメント）／`gradio_ui/ui.py`（±ボタンUI・推計表示・タイムアウト初期値）／`gradio_ui/handlers.py`（チェーンハンドラの24枠化・クリップ数範囲・タイムアウト）／`gradio_ui/presets.py`（推計計算と±状態遷移の純関数・プリセット24枠化）／`gradio_ui/i18n.py`（EN/JA文言）／`gradio_ui/validation.py`・`gradio_ui/batch.py`（ミラー定数・タイムアウト）
- テスト: 既存テストの24枠追随＋新規（推計計算・±状態遷移・APIの24受理/25拒否境界）。**pytest 598 passed / 1 skipped**（退行ゼロ）。
- 残タスク: 24クリップ通しのVRAM実測（オーナー手動）／git commit（レビュー後）。

### 実機ゲート結果（2026-07-14・オーナー承認済み）

上の「残タスク」にあったVRAM実測をオーナーが実施し、あわせてAPI自動実行での二点測定を行った。結論を先に述べると、**上限24クリップ/11544フレームの実装は今回このままリリースする**。320p帯では安全に実証済みで、768pの長尺だけはスピル（GPU専用メモリ＝VRAMから、共有GPUメモリ＝メインメモリ側へデータが溢れる現象。溢れると転送のぶん大幅に遅くなり、共有メモリも尽きればハードOOM＝メモリ不足でジョブが落ちる）による低速・リスクありという既知の制約として明記する。

#### GUI実機確認

オーナー確認済みでクローズ。±ボタン・カウンター・グレーアウト・推計秒数のいずれも想定どおりに動作した。

#### 機能スパイク（オーナー実施）

512x320・49フレーム×24クリップ・オーバーラップ3の条件で実行。**1160.3秒で完走し、peak_vram（処理中に観測されたVRAM使用量の最大値）＝9525MB、出力785フレーム**。比較として、同じ解像度の単発49フレームジョブは8440MB／129.2秒だった。**24クリップ連結という機能そのものは成立している。**

#### 768pでの二点測定（API自動実行）

目的は、総尺（動画全体の長さ）に比例して増えるVRAM消費の項の「傾き」を実測し、24クリップ相当まで外挿（測定した点から先を直線で延長して推定すること）することにある。測定機体はGPUがRTX 4070 Ti SUPER（専用VRAM 16375MB）、メインメモリ64GB（そこから割り当てられる共有GPUメモリの上限は約32GB、専用＋共有を合わせた総上限は約48GB）。5秒間隔で専用VRAMと共有GPUメモリの両方をサンプリングした。

| | 測定A | 測定B |
|---|---|---|
| 条件 | 1280x768・481f×4・overlap3/0.5 | 同左で481f×8 |
| 総フレーム/latentフレーム | 1873f / 235 | 3729f / 467 |
| 結果 | 完走・1552秒 | 完走・4104秒 |
| サーバー報告peak_vram_mb | 15932 | 29026（物理16GB超） |
| 実測 専用ピーク | 15768MB | 16021MB（物理上限に張り付き） |
| 実測 共有ピーク | 9840MB（約35秒間のみ） | 24067MB |
| 専用+共有の合計需要 | 25608MB | 40084MB |

latent（latentフレーム＝VAEでピクセルに戻す前の圧縮された時間方向の単位。ピクセルフレームより粗い）で見ると、測定Aが235、測定Bが467である。

#### スピルの発生箇所の特定

スピルは処理の全体で起きているのではなく、**stage-1（各クリップの下書き生成）が全クリップぶん完了した直後の「連結したタイムライン全体を一括で高解像度化するアップサンプル処理」の瞬間に集中している**（`engine/pipeline/chain_pipeline.py:728-733` の `upsample_video` 呼び出し）。この一括アップサンプルが、総尺に比例して増える最大のVRAM項である。

一方、その後の**stage-2（全体の仕上げデノイズ＝ノイズ除去による最終的な画質の作り込み）は時間タイル（時間方向に分割した処理単位）ごとに実行され**（1タイル22latent、測定Aは13タイル・測定Bは26タイル）、専用VRAMは15.7GBで頭打ちになりスピルは起きなかった。**タイル化は有効に働いている。** なお、ログに出る「tiled upsample」という表示はこのstage-2の仕上げループを指すラベルであって、アップサンプル本体（一括処理される側）ではない点に注意（紛らわしい命名になっている）。

副次的な観察として、長尺のVAEデコード（圧縮latentを実際のピクセル映像へ戻す処理）の最中も共有GPUメモリを約4.6GB持続的に使い続けており、測定Bのデコードは25分と非線形に（尺の増加以上に）悪化していた。

#### 外挿と判定

latentフレーム 235（測定A）と 467（測定B）の二点から、24クリップ相当の 1395latent へ直線で外挿すると、**サーバー報告ベースで約81GB、専用＋共有の合計需要ベースで約98GB**になる。これは機体の総上限（約48GB）を大幅に超えるため、**768pでの24クリップはハードOOMが確実に発生する＝危険**である。さらに、旧上限だった8クリップ×481fですら768pでは既にスピルしていた（従来も共有メモリに救われて動いていただけ）。一方、320pの24クリップは9.5GBで安全に収まっている。**つまり安全性は「総尺 × 解像度」の積で決まる**。この傾向は、`config` の `spill_free_frames`（768pでは単発257フレームまでがスピルなしの目安）とも整合している。

#### 結論と決定（オーナー承認済み・2026-07-14）

- **上限24クリップ/11544フレームの実装は、今回このままリリースする。** 320p帯では実証済みであり、768pの長尺だけは「現状ではスピルにより低速・リスクあり」という既知の制約として明記する。
- 根本対策として、**次セッションで「アップサンプルの時間チャンク化（時間方向に分割して少しずつ高解像度化することで、一括処理による巨大なVRAMピークを避ける手法）」に取り組む。** 詳細は新設のワークオーダー [`CHUNKED_UPSAMPLE_WORKORDER.md`](CHUNKED_UPSAMPLE_WORKORDER.md) を参照。

---

**本書の位置付け（2026-07-13時点）**: まだ実装は開始していない。オーナーとの合意事項の整理である。次セッション着手前に、下記「未決・着手手順」のVAEデコードのメモリ挙動調査から始めること。

---

## 1. 背景と動機

Clip Chainタブ（クリップ＝連結される動画の1区間を複数つなげて1本の長い動画にする機能）は現在、クリップ入力欄が8枠すべて常時縦に並んでおり、縦に長く見栄えが悪い。実際に使う本数は毎回2〜3枠程度であることが多く、残りの空欄が画面を圧迫している。

Generateタブの Keyframe images（i2v＝画像から動画を生成する機能のキーフレーム指定欄。5スロット）も同じ問題を抱えている。

デフォルトでは少数の枠だけを表示し、＋ボタンを押すごとに次の枠を開いていくUIに変更したい。あわせて、クリップ連結の上限を「ユーザーが上限をほとんど意識しなくて済む数」へ引き上げたい。

## 2. オーナー確定事項（2026-07-13）

1. **クリップ連結の上限は24に引き上げる。** 当初案の120（1クリップ約240秒×120＝8時間）は、チェーンが不可分の単一ジョブであるリスク（後述§4参照）の指摘を受けて撤回した。
2. **＋ボタンと−ボタンの両方を設ける。** ＋で次の枠を開き、−で最後に開いた枠を閉じる（Clip 2までは閉じない＝最低2枠は常に表示する）。デフォルト表示はClip 2まで。
3. **キーフレームは5枚固定を維持する（上限は変えない）。** UIのみ折りたたみ式にする: デフォルトは1枚目のみ表示し、＋で2〜5枚目を開いていく。
4. **VAEデコード（latent＝VAEでピクセルに戻す前の圧縮データ。それを実際のピクセル映像へ戻す処理）のメモリ挙動の調査を、着手前の必須ステップとする。** オーナー自身のログ観察でも「全クリップぶんのlatentを貯めてから最後にまとめてVAEデコードしている」ように見え（実機ログで `chain VAE decode started` がチェーン全体で1回のみ出ることと整合する）、クリップ数を増やすとVRAM（GPUのメモリ）溢れのリスクがあるとの認識で一致している。

## 3. 技術的裏取り済みの事実（2026-07-13、コード確認済み）

- **クリップ数上限はUIの8枠だけでなく、サーバー側にも存在する。** `GenerateChainRequest.clips` は `min_length=1, max_length=8`（`api/models.py:323`）。チェーン総尺の上限は `MAX_CHAIN_TOTAL_PIXEL_FRAMES = 8 * 481 = 3848`（`api/models.py:201`）。同ファイルのコメント（`api/models.py:196-201`）には、この上限は「安全上限（sanity ceiling）」であり、「8クリップ×481フレーム（最大クリップ数×クリップ単体の凍結上限）」という積で決めた数値であって、UIが無制限の尺を要求できないようにするための天井だと明記されている。**これはLTX 2.3本体の仕様値ではなく、本バックエンド独自の上限**である（LTX側にそもそも「チェーン」という概念は無く、LTX由来の凍結上限は1クリップあたり481フレームのみ）。`config.yaml` には総尺の設定項目は無く、コード内定数（`api/models.py`）のみで管理されている。
- **同コメントには「stage-2（全体の高解像度化・仕上げ段階）は常にタイル処理〔always-tiled〕でデコードするため、単一クリップの481フレーム上限には縛られず、タイル化によってどんな長さでもVRAM使用量はほぼ一定に保たれる」という趣旨の記述がある。** これは§4の調査対象そのものに関わる手がかりであり、着手前調査で実装（`engine/pipeline/chain_pipeline.py`等）の実際の挙動と照らし合わせて確認すること。コメントの記述と、オーナーが観察した「全クリップぶんのlatentを貯めてから最後にまとめてデコードしている」ように見えるログ挙動が両立するのか（＝1回でまとめてデコードするが内部はタイル分割されているためVRAMは一定、という可能性）、それとも記述と実態がずれているのかは、まだ確認できていない。
- **GUIは8枠のうちUseチェックがオンの枠だけを抽出して `clips` 配列（単純なJSON配列。配列長＝クリップ数）を組み立てて送信する**（`gradio_ui/handlers.py:804-814`）。クリップ数に依存する特殊な通信仕様は無く、動的UI化（枠数の可変表示）はサーバーAPI無改修で実現できる。
- **動的UIの実装方式は「全枠をあらかじめ生成して `visible=False` で隠しておき、＋ボタンで `gr.update(visible=True)` にする」可視性切り替え方式を推奨する。** 既存の前例として、Clip ChainタブのV2Vパネル（`v2v_group`）が同じ手法で実装されている（`gradio_ui/ui.py:659` で `gr.Group(visible=False)` として定義し、`gradio_ui/ui.py:1552-1558` の `on_chain_mode_change` で `gr.update(visible=is_v2v)` により切り替える）。Gradio 6.19には `gr.render`（要素を動的に再生成する仕組み）も存在するが、再生成のたびにイベント配線（`.click` や `.change` の紐付け）もやり直しになるため、本UIのような固定枠数の開閉には不向きと判断する。
- **−ボタンの実装上の罠**: 送信対象はUseチェックボックスの状態で決まるため（上記のとおり）、枠を非表示にするだけでは、Useチェックがオンのまま画面上は見えなくなったクリップの内容が送信されてしまう。−ボタンは「枠の非表示化」と「そのスロットのUseチェックをオフにする」を必ずセットで行うこと。入力済みのテキストや画像は消さない非破壊方式にする（＋で再度開けば内容がそのまま残っている）。
- **キーフレーム上限5の定義箇所**: `config.yaml` の `limits.max_conditioning_images: 5` と、APIバリデーション側の `api/models.py:146-147`（単発 `GenerateRequest`）・`api/models.py:444-447`（チェーンの各クリップ）。**いずれも `5` のリテラル直書きであり、`config.yaml` の値は実際には参照されていない**（コード上の数値が正）。仮に上限を変更するならAPI側2箇所の修正が必要になるが、今回のスコープでは上限自体は変えない。
- **GUIのジョブ監視（ポーリング）タイムアウトの既定値は3600秒。** 24クリップ（総尺次第では生成に数時間かかりうる）を回す場合、このタイムアウト値が実運用に十分かは要確認事項として残る。

## 4. リスクと着手手順（合意済み）

### リスク

- チェーンは**不可分の単一ジョブ**であり、実行中に安全に中断する手段が無い（Phase 1からの既知の制約）。電源断が起きれば実行中のジョブは全ロストする。
- GUIのジョブ監視タイムアウトの既定値は3600秒（上記§3参照）。24クリップの長尺チェーンでこの値が妥当かは未検証。
- VAEデコード時にメモリ蓄積がチェーンの総尺に比例して増える可能性がある（現行の `MAX_CHAIN_TOTAL_PIXEL_FRAMES = 3848` という上限自体が、実はこのメモリ制約を暗黙に反映した数値である可能性も否定できない）。

### 着手手順

1. **手順1**: エンジン側のデコード処理をコード読解で確定する。具体的には、全クリップぶんのlatentをまとめて一括デコードしているのか、時間方向に分割して逐次デコードしているのか。また、デコード後のピクセルフレームはRAM上に全量蓄積されるのか、それとも都度ディスクへ書き出されるのか。§3で触れた「stage-2は常にタイル処理」というコメントの実態を、この読解で裏取りする。
2. **手順2**: 手順1の結果をもとに総尺上限を決定する。24クリップ×481フレーム＝11544まで引き上げて安全か、それとも24クリップは許すが総尺は控えめに抑えるべきか、を判断する。変更が必要になる箇所は `MAX_CHAIN_TOTAL_PIXEL_FRAMES`（`api/models.py:201`）と `max_length=8`（`api/models.py:323`）の両方、および対応するUI側の枠数上限。
3. **手順3**: 実測が必要な場合、「短いクリップ×24本」を1回通しで実行すれば、最悪ケースのVRAM/RAM使用量を安価に確認できる。
4. **手順4**: UI実装に着手する（可視性切り替え方式による±ボタン、キーフレームの折りたたみ表示）。

### 遠い将来の構想（今回のスコープ外）

120クリップ級の長尺チェーン構想は、2026-07-27の整理で[`PENDING_TASKS.md`](../../Nz-LTX23-frontend-AviUtl2/Docs/PENDING_TASKS.md) §4-10へ移設した。**以後の管理は同書で行う。** 要旨は「やるとすれば固定枠の拡張ではなく、バッチA2V機能（[`BATCH_A2V_WORKORDER.md`](BATCH_A2V_WORKORDER.md)）と同様の『表＋State方式のUI＋区間分割生成して繋ぐ再開可能な仕組み』として別途の設計課題で扱う」。撤回の理由（チェーンが不可分の単一ジョブであるリスク）は上の§2に残す。
