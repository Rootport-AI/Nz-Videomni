# 快適上限テーブル（フレーム数ごとの総ピクセル数の上限・実測較正版）

作成: 2026-08-18 / 改訂: 2026-09-05（**第9節の配信経路を訂正した**——画角拡張〔Outpainting〕のトークン予算は、フロントエンドが自前で持つ定数ではなく、`GET /config`が`limits.comfort_budgets[系統].outpaint_budget`として配信する値になった〔台帳[`PENDING_TASKS.md`](PENDING_TASKS.md) §2-8＝§3-135の移送先。記録は[`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §99〕。**線の値は1つも動いていない**——訂正したのは第9.4節の「サーバー側に対応物は無い」という記述と、第9節本文のミラー先の名指しだけである）／2026-09-05（**画角拡張〔Outpainting〕のトークン予算を、加速設定を全てonにした構成で測り直した**——Edit画面が生成要求に加速設定を載せるようになったためである。動いたのは`ltx25`の行だけで、`ltx`は据え置き。**現在の値は第9節が正本**であり、下の2026-09-04の行に書いてある数値はその時点のものである）／2026-09-04（参照動画つき単発生成の境界を第4.9節へ追加した。結論は「単発の線をそのまま当ててよい」で、**線44,880は動いていない**。あわせて**画角拡張（Outpainting）のトークン予算をエンジン系統別の実測値へ置き換え、第9節を新設した**——`ltx`＝42,240／`ltx25`＝44,880、オーナー裁定）／2026-08-31（エンジン系統〔ベースモデルの世代〕の軸を第1.1節へ追加し、快適の判定規則を第4.2節でv3へ改めた。本体の全面改訂＝フェルミ推定版から実測較正版へ、は2026-08-18。線44,880そのものはこのときから変わっていない）
性格: **本書は快適上限の3つの守備範囲——単発生成の線（第1.1節）・連結生成の予算（第1.1節・第6節）・画角拡張〔Outpainting〕のトークン予算（第9節）——の正本である**（ファイル名は参照互換のため`COMFORT_LIMIT_TABLE.md`のまま据え置く）。出発点は**Singleタブのフレーム数スライダーに引く「快適上限」の線を、プリセット解像度単位ではなく1フレーム単位で動かすための下ごしらえ**だった。前版はこの表を「実測から逆算した式を機械的に適用した推定値」として公開していたが、その後の実測検証で線の値そのものが更新され、かつ線が成立する前提条件（後述の「全on構成」）が判明した。本版は更新後の線で表を再計算したものであり、**表の性格は「フェルミ推定」から「実測較正済みの式による計算値」へ変わった**。

---

## 1. 結論を先に

### 1.1 快適上限の線 — エンジン系統×高速化構成の表

**快適上限の線は、エンジン系統（ベースモデルの世代）と高速化構成の組で決まる。** 下の表を上から順に見て、条件をすべて満たした最初の行の予算を採る。**どの行にも当てはまらないときは線を引かず、レガシー表`spill_free_frames`（解像度ごとの快適上限フレーム数。§付記）へ落ちる**——これは異常ではなく、設計どおりの正常な経路である。

| エンジン系統 | ベースモデル | 条件 | 単発生成の予算 | 連結生成（Chained）の予算 |
| :--- | :--- | :--- | ---: | ---: |
| `ltx` | LTX 2.3 | 下記5項目がすべてon（「全on構成」） | 44,880 | 40,000 |
| `ltx25` | LTX 2.5 | 無条件（どの高速化設定でも同じ） | 44,880 | 44,880 |

「全on構成」の5項目は次のとおりである。

1. sage attention（`attentionBackend: "sage"`）
2. block_swap_prefetch（`blockSwapPrefetch: true`）
3. keep_resident（`keepResident: true`）
4. fused_gguf_dequant_kernel（`fusedGgufDequantKernel: true`）
5. vae_mode = prune_vaed（`vaeMode: "prune_vaed"`）

**LTX 2.3 の既定構成には、意図的に行を置いていない。** 既定構成は素のVAEデコーダ（PrunaVAEDを使わない復元処理）で動くため、復元（デコード）の段で共有GPUメモリへの退避が起き、**快適と非快適の境界がトークン数に対して単調にならない**。境界は復元のかたまり（チャンク）の数がちょうど1段増える点（7→8・4→5・2→3）と一致しており、しかも境界付近の判定は同じ条件で走らせても実行のたびに変わる（非決定的）。**ただし3つの段のうち1080pの4→5（161→169フレーム）だけは、段差の点そのもの（169フレーム）が2回の実行で快適・非快適に割れたため、720p（273→289）・1440p（81→89）のように段差と境界がぴったり重なるとは言い切れない**（第4.8節）。**1本の線では表せないので、この構成では`spill_free_frames`へ落とすのが正しい。** ここへ「条件が空の行」を足してはならない——足せば、退避が起きる領域まで快適と表示してしまう。現象そのものの解明は研究課題として第8節に残した。

**線44,880の由来**: オーナー裁定による確定値（実測で快適と確認できた最大の共通値）で、実測アンカーのひとつ（1920×1088・169フレーム）がちょうどこの値を刻んでおり、かつ快適と判定されている（第4節）。**この裁定そのものの記録は台帳[`PENDING_TASKS_CLOSED.md`](PENDING_TASKS_CLOSED.md) §3-12と[`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §66にある。** LTX 2.5 については 2026-08-31 の再較正で、単発生成・連結生成ともこの同じ線まで快適であることを確かめた（第7節）。**参照動画（IC-LoRAの制御アダプタ）を添えた単発生成にも、この同じ線をそのまま当てる**——2026-09-04 の較正で両系統とも確かめてある（第4.9節。適用は幅・高さが128の倍数の幾何に限る）。

### 1.2 使っている式

生成1本が扱う注意（アテンション）のトークン数は、次の1本の式で決まる。

```
トークン数 N = (幅 ÷ 32) × (高さ ÷ 32) × 潜在フレーム数
潜在フレーム数 = (フレーム数 − 1) ÷ 8 + 1
```

幅と高さの割り算は切り捨てである。映像のVAE（映像を圧縮・伸長する仕組み）が空間方向を32分の1に、時間方向を8分の1に圧縮するため、32×32ピクセルの1区画が1つの潜在位置になり、その潜在位置1つが注意のトークン1つになる。この式自体は全on構成でも既定構成でも変わらない。変わるのは「何トークンまでが快適か」という線の位置だけである。

### 1.3 逆算式（実装で使う向き）

Singleタブのフレーム数スライダーが必要としているのは、表の向きとは逆の「今の解像度なら快適上限は何フレームか」である。式は次の1行で書ける。

```
潜在フレーム数の上限 N_latent_max = floor(44880 ÷ ((幅÷32) × (高さ÷32)))
フレーム数の上限 = 8 × (N_latent_max − 1) + 1     ※8n+1グリッドへ丸め、[9, 481] へ収める
```

**この式は表を持たずに実装できる。** 60行の表をコードへ焼き込む必要はなく、線の値（44,880、全on構成のとき）と現在の幅・高さから毎回計算すればよい。表は人が確かめるための資料であって、実装の入力ではない。

---

## 2. 表の読み方

- **フレーム数**: 生成できるフレーム数は8n+1（9・17・25…481）に限られる。下限9は`webui/src/modes/single/defaultConfig.ts`の`MIN_NUM_FRAMES`、上限481は`config.yaml`の`limits.max_num_frames`である。
- **潜在フレーム数**: `(フレーム数 − 1) ÷ 8 + 1`。
- **空間マス目の上限**: `44,880 ÷ 潜在フレーム数`の切り捨て。32ピクセル四方の区画が何個まで許されるかである。
- **総ピクセル数の上限**: 空間マス目の上限 × 1,024（＝32×32）。これは幅×高さの積の上限であって、幅と高さそれぞれの上限ではない。片方を下げればもう片方はこの表の目安解像度より大きくできる。
- **16:9に近い目安の解像度**: 総ピクセル数の上限を16対9の比で分け、幅・高さともに64の倍数へ切り捨てた組である（生成サイズは必ず64の倍数という契約による）。切り捨てのぶん、実トークン数は44,880をやや下回ることが多い。
- **その解像度の実トークン数／予算に対する割合**: 64の倍数へ切り捨てる都合で90〜100パーセントの範囲でばらつく。
- 幅・高さの本体上限は4,096である（`config.yaml`の`limits.max_width` / `max_height`）。フレーム数が少ない側の数行は、トークンの予算より先にこの4,096が効く。該当行には注記を入れた。

**表の性格について**: 各行は「トークン数 ≤ 44,880」という1本の式から機械的に生成した計算値であり、前版のような「40,000という数値が正しいと仮定した推定」ではない。44,880そのものが実測アンカー（1920×1088・169フレーム）であり、下の第4節のとおり周辺の実測点との整合も確認済みである。ただし60行すべてを個別に実機検証したわけではない点は変わらない——検証されているのは「線の位置」であって「表の全行」ではない。

**行数について**: 9から481まで8刻みで数えると60パターンである（`(481 − 9) ÷ 8 + 1 = 60`）。

---

## 3. 本体の表（線44,880で再計算）

**この表が使えるのは、第1.1節で予算が44,880になる行——LTX 2.3の全on構成と、LTX 2.5の全構成——だけである。** 予算が40,000の行（LTX 2.3の連結生成）や、行に当てはまらない構成には当てはまらない。

| フレーム数 | 潜在フレーム数 | 空間マス目の上限 | 総ピクセル数の上限 | 16:9に近い目安の解像度 | その解像度の実トークン数 | 予算に対する割合 |
| ---: | ---: | ---: | ---: | :--- | ---: | ---: |
| 9 | 2 | 22,440 | 22,978,560 | 4096×4096（幅高とも本体の上限4096で頭打ち） | 32,768 | 73.0% |
| 17 | 3 | 14,960 | 15,319,040 | 4096×3712（幅は本体の上限4096で頭打ち） | 44,544 | 99.3% |
| 25 | 4 | 11,220 | 11,489,280 | 4096×2752（幅は本体の上限4096で頭打ち） | 44,032 | 98.1% |
| 33 | 5 | 8,976 | 9,191,424 | 4032×2240 | 44,100 | 98.3% |
| 41 | 6 | 7,480 | 7,659,520 | 3648×2048 | 43,776 | 97.5% |
| 49 | 7 | 6,411 | 6,564,864 | 3392×1920 | 44,520 | 99.2% |
| 57 | 8 | 5,610 | 5,744,640 | 3136×1792 | 43,904 | 97.8% |
| 65 | 9 | 4,986 | 5,105,664 | 3008×1664 | 43,992 | 98.0% |
| 73 | 10 | 4,488 | 4,595,712 | 2816×1600 | 44,000 | 98.0% |
| 81 | 11 | 4,080 | 4,177,920 | 2688×1536 | 44,352 | 98.8% |
| 89 | 12 | 3,740 | 3,829,760 | 2560×1472 | 44,160 | 98.4% |
| 97 | 13 | 3,452 | 3,534,848 | 2496×1408 | 44,616 | 99.4% |
| 105 | 14 | 3,205 | 3,281,920 | 2368×1344 | 43,512 | 97.0% |
| 113 | 15 | 2,992 | 3,063,808 | 2304×1280 | 43,200 | 96.3% |
| 121 | 16 | 2,805 | 2,872,320 | 2240×1280 | 44,800 | 99.8% |
| 129 | 17 | 2,640 | 2,703,360 | 2176×1216 | 43,928 | 97.9% |
| 137 | 18 | 2,493 | 2,552,832 | 2112×1152 | 42,768 | 95.3% |
| 145 | 19 | 2,362 | 2,418,688 | 2048×1152 | 43,776 | 97.5% |
| 153 | 20 | 2,244 | 2,297,856 | 1984×1152 | 44,640 | 99.5% |
| 161 | 21 | 2,137 | 2,188,288 | 1920×1088 | 42,840 | 95.5% |
| **169** | **22** | **2,040** | **2,088,960** | **1920×1088** | **44,880** | **100.0%** |
| 177 | 23 | 1,951 | 1,997,824 | 1856×1024 | 42,688 | 95.1% |
| 185 | 24 | 1,870 | 1,914,880 | 1792×1024 | 43,008 | 95.8% |
| 193 | 25 | 1,795 | 1,838,080 | 1792×1024 | 44,800 | 99.8% |
| 201 | 26 | 1,726 | 1,767,424 | 1728×960 | 42,120 | 93.9% |
| 209 | 27 | 1,662 | 1,701,888 | 1728×960 | 43,740 | 97.5% |
| 217 | 28 | 1,602 | 1,640,448 | 1664×960 | 43,680 | 97.3% |
| 225 | 29 | 1,547 | 1,584,128 | 1664×896 | 42,224 | 94.1% |
| 233 | 30 | 1,496 | 1,531,904 | 1600×896 | 42,000 | 93.6% |
| 241 | 31 | 1,447 | 1,481,728 | 1600×896 | 43,400 | 96.7% |
| 249 | 32 | 1,402 | 1,435,648 | 1536×896 | 43,008 | 95.8% |
| 257 | 33 | 1,360 | 1,392,640 | 1536×896 | 44,352 | 98.8% |
| 265 | 34 | 1,320 | 1,351,680 | 1536×832 | 42,432 | 94.5% |
| 273 | 35 | 1,282 | 1,312,768 | 1472×832 | 41,860 | 93.3% |
| 281 | 36 | 1,246 | 1,275,904 | 1472×832 | 43,056 | 95.9% |
| 289 | 37 | 1,212 | 1,241,088 | 1472×832 | 44,252 | 98.6% |
| 297 | 38 | 1,181 | 1,209,344 | 1408×832 | 43,472 | 96.9% |
| 305 | 39 | 1,150 | 1,177,600 | 1408×832 | 44,616 | 99.4% |
| 313 | 40 | 1,122 | 1,148,928 | 1408×768 | 42,240 | 94.1% |
| **321** | **41** | **1,094** | **1,120,256** | **1408×768** | **43,296** | **96.5%** |
| 329 | 42 | 1,068 | 1,093,632 | 1344×768 | 42,336 | 94.3% |
| 337 | 43 | 1,043 | 1,068,032 | 1344×768 | 43,344 | 96.6% |
| 345 | 44 | 1,020 | 1,044,480 | 1344×768 | 44,352 | 98.8% |
| 353 | 45 | 997 | 1,020,928 | 1344×704 | 41,580 | 92.6% |
| **361** | **46** | **975** | **998,400** | **1280×768** | **44,160** | **98.4%** |
| **369** | **47** | **954** | **976,896** | **1280×704** | **41,360** | **92.2%** |
| **377** | **48** | **935** | **957,440** | **1280×704** | **42,240** | **94.1%** |
| 385 | 49 | 915 | 936,960 | 1280×704 | 43,120 | 96.1% |
| 393 | 50 | 897 | 918,528 | 1216×704 | 41,800 | 93.1% |
| 401 | 51 | 880 | 901,120 | 1216×704 | 42,636 | 95.0% |
| 409 | 52 | 863 | 883,712 | 1216×704 | 43,472 | 96.9% |
| 417 | 53 | 846 | 866,304 | 1216×704 | 44,308 | 98.7% |
| 425 | 54 | 831 | 850,944 | 1216×640 | 41,040 | 91.4% |
| 433 | 55 | 816 | 835,584 | 1216×640 | 41,800 | 93.1% |
| 441 | 56 | 801 | 820,224 | 1152×704 | 44,352 | 98.8% |
| 449 | 57 | 787 | 805,888 | 1152×640 | 41,040 | 91.4% |
| 457 | 58 | 773 | 791,552 | 1152×640 | 41,760 | 93.0% |
| 465 | 59 | 760 | 778,240 | 1152×640 | 42,480 | 94.7% |
| 473 | 60 | 748 | 765,952 | 1152×640 | 43,200 | 96.3% |
| 481 | 61 | 735 | 752,640 | 1152×640 | 43,920 | 97.9% |

**169フレーム（潜在22・1920×1088）の行がちょうど100.0%を刻んでいる。** これは表の計算上の一致ではなく、線44,880そのものがこの解像度・フレーム数の実測アンカー（M2、後述）から採られているためである。太字にした行（169・321・361・369・377）は、第4節の実測アンカーと直接対応する行である。

---

## 4. 実測アンカー一覧と検証の経緯

### 4.1 実測条件

すべて全on構成（sage attention・block_swap_prefetch・keep_resident・fused_gguf_dequant_kernel・vae_mode=prune_vaed の5項目すべてon）・シード12345・車のプロンプト・24fpsで統一して測定した。

### 4.2 快適の判定規則

#### 判定規則 v3（2026-08-31・現行）

**快適とは、同じ解像度の基準点と比べて、stage-2（仕上げのノイズ除去）の窓でも復元（デコード）の窓でも、共有GPUメモリの中央値が持続的に +300MB 以上は上がらないことである。** どちらかの窓でこれを超えたら非快適とする。速度低下（トークン当たりの所要時間）とPCIe転送量は、何が起きているかを説明する証拠として「有害／オーガニック」の分類には記録するが、**線の決定には使わない**。

**速度とPCIeを線から外した理由**: 同じだけ共有メモリへ退避していても、その先で実際にスラッシング（本体メモリとGPUの間でのデータの奪い合い）が起きるかどうかは確率的で、走らせるたびには再現しない。2026-08-31 の較正では、復元窓の共有メモリが同程度に高止まりした3点のうち2点だけがPCIe受信の張り付きを示し、残る1点は無症状のままだった。**線は、再現する指標だけで引く。** 詳細は一次記録 `outputs/comfort-calib-2026-08-31/RESULTS.md` 第2節（複写: [`Docs/Outputs-archive/comfort-calib-2026-08-31/RESULTS.md`](Outputs-archive/comfort-calib-2026-08-31/RESULTS.md)）。

#### 判定規則 v2（2026-08-18 時点。第4.3節のアンカーはこの規則で判定した）

「有害」と判定する基準は次の3軸すべてが同時に成立することである。1軸だけでは有害と判定しない。

1. **共有GPUメモリの持続的プラトー**（+0.8〜3GBの範囲で高止まりする）
2. **PCIe持続転送**（`nvidia-smi dmon`のサンプルの過半数が5MB/sを超え、帯域上限27,000MB/s付近まで飽和する挙動が見える）
3. **トークン当たりの所要時間が+20%を超える**

この3軸を分ける理由は、共有メモリへの確保だけなら速度低下を伴わない「オーガニックな溢れ」（後述4.5）と区別する必要があるためである。

#### v2 の出力と v3 の再判定を混同しないこと

**2026-08-31 の較正台が書き出した `manifest.jsonl` と `runs/<ラベル>/run.json` のうち、`verdict` は v1、`verdict_v2` は v2 の出力である。どちらも v3 ではない。** v3 で引き直した判定は `outputs/comfort-calib-2026-08-31/RESULTS.md` の結果表の「v3快適」列が正本で、**両者が食い違う点が実際にある**（同 第3節・第4節）。数値を引くときは必ずどちらの規則の値かを確かめること。

### 4.3 アンカー一覧

| ラベル | 解像度 | フレーム数 | トークン数 | 判定 | 備考 |
| :--- | :--- | ---: | ---: | :--- | :--- |
| A1' | 1280×768 | 321 | 39,360 | 快適 | |
| A2' | 1920×1088 | 153 | 40,800 | 快適 | |
| M1 | 1280×768 | 369 | 45,120 | 快適 | |
| **M2** | **1920×1088** | **169** | **44,880** | **快適** | **線そのもの** |
| AR1 | 2560×1472（横長） | 89 | 44,160 | 快適 | |
| AR4 | 768×1280（縦長） | 361 | 44,160 | 快適 | AR1とトークン数一致・判定も一致 |
| M5 | 1280×768 | 377 | 46,080 | 境界域 | s/tok +16% |
| AR3 | 768×1280（縦長） | 369 | 45,120 | 境界域 | s/tok +19.96%・共有メモリ+330〜390MB |
| U1 | 1280×768 | 401 | 48,960 | 有害 | |
| U3 | 1920×1088 | 185 | 48,960 | 有害 | |
| M3 | 1280×768 | 385 | 47,040 | 有害 | |
| M4 | 1920×1088 | 177 | 46,920 | 有害 | |
| AR2 | 2560×1472（横長） | 97 | 47,840 | 有害 | |

**AR3とM1を並べると、線の性格がよく分かる。** どちらもトークン数はほぼ同じ帯（AR3=45,120、M1=45,120）だが、横長のM1は快適、縦長のAR3は境界域である。これが第5節で述べる「縦長の注意」の実測根拠である。

### 4.4 検証の経緯（段階1〜4の要約）

各段階のラベルの並びから読み取れる設計意図は次のとおりである。

- **段階1（初期アンカー確認・A1'/A2'）**: 既定構成時代の実測から引かれていた40,000前後の帯（39,360・40,800）が、全on構成でも変わらず快適であることをまず確かめた段階。ここで「全on構成でも既存の帯の下限は崩れない」ことが分かった。
- **段階2（本探索・境界の追い込み・M1〜M5）**: 1280×768と1920×1088の2解像度でフレーム数を刻みながら、快適→境界域→有害の遷移を追い込んだ段階。M2（1920×1088・169f・44,880）が快適の最上点、M5（1280×768・377f・46,080）が境界域、M3・M4が明確な有害という並びから、快適の上限が44,880付近にあることが確定した。
- **段階3（縦横比・解像度依存性の検証・AR1〜AR4）**: 線がトークン数だけで説明できるか、それとも解像度の形状（横長か縦長か、絶対解像度の高低）に依存するかを確かめた段階。AR1（横長）とAR4（縦長）が同一トークン数・同一判定（快適）になったことで「線の内側では縦横の別なく快適」が支持された一方、AR2（横長・有害）とAR3（縦長・境界域）の比較から「境界近傍では縦長のほうが余裕が薄い」という非対称性も同時に見つかった。
- **段階4（有害判定の確定・U1/U3）**: 3軸の有害判定基準（共有メモリのプラトー・PCIe持続転送・所要時間+20%超）を、複数解像度（1280×768と1920×1088）で明確に有害な条件に対して当てはめ、判定基準そのものが解像度をまたいで一貫することを確認した段階。

### 4.5 オーガニックな溢れ（有害とは別物）

速度低下もPCIe転送も伴わない、共有メモリへの容量予約だけの現象を「オーガニックな溢れ」と呼ぶ。既定構成の720p・321フレームでは約1GBのオーガニックな溢れが観測されている。**全on構成では、この容量予約はPrunaVAED（`vae_mode=prune_vaed`）が確保済みVRAMに残す余裕（reserved−2.6GB）によって吸収され、消滅する。** つまり全on構成のほうが既定構成よりオーガニックな溢れに強いという副次効果があり、これも快適上限が既定構成の40,000から全on構成の44,880へ引き上がった一因である。

### 4.6 既定構成では説明できない逆転が全on構成では起きない

既定構成（全onでない構成）では、トークン数だけで順序が説明できない逆転（39,360トークンで溢れ、40,800トークンで快適）が過去に観測されていた。全on構成のアンカー（4.3節）ではこの種の逆転は再現しておらず、快適・境界域・有害の並びはトークン数の順序どおりに素直に並んでいる。**線44,880が「全on構成限定」で成立するのは、この逆転が起きない条件だからでもある。**

### 4.7 段階1（既定構成時代）のデータ — 歴史的経緯として

全on構成の検証に先立つ既定構成時代の測定（ラベルB・B'・S1・S2・S3）では、共有GPUメモリの使用量がフレーム数・解像度の増加につれて天井へ緩やかに近づいていく勾配が観測されていた。この発見が「共有メモリへのはみ出しは急激な崖ではなく連続的な現象である」という前提を作り、後の40,000という運用上の線の考え方につながっている。

これと並行して、`keep_resident`によるRAMコミット圧（システムメモリの占有率）も全計測を通じて確認されているが、いずれの計測でも95%に達しておらず、ページアウトによる速度低下は顕在化していない。この点は全on構成の検証でも変わっていない。

### 4.8 2026-08-31 の再較正で確かめた境界（判定規則 v3）

**一次記録は `outputs/comfort-calib-2026-08-31/RESULTS.md` 第4節・第5節である**（全計測点の表・計器・生データの所在はそちらが正本。ここには境界だけを写す。複写: [`Docs/Outputs-archive/comfort-calib-2026-08-31/`](Outputs-archive/comfort-calib-2026-08-31/)）。第4.3節までのアンカーが全on構成のものであるのに対し、こちらは**既定構成のLTX 2.3**と**LTX 2.5**を測ったものである。

**測定条件は §付記 が書いている 2026-08-18 の条件とは別である**——LTX 2.3 のバッチはシード12345・短い車のプロンプト、LTX 2.5 の単発生成はシード12345・長い車のプロンプト、LTX 2.5 の連結生成はシード1234・同じ長いプロンプトで測った（系統間でシードとプロンプトが揃っていない。詳細は `outputs/comfort-calib-2026-08-31/RESULTS.md` 第1節・第2節）。

**LTX 2.3 既定構成（単発生成）**

| 解像度 | 基準点 | 最後に快適だった点 | 最初に非快適だった点 |
| :--- | :--- | :--- | :--- |
| 1280×768 | 257フレーム | 273フレーム（2回確認） | 289フレーム |
| 1920×1088 | 121フレーム | 161フレーム（2回確認） | 177フレーム |
| 2560×1472 | 57フレーム | 81フレーム（2回確認） | 89フレーム |

1920×1088 の 169フレーム（44,880トークン）は初回が快適・再走が非快適で再現しなかったため、境界には含めていない。この3つの値が §付記 のレガシー表の再測定値である。

**LTX 2.5 既定構成（単発生成）**

| 解像度 | 基準点 | 最後に快適だった点 | 最初に非快適だった点 |
| :--- | :--- | :--- | :--- |
| 1280×768 | 169フレーム | 385フレーム（47,040トークン） | 401フレーム（48,960トークン） |
| 1920×1088 | 89フレーム | 177フレーム（46,920トークン。測定上限） | 未測定 |

**LTX 2.5 連結生成（stage-2の窓22）**

| 解像度 | 基準点 | 最後に快適だった点 | 最初に非快適だった点 |
| :--- | :--- | :--- | :--- |
| 各解像度共通 | 1280×768 のクリップ〔25＋161フレーム〕 | 1984×1088（46,376トークン。測定上限） | 未到達 |

**線を 44,880 に置いた理由——「まだ快適」の内側に、退避の兆しが2点ある。** どちらも v3 の +300MB には届いていないので形式上は快適だが、上限を測定値いっぱいまで攻めない根拠になっている。

- **LTX 2.5・1280×768・385フレーム（47,040トークン）**: stage-2 窓の共有メモリは基準比 +121.0MB で閾値内だが、トークン当たりの所要時間が基準の1.426倍まで伸び、速度の曲線が折れ始めている。
- **LTX 2.5・連結生成・1984×1088（46,376トークン）**: stage-2 窓の共有メモリは基準比 +192.6MB で閾値内だが、stage-2 窓のPCIe受信の90%点が 14,664.8MB/s（基準点は 11.2MB/s）まで跳ね上がっている。

したがって 44,880 は「ここを1トークンでも超えたら不快になる崖」ではなく、**兆しが出る手前で止めた安全側の線**である。

### 4.9 参照動画つき単発生成の境界（2026-09-04 実測）

**結論は「単発の線をそのまま当ててよい」である。** 参照動画（IC-LoRAの制御アダプタ）を添えた単発生成にも、参照を添えないときと同じ 44,880 トークンの線を引く——**LTX 2.3・LTX 2.5 のどちらでも別の線は要らない。** **一次記録は `outputs/comfort-calib-2026-09-04/RESULTS_comfort-calib-2026-09-04.md`** で、全31計測点の表・参照の符号化が確保するGPUメモリの法則・再走との対比はそちらが正本である（ここには写さない。複写: [`Docs/Outputs-archive/comfort-calib-2026-09-04/`](Outputs-archive/comfort-calib-2026-09-04/)）。較正そのものの記録は[`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §94。

**根拠は3つある。**

- **LTX 2.3（全on構成）の境界は [44,160, 47,040) トークンの区間にあり、線 44,880 はその内側である。** 下端 44,160 は 1280×768・361フレームと 1920×1024・177フレームという別々の2つの幾何で一致し、上端 47,040 は再走でも非快適の側に落ちた（判定は第4.2節のv3——同じ幾何の基準点と比べた共有GPUメモリの中央値）。
- **LTX 2.5（サーバー既定）は掃引の上端 47,040 トークンまで一度も反転しなかった。** 線 44,880 はこの系統では保守的に安全な側にある。**ただし「境界が無い」という意味ではない**——上端の点で専有GPUメモリのピークが物理の97パーセントに達しており、掃引を上へ伸ばせば反転する見込みが高い（一次読み）。
- **参照を添えたことによる上乗せは、v3の2つの窓をほとんど動かさない。** 同一の幾何・同一のフレーム数で参照あり／なしを対にして直接引き算すると、差は最大でも +80.3MB（LTX 2.3・1280×768・361フレーム・stage-2の窓）で、判定の閾値 +300MB には遠く及ばない。

**この線を当ててよい範囲（適用条件）**

1. **幅・高さがどちらも128の倍数であること。** 参照動画つきの生成は`api/generate.py`がこれを必須にしており（制御アダプタの倍率2では参照が出力の半分の解像度で扱われるため）、64の倍数でしかない幾何はそもそも422で断られる。上の実測もすべて128倍数の幾何である。
2. **測ったのは倍率2の制御アダプタ（`canny-control`。タイル化しない経路）である。** 倍率1のアダプタ（deblur系や、画角拡張が使う`in-outpainting`）は参照の符号化の経路そのものが違い、確保するGPUメモリが参照トークン数に比例しない——**この線の根拠には含まれていない**（振る舞いの違いは `RESULTS_comfort-calib-2026-09-04.md` 第4節）。
3. **画角拡張（Outpainting）はこの線の対象外である。** 別のワークロードであり、予算も別軸で扱う——**第9節（とくに第9.4節）を見よ。**

**判定の物差しについて申し送りが1つある。** v3に参照の符号化の窓を足したv3′でも併せて判定したが、**LTX 2.3 の参照つき単発では符号化の窓が0.41〜3.41秒しかなく、1秒間隔の採取では標本数が足りずに全点で判定不能になった**（LTX 2.5 の同じ窓はさらに短い）。**実際に境界を捉えたのはv3のstage-2の窓のほうである。** 参照の符号化が確保するGPUメモリそのものは、ワーカーの記録が出す区間ピーク（予約）で読める。

**前提条件**: 上の実測は**sysmem fallback（GPUメモリが足りないときにシステムメモリへ自動で退避するNVIDIAの機能）が有効な機体**でのものである。「Prefer No Sysmem Fallback」に設定した機体では退避ではなくメモリ不足で落ちるため、線の意味そのものが変わる（同じ前提は長尺IC-LoRAの較正にも付いている——[`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §57.6のG4、[`PENDING_TASKS_CLOSED.md`](PENDING_TASKS_CLOSED.md) §3-77）。

---

## 5. 縦長の注意

**線の内側（44,880トークン未満で十分な余裕がある領域）では、縦横の別なく快適である。** AR1（横長・2560×1472・89f）とAR4（縦長・768×1280・361f）が同一トークン数（44,160）・同一判定（快適）になったことがその根拠である。

**一方、境界近傍（線のすぐ外側、44,880〜47,000トークン程度の帯）では、縦長のほうが横長より余裕が薄い。** 同じトークン数帯（45,120前後）で比べると、横長のM1は快適、縦長のAR3は境界域（s/tok +19.96%・共有メモリ+330〜390MB）だった。実装上のマーカーは横縦比を区別しない1本の線（44,880）を引く設計になるが、**縦長の解像度をこの線ぎりぎりまで攻めるユーザーには、横長より早めに境界域へ入る可能性がある**ことを踏まえておく必要がある。線そのものを縦横比別に分ける必要はない（線の内側では問題が出ていない）が、境界域の警告文言や余裕の持たせ方を検討する際にはこの非対称性を考慮すべきである。

---

## 6. 実装への引き渡し事項

1. **マーカーの実装は逆算式1行で足りる。** 第1.3節の式（`N_latent_max = floor(44880 ÷ ((幅÷32)×(高さ÷32)))`、フレーム数へ変換して8n+1グリッドへ丸め）をそのままコードへ落とす。60行の表はここでは不要で、あくまで人が検算するための資料である。
2. **行の選択は「実効の高速化設定をサーバー語彙へ写してから照合する」。** フロントエンド`webui/src/shell/accelerationSettings.ts`の`effectiveAccelerationFields()`が、`effectiveAcceleration()`（keep_residentをblock_swap_prefetchの実効状態で畳み込んだ後の値）を通した設定を、リクエストのフィールド名と同じ語彙（`attention_backend`・`block_swap_prefetch`・`keep_resident`・`fused_gguf_dequant_kernel`・`vae_mode`）へ写す。`webui/src/shell/comfortTable.ts`の`resolveComfortRow()`が、その写した値で第1.1節の表を上から順に照合し、全鍵が一致した最初の行を返す。**生の（畳み込み前の）設定値で照合してはならない**——prefetch不可のときに自動offされたkeep_residentを見誤る。**5条件を直接数え上げる判定関数は置かない**（条件はコードではなく表の側にある）。
3. **一致する行が無いときはレガシー表`spill_free_frames`へ落ちる。** LTX 2.3 で5項目のどれか1つでもoffのとき（＝既定構成を含む）がこれにあたる。`config.yaml`の`spill_free_frames`（解像度ごとの実測快適上限フレーム数の表、`webui/src/modes/single/spillUtils.ts`の`resolveSpillFreeFrames`が読む）を従来どおり使う。**一致行が無いのは異常ではなく設計どおりの正常系である**（第1.1節）。
4. **予算はエンジン系統ごとの表として配信する（2026-08-31〜）。** バックエンドは`GET /config`の`limits.comfort_budgets`で、第1.1節の表そのもの（エンジン系統ごとの係数と行の並び）を配信する。行の`requires`はリクエストのフィールド名と同じ語彙で書かれており、フロントエンドは上から順に全鍵一致した最初の行を採る。**将来モデルや高速化トグルが増えても、行を1本足すだけで済む形にしてある。** 旧来の2鍵（`chain_comfort_token_budget`＝40,000／`single_comfort_token_budget`＝44,880）は**表を持たない古いサーバー向けの互換値として残置**しており、フロントエンドは表が配信されていないときだけこの2鍵へ落ちる。

**決着（2026-08-18）＝現在は上記4の表方式へ移行済み**: 当時は別鍵の新設で決着した。`config.py`の`LimitsConfig`へ`single_comfort_token_budget: int = 44880`を独立フィールドとして追加し、`chain_comfort_token_budget`（40,000）とは値も鍵も完全に分離している。`GET /config`の`limits`ブロックへ`model_dump()`経由でそのまま載るため、配信経路の追加配線は不要だった。フロントエンドは`webui/src/modes/single/spillUtils.ts`の`resolveSingleComfortBudget`を通してのみこの値を読み、欠落・不正値のときは同ファイルのミラー定数`SINGLE_COMFORT_TOKEN_BUDGET = 44880`へ落ちる。実装・機械検証の正本はバックエンド`Docs/VERIFICATION_LOG.md`の該当節。**2026-08-31 に配信形が`comfort_budgets`（上記4）へ移り、この2鍵は互換値として残置する扱いになった**——較正と再判定の記録は同 §84。

**1280×768の式値361とM1（369f）の関係**: 逆算式に幅1280・高さ768をそのまま代入すると、`cells = floor(1280/32)*floor(768/32) = 40*24 = 960`、`N_latent_max = floor(44880/960) = 46`、`frames = 8*(46-1)+1 = 361`となる（第3節の表で1280×768が目安解像度として対応する行と一致）。一方、第4節の実測アンカーM1は**同じ1280×768で369フレーム**（潜在47・総トークン`960*47=45,120`）を快適と判定している——**式の出力（361）より1段外側（369）まで実測では快適だった**、という関係にある。これは式が誤っているのではなく、式が**切り捨て（floor）による安全側の丸め**を含むために起きる差である。式は「44,880 ÷ 空間マス目」を潜在フレーム数へ切り捨ててから8n+1グリッドへ変換するため、実際のトークン数が予算をわずかに下回る解像度・フレーム数の組み合わせでは、線ぎりぎりの1段が式の出力から漏れる。M1の実トークン数45,120は44,880をわずかに超えているにもかかわらず実測では快適と判定されており、この点は44,880という線そのものに数百トークン程度の実測誤差・個体差が伴うことを示している——線を「ここを1トークンでも超えたら不快になる崖」ではなく「この付近から先は注意が要る」という目安として扱うべき根拠でもある（第5節「縦長の注意」の非対称性とあわせて、線の位置は常に安全側に引いてある）。マーカーの実装は式の出力（361）をそのまま使い、369まで攻める調整はしない。

## 7. LTX 2.5（エンジン系統 `ltx25`）

**LTX 2.5 は、高速化トグルの状態にかかわらず、単発生成・連結生成とも 44,880 トークンの線を1本引けばよい。** 2026-08-31 の再較正で、既定構成でも4項目on（この系統で選べる最速の組み合わせ）でもVRAMの挙動が変わらないことを確認したため、**条件（`requires`）が空の行を1本だけ持つ**（第1.1節の表の2行目）。LTX 2.3 のように構成で場合分けする必要はない。

- **なぜ既定構成でも線が引けるのか**: 復元（デコード）の段で退避が起きないため、LTX 2.3 の既定構成で問題になったトークン数に対する非単調性（第1.1節）が現れない。実測の境界は第4.8節。
- **連結生成の予算も 44,880 である。** 従来この系統の画面に出ていた線は 40,000 だったが、これは LTX 2.3 の実測値をそのまま持ち込んでいたもので、2.5 では測り直していなかった。2026-08-31 の較正で 1,920×1,088（44,880トークン）まで共有メモリ・PCIeとも基準点並みに平坦であることを確認し、単発と同じ値へ揃えた。**これが今回変わった唯一の連結生成の予算である**（LTX 2.3 の 40,000 は据え置き）。
- **PrunaVAED（`vae_mode`）は LTX 2.5 では 422 で断られたままである。** 枝刈り済みVAEデコーダのLTX 2.5版が上流に無いためで、状況の正本は[`PENDING_TASKS_CLOSED.md`](PENDING_TASKS_CLOSED.md) §3-102 である（2026-09-01にクローズ済み。**枝刈り済み重みが上流に出るまで対応しようがない**という決着で、422で断る挙動そのものは変わっていない）。**ただし 2.5 の行は無条件なので、`vae_mode`が何であってもマーカーは正しく引かれる**——ブラウザに`prune_vaed`が残っていると生成そのものが422になる問題（2026-09-01に解消済み。[`PENDING_TASKS_CLOSED.md`](PENDING_TASKS_CLOSED.md) §3-139）とは別の話である。
- **単発生成のa2v（音声から動画を生成する機能）にも同じ線を使ってよい。** 同一の幾何で比べたとき、a2vとt2v（テキストから動画を生成する機能）はVRAMに効く指標（ピーク使用量・予約ピーク・両窓の共有メモリ中央値・トークン当たりの所要時間）が±4%以内で一致した（一次記録 `outputs/comfort-calib-2026-08-31/RESULTS.md` 第3節「バッチE4」・第5節）。この確認は LTX 2.3 既定構成で行ったが、線の適用先はエンジン系統を問わない。

## 8. 研究メモ — LTX 2.3 既定構成の境界がまだモデル化できていない

**第1.1節で「線を持たない」と決めた LTX 2.3 既定構成について、境界がなぜそこに来るのかは説明できていない。** 分かっているのは次の2点だけである。

- 快適／非快適の境界が、復元のかたまり（チャンク）の数が1段増える点（273→289フレームで7→8、161→169フレームで4→5、81→89フレームで2→3）とちょうど一致している（第4.8節）。**これは観測であって、機序の説明ではない。** **3つのうち1080pの4→5だけは根拠が弱い**——段差の点そのもの（169フレーム）が2回の実行で快適・非快適に割れたため（次の項目）、720p（273→289）・1440p（81→89）のように段差と境界がぴったり重なるとは言い切れない。
- 境界のすぐ内側は非決定的である。1920×1088・169フレームは、同じ条件の2回の実行で快適・非快適が割れた（第4.8節、および[`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §84）。

**モデル化の候補は3つある**（どれも未検証。着手するなら台帳[`PENDING_TASKS.md`](PENDING_TASKS.md)の該当項目を先に読むこと）。

1. **かたまりの数そのものを式に入れる**（トークン数ではなく `(フレーム数 − 1 + 39) ÷ 40` の切り上げで線を引く）。
2. **復元済みフレームの保持量を数える**（かたまりを書き出すまでに何フレーム分をメモリ上に抱えるか）。
3. **メモリ確保器（アロケータ）の環境変数を変える**（断片化の状態が実行ごとに変わることが非決定性の原因なら、確保器の設定で境界が動きうる）。

## 9. 画角拡張（Outpainting）のトークン予算（2026-09-05 全on実測・エンジン系統別）

**画角拡張の警告に使うトークン予算は、画角拡張自身の実測で引いた、エンジン系統ごとの線である。** 2026-09-04 に「連結生成の予算からの流用」をやめて系統別の実測値へ置き換え（オーナー裁定）、**2026-09-05 に、加速設定を全てonにした構成で測り直した**——Edit画面が生成要求に加速設定を載せるようになったためである（第9.2節）。

| エンジン系統 | ベースモデル | 画角拡張のトークン予算 |
| :--- | :--- | ---: |
| `ltx` | LTX 2.3 | 42,240 |
| `ltx25` | LTX 2.5 | 46,080 |

**この2つの数値の正本は本節である。** 製品が実際に配る値は`config.py`の`_default_comfort_budgets()`にあり、これが本節のミラーになる側である——値を動かすときは本節を先に直す。**フロントエンドはこの表を自前で持たず、サーバーから受け取った値を使う**（配信の仕組みは第9.4節）。同じ値をもう一度写しているのは、サーバーに繋がっていないときのために置いてあるフロントエンドの2ファイル（`webui/src/modes/single/defaultConfig.ts`と`webui/src/bridge/mockBridge.ts`）だけである。トークン数の式そのものは第1.2節と同じで、変わるのは線の位置だけである。**なお、エンジン系統が分からないときにフロントエンドが使っていた据え置き値は、2026-09-05 に概念ごと廃止した**——線が無ければ警告を出さない、というのが唯一の規則である（第9.4節）。**2026-09-05 の測り直しで動いたのは`ltx25`の行だけで、`ltx`の行は1バイトも変えていない。**

**一次記録は `outputs/comfort-calib-2026-09-05/RESULTS_comfort-calib-2026-09-05.md`**（全20計測点の表・境界の決定・対照実験はそちらが正本。ここには写さない。複写: [`Docs/Outputs-archive/comfort-calib-2026-09-05/`](Outputs-archive/comfort-calib-2026-09-05/)）。較正そのものの記録は[`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §98（目視結果とオーナー裁定は§98.11）、台帳の記録は[`PENDING_TASKS_CLOSED.md`](PENDING_TASKS_CLOSED.md) §3-143である。**この線はフロントエンドへの反映とオーナーの画面目視まで済んで完結している**（目視合格 2026-09-05）。

### 9.1 採用の根拠（2026-09-05 の再較正）

採る規則は前回と同じである——**反転を観測できたら、境界区間の下端（＝快適側で実測できた最大の値）を線にする**（較正側の呼び名は規則 **R-A**）。**今回は両系統ともこの規則が当たった。**

- **LTX 2.3 ＝ 42,240（据え置き）。** 境界区間は [42,240, 44,160) で、**両端とも2回ずつ走らせ、2回とも同じ側に落ちた。** これは前回の区間 [42,240, 46,080) の部分集合であり、**不確かさが狭まっただけで答えは動いていない**（前回は 44,160 を測っていないので、矛盾ではない）。
- **LTX 2.5 ＝ 46,080。** 境界区間は [46,080, 48,000) で、こちらも**両端2回ずつで判定は割れなかった。** 前回は掃引の上端まで一度も反転せず、境界そのものを掴めていなかったため保守的に 44,880 を据え置いていた。**今回はその1段上で反転を捉えたので、掴めた境界の下端をそのまま採れる。**
- **この境界は画角拡張に固有である。** LTX 2.3 が反転した点と**同じキャンバス・同じコマ数で、画角拡張だけを外した対照**を1本走らせたところ、stage-2 の窓の共有GPUメモリはその対照自身の基準点と変わらなかった（**−17.2MB**。同じ点に画角拡張を付けると **+769.9MB／再走 +847.4MB**）。**したがってこの反転は、キャンバスの大きさの副作用でも、高解像度側の復号の天井（台帳[`PENDING_TASKS.md`](PENDING_TASKS.md) §4-34）でもない。** 画角拡張の予算を単発生成の予算と混ぜてはならないという第9.4節の原則を、実測で裏づけた形である。
- **判定はトークン数に対して単調である。** 両系統とも、反転より下の点はすべて快適側、上の点はすべて退避側に落ちた（LTX 2.5 では退避の深さも基準比 442 → 889 → 1,586MB と単調に悪化する）。**復元のかたまり（チャンク）の段差で説明する仮説は 2026-09-04 の較正で棄却されており**（[`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §95.4）、今回の実測もそれを覆さない。第1.1節が LTX 2.3 の既定構成の単発生成について書いている非単調性は、このワークロードでは現れていない。

### 9.2 前提（変わったら測り直す）

**上の実測は、Settingsの加速設定を全てonにした構成で測ったものである**（「全on」の中身は系統で違う。定義の正本は本書末尾の付記の1項目目）。

**Edit画面（画角拡張・撮り直し）は、2026-09-05 から生成要求に加速設定を載せる。** それまではこの画面から出たジョブだけが、利用者がSettingsで何を選んでいても常にサーバー既定の構成で走っていた（改修と再較正・画面目視の記録は台帳[`PENDING_TASKS_CLOSED.md`](PENDING_TASKS_CLOSED.md) §3-143）。**いまは利用者が選んだ構成がそのままこの画面のジョブに効くので、線も全on構成で測り直した。**

**したがってこの線は「全on構成の実測値」であって、構成が1つでも違えば線の値は保証されない。** 第1.1節が LTX 2.3 の線を「全on構成」とそれ以外で分けているとおり、この系統の線は構成で動く。

**そのうえで、本節の線には「構成が合わないときに別の線へ落ちる」仕組みが無い。** 単発生成・連結生成のマーカーは、実効の加速設定が第1.1節の表の行と一致しなければレガシー表`spill_free_frames`へ落ちる（第6節の2〜3）。**画角拡張の線はエンジン系統だけで決まる固定線なので、加速を切っている利用者にも全on構成の線がそのまま当たる——つまり楽観側に出る。**

**この帰結は、2026-09-05のオーナー裁定で「このままでよい」と決まった。** 両系統とも固定線のままとし、構成に連動して線を切り替える仕組みは入れない——**実害が小さいため**である（LTX 2.3 は線がそもそも動いておらず、LTX 2.5 の新しい線の位置は、サーバー既定の構成で行った2026-09-04の掃引でも快適だった）。

**ただし、この裁定を一般の設計思想として持ち出してはならない。** 裁定は**エンジン系統ごとの個別判断**であって、「全on構成で測った線は、構成を問わず当ててよい」という原則を立てたものではない。**将来ベースモデルが増えたときは、その系統についてあらためて裁定を仰ぐこと**——ここに前例があるからという理由で引き写さない。裁定の記録は[`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §98.11、台帳の記録は[`PENDING_TASKS_CLOSED.md`](PENDING_TASKS_CLOSED.md) §3-143 である。

### 9.3 この境界読みに付く留保

1. **測ったキャンバスは1種類だけである**（1920×1024、余白は左右320・上下128、内側は素材と同じ1280×768）。**2026-09-04・2026-09-05 のどちらの較正も同じ1種類**で、余白の大きさや縦横比を変えた対照は走らせていない——**境界がトークン数だけで決まるのか、余白の形にも依存するのかは分かっていない。**
2. **境界区間の幅は、このキャンバスの最小刻み（8コマ＝1,920トークン）まで詰まった。** 両系統とも、快適側の上端と退避側の下端が掃引の隣り合う点である。**区間の内側のどこで反転するかは相変わらず測っていない**が、フレーム数は8の倍数＋1しか選べないので、この幾何ではこれ以上は詰められない。
3. **LTX 2.5 の「境界を掴めていない」という留保（2026-09-04）は解消した。** 2026-09-05 にその1段上で反転を捉えている。**ただし第9.1節の「拡張なしの対照」は LTX 2.3 で1本走らせただけで、LTX 2.5 では走らせていない**——2.5 の反転も画角拡張に固有だろうという読みは、系統をまたいだ外挿である。
4. **sysmem fallback（GPUメモリが足りないときにシステムメモリへ自動で退避するNVIDIAの機能）が有効な機体での実測である。** 「Prefer No Sysmem Fallback」に設定した機体では退避ではなくメモリ不足で落ちるため、線の意味そのものが変わる（第4.9節の前提条件と同じ）。

### 9.4 この予算の性格 — 警告専用・別軸

- **超えても生成は止めない。** これは警告だけの線であり、生成を拒む理由の一覧にはトークン予算の項目を置いていない（`webui/src/modes/edit/editReasonMessages.ts`）。**実測値へ置き換えても、この性格は変えていない。**
- **サーバーが配信する（2026-09-05 訂正）。** 第1.1節の表を配信する`comfort_budgets`（`GET /config`の`limits`）の、**エンジン系統プロファイルの直下**に`outpaint_budget`として載っている。**`rows`とは別のフィールドである**——`rows`は加速構成（`requires`）で選ぶ行の列だが、画角拡張の線は構成に依存しない固定線なので、行へ相乗りさせない（裁定J1。第9.2節）。**較正と裁定の済んでいない系統は`None`＝線なしで、クライアントは警告を出さない**——仮の数字を当てて「分かったふり」の表示をしないためである。**`config.yaml`で`limits.comfort_budgets`を上書きするとテーブルは丸ごと置き換わるので、`rows`だけ書いて`outpaint_budget`を書き忘れると、その系統は線なしになる**（フィールド単位の引き継ぎは無い）。それまではフロントエンドが自前の定数表を持っており、**本節も2026-09-05 まで「サーバー側に対応物は無い」と書いていた**（記録は[`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §99）。
- **第1.1節の線とは別軸である。** 同じトークンの式を使うが別のワークロードであり（素材動画自身のVAE符号化とブレンドが乗る）、**統合してはならない。この原則は2回の較正のどちらでも変わっていない**——変わったのは、その別軸の線の値が「連結生成からの流用」から「画角拡張自身の実測」（2026-09-04）へ、さらに「加速を全onにした構成での実測」（2026-09-05）へ、と2度置き換わったことだけである。**2026-09-05 の対照実験は、この「別軸」という設計判断そのものを実測で裏づけた**（第9.1節の3つ目）。

**第1回の較正（2026-09-04・サーバー既定の加速構成）の一次記録は `outputs/comfort-calib-2026-09-04/RESULTS_comfort-calib-2026-09-04.md` 第4節・第5節・第6.2節**（全計測点の表・仮説の検証・境界の一次読みはそちらが正本。ここには写さない。複写: [`Docs/Outputs-archive/comfort-calib-2026-09-04/`](Outputs-archive/comfort-calib-2026-09-04/)）。較正そのものの記録は[`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §95、フロントエンドへの反映は[`PENDING_TASKS_CLOSED.md`](PENDING_TASKS_CLOSED.md) §3-134でクローズ済みである。**現在の線の根拠である第2回の較正（2026-09-05・全on構成）は、本節冒頭のポインタを見よ。**

---

## 付記（前提と限界）

- 本書の実測アンカーは、**節ごとに構成を断っている場合を除き**、すべて**加速設定を全てonにした構成**・シード12345・車のプロンプト・24fpsで統一して測定されたものである。構成が1つでも異なれば線の値は保証されない。**「全on」の中身はエンジン系統で違うので、次の2つを取り違えないこと。**
  - **LTX 2.3（`ltx`）の全on**: sage attention・block_swap_prefetch・keep_resident・fused_gguf_dequant_kernel・vae_mode=prune_vaed の5項目on。6項目目にあたる`keep_resident_embeddings`（LTX 2.5の埋め込み処理器をジョブ間で常駐させる設定）は、**この系統では真にすると422で断られる**ため、offであることが全onである（この系統には埋め込み処理器という部品が無い）。**定義の正本は`config.py`が`limits.comfort_budgets`として配信する`ltx`行の`requires`**で、第1.1節の表はその写しである。
  - **LTX 2.5（`ltx25`）の全on**: sage attention・block_swap_prefetch・keep_resident・fused_gguf_dequant_kernel・**`keep_resident_embeddings`をon**、`vae_mode`は既定（この系統は`prune_vaed`を422で断る）。**この系統には「全onとは何か」を定めた正本が製品のどこにも無い**——`config.py`の2.5の行は条件が空（無条件）だからである。**上の6項目は、第9節の較正（2026-09-05）が「LTX 2.5 で合法にとれる最大の加速」として初めて定義したもの**であり、実測の副産物であって製品の宣言ではない。他所へ持ち出すときは、この断りを必ず添えること。
- 表の計算では、幅と高さの割り算の切り捨てを厳密に再現している。実際に選べる解像度は64の倍数なので、目安解像度の実トークン数は44,880をやや下回ることが多い。
- 旧40,000トークン線は既定構成時代の較正であり、全on構成の実測線44,880に置き換わる。ただし既定構成向けのフォールバックとしては`spill_free_frames`が引き続き正であり、40,000という数値そのものが誤りだったわけではない。
- 本表は60行すべてを実機検証したものではない。検証済みなのは第4節のアンカー（線の位置とその周辺の判定）であり、表はその線を式に代入して機械的に生成した計算値である。
- **レガシー表`spill_free_frames`は 2026-08-31 に、3解像度（1280×768・1920×1088・2560×1472）を再測定し、うち2つが動いた**（判定規則 v3・LTX 2.3 既定構成。境界の根拠は第4.8節、較正の記録は[`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §84、一次記録は `outputs/comfort-calib-2026-08-31/RESULTS.md`。複写: [`Docs/Outputs-archive/comfort-calib-2026-08-31/`](Outputs-archive/comfort-calib-2026-08-31/)）。**512×320 と 960×576 は API 上限の481フレームで頭打ちになるため測定対象外で、値は据え置きである。** **この表の値をここ以外の文書へ書き写さないこと**——設定の実体は`config.yaml`の`limits.spill_free_frames`であり（`config.yaml` は git 追跡外。リポジトリで確認するときは配布元の `config.yaml.example`。`config.py` の既定は空の辞書で、鍵が無ければ配信されない）、説明の正本は本書のこの項である。

  | 生成サイズ | 快適上限フレーム数 | 前の値 |
  | :--- | ---: | :--- |
  | 512×320 | 481 | 据え置き（API上限） |
  | 960×576 | 481 | 据え置き（API上限） |
  | 1280×768 | 273 | 257 |
  | 1920×1088 | 161 | 153 |
  | 2560×1472 | 81 | 据え置き |

**追記（2026-08-19）**: `config.yaml`の`standard_720p`/`FHD_1080p`/`WQHD_1440p`プリセットのフレーム数（257/153/81）が、本書の線44,880を第1.3節の逆算式に代入した値（361/169/89）へ引き上げられた（`generation_defaults`も361へ揃えた）。第3節の表でも169・89・361の各行がちょうど100.0%・98.4%・98.4%を刻んでおり、この改修は本書と整合している。旧値257/153/81は`limits.spill_free_frames`（線に一致する行が無いときのフォールバック表）の値と一致していたが、プリセットとspill_free_framesは今後別々の数値になる——このときspill_free_frames自体は据え置きだった。詳細はフロントエンド`Docs/DEVLOG.md` §85。**そのspill_free_framesは 2026-08-31 に再測定して更新した**（上記の項）。
