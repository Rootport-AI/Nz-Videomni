# マルチエンジン設計 — 複数の動画生成AIを切り替えて使えるようにする

## 本書の位置づけ

**生きた文書（マルチエンジン化の設計正本）。** ヘッダーのドロップダウンで「LTX 2.3」「LTX 2.5」といった**ベースモデル**（動画生成AIの土台となるモデル一式）を切り替え、それぞれに適した**推論エンジン**（実際に計算を回す実装）でサーバーが動く仕組みを記述する。**本書には現在の姿だけを現在形で書く。** どういう順序でここへ辿り着いたか（設計当時の見立て・実装での訂正・敵対的レビューの指摘）は記録簿側が保持しているので、本書は繰り返さない——起票とクローズの記録は [`PENDING_TASKS_CLOSED.md`](PENDING_TASKS_CLOSED.md) §3-97（土台）・§3-98（LTX 2.5）、検証と実測は [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §68（土台）・§69（LTX 2.5）にある。

**現在の実装範囲**: 土台と、その上に載る2系統（LTX 2.3 と LTX 2.5）が動いている。**LTX 2.5 で使えるのは、基本生成（テキストから動画・画像から動画）と、2026-08-23 に加わったクリップ連結（Chained）・V2V 継続・A2V〔Single・長尺・バッチ〕と、2026-08-24 に加わったスタイル LoRA・IC-LoRA〔参照動画による制御。1 本の参照動画をクリップごとに配る長尺 IC-LoRA を含む〕、2026-08-24 に加わった高速化2つ〔fused GGUF逆量子化カーネル・先読み block swap。いずれも既定 on〕、2026-08-25 に加わったモデル骨格の常駐〔`keep_resident`。既定 off のオプトイン〕と SageAttention〔`attention_backend`。既定は `"sdpa"` のまま〕、2026-08-26 に加わった撮り直し〔Retake〕と素材（末尾）〔End source〕、2026-08-29 に加わった画角拡張〔Outpainting〕、2026-08-30 に加わった非CFGネガティブプロンプト〔NAG／VSF〕である**（§5.6）。**まだ使えないのは PrunaVAED と非蒸留パイプラインの2件で、いずれも「モード」ではなくエンジンが持たない機能である**——**しかも残る2件はどちらも外部要因待ちで、いま着手できる課題は1つも無い**（非蒸留モデルはそれを回せる計算機が、PrunaVAED は上流の LTX 2.5 版の枝刈り済み重みが、それぞれ揃っていない）——**タブ・サブタブ・カードとして画面に出るモードは、単発生成の側も連結生成の側も、すべて LTX 2.5 で動く。** **このうち 2026-08-25 までに加わった機能は、すべてオーナーの実機確認に合格済みである**（クリップ連結は 2026-08-23、V2V 継続と A2V は 2026-08-24、スタイル LoRA と IC-LoRA は 2026-08-25。長尺 A2V はオーナー裁定により合格扱い）。**ただし高速化3つ（fused GGUF逆量子化カーネル・先読み block swap・モデル骨格の常駐）にはオーナーの目視ゲートが無い**——出力が固定ベンチマークと mp4 の SHA-256 で一致すること、すなわち**絵が1バイトも変わっていないことを機械で証明できている**ためで、実機確認を経ていないのではなく、目視で確かめるべき差が存在しない、という意味である（[`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §75.9・§76.5・§76.7）。**残る1つ（SageAttention）だけは選ぶと絵の細部が変わるので目視ゲートがあり、2026-08-25 に合格した**（絵の見比べ4項目と音の聴き取り1項目。同 §77.10）。**2026-08-29 の画角拡張は、同日にオーナーの目視ゲート（G8′）にも合格して完結した**（実測は [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §79・§79.10、目視の結果は同 **§79.11**）。**2026-08-26 の撮り直し・素材（末尾）も、2026-08-30 の NAG／VSF も、2026-08-30 にオーナーの目視ゲート（G8）へ合格して完結した**（撮り直し・素材（末尾）は [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) **§78.14**、NAG／VSF は同 **§80.10**。どちらもチェックリストを置いていた台帳の節は、全項目合格の運用規則どおり削除済みである）——**この2件はどちらも出来上がる絵そのものを変える改修なので、高速化3件のように SHA-256 の一致では合否を出せず、目視ゲートを設けてあった。** **これで LTX 2.5 の全段が、オーナーの目視まで含めて決着している**（**撮り直し・素材（末尾）の監督裁定2件＝R-1 の天井の読みと M8 のキャリー基準も、2026-08-30 にオーナーが追認して決着した**。同 **§78.15**。G8 の合格宣言には含まれておらず別途の追認だった経緯は同 §78.14(4)）。 その後続作業は [`PENDING_TASKS.md`](PENDING_TASKS.md) §3-102 で管理する（**残っているのは非蒸留モデルと、LTX 2.5 版の枝刈り済み重みが出るまで先送りの PrunaVAED の2件だけである**）。

本書のうち **【オーナー裁定】** と書いた規則は、オーナーの承認なしに変えない設計上の約束である。

### 関連文書

| 文書 | 関係 |
|------|------|
| [`PENDING_TASKS.md`](PENDING_TASKS.md) §3 | **生きている後続課題の一覧の正本。作業の入口はこちら。** 件数も項目番号も本書には書かない（文書ごとに数が食い違う事故を繰り返したため、列挙は台帳の1箇所へ集約した） |
| [`PENDING_TASKS_CLOSED.md`](PENDING_TASKS_CLOSED.md) §3-97・§3-98 | 本テーマの起票とクローズ記録（経緯の正本） |
| [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §67・§68・§69 | 実測と検証の正本（GGUF KVの実ファイル確認・仮想環境の容量・各ゲートの結果） |
| [`../Videomni_Backend_Specification.md`](../Videomni_Backend_Specification.md) §6 | 凍結 API 契約の正本。ベースモデル軸の加算は §6.9、LTX 2.5 の対応範囲は §6.10 |
| [`LTX25_RESEARCH_NOTES.md`](LTX25_RESEARCH_NOTES.md) | LTX 2.5 の事前調査（**参考資料**。設計の正本ではない） |
| [`MODEL_MANAGEMENT_DESIGN.md`](MODEL_MANAGEMENT_DESIGN.md) | **凍結文書**。Settings のモデル選択ドロップダウン（4カテゴリ）を作ったときの設計記録。本書はその上に**ベースモデル**という軸を足す |
| [`CHAIN_STAGE2_RESEARCH_NOTES.md`](CHAIN_STAGE2_RESEARCH_NOTES.md) | クリップ連結の設計正本。エンジンごとに異なるフレーム規約が影響する範囲の参照先 |

---

## 1. 結論（この機能が何をするのか）

**ヘッダーのドロップダウンで、使う動画生成AIを切り替えられる。**

利用者から見た振る舞いはこうなる。

1. ヘッダーのドロップダウンに「LTX 2.3」「LTX 2.5」……とベースモデル名が並ぶ。導入済みかどうかも一緒に表示される。
2. 導入済みのものを選ぶと、**その場でモデルの読み込みが始まる**。押すボタンは無い。
3. 読み込みが終われば、そのベースモデルで生成できる。Settings のモデル選択欄（transformer＝動画モデル / text_encoder＝テキストエンコーダ / video_vae＝動画VAE / audio＝音声モデル）の中身も、選んだベースモデルのものへ入れ替わる。
4. 未導入のものを選ぶと「まず導入してください」と案内が出て、切り替えは行われない。ダウンロードはサーバーがやらない。

内部では、**利用者が選ぶ単位（ベースモデル）**と**推論実装の単位（エンジン系統）**を分けてある。この分離により、新しいモデルを足すときの重さが2通りに分かれる。

- **既存のエンジン系統でそのまま読める重みなら、記述ファイル（manifest）を1本足すだけで済む。**
- **既存の系統と依存関係やコードが同居できない世代・系統は、専用の推論エンジンと専用の Python 環境を新設する**（既存の系統には一切手を入れない）。**LTX 2.5 がこれに当たり**、将来の Wan なども同じ扱いになる。

---

## 2. 責任分界の大原則 — エンジン系統は記述子が決め、KVメタデータが照合する 【オーナー裁定】

### 2.1 何が何を決めるか

**どの推論エンジンで動かすかは、選択中のベースモデルの記述子（manifest）が決める。** 記述子の `engine_family` から Runner クラスを引き（`services/engines/__init__.py` の `runner_class_for`、呼び出しは `services/pipeline_manager.py`）、その系統の仮想環境とワーカーが起動する。

**重みファイルの中身（GGUFのKVメタデータ）は、その選択が正しいかを照合する材料である。** 読み込みの直前に、選ばれた transformer のKVから系統を導き、記述子が宣言する系統と突き合わせる。**食い違っていても自動では解決しない——422 で止めて、選び直しを案内する（fail loud）。**

> このtransformerはltxv 2.5.0（LTX 2.5）です。選択中のベースモデル「LTX 2.3」はLTX 2.3用のため読み込めません。ベースモデルに「LTX 2.5」を選んでください。

黙って別の系統に切り替えないのは、**「利用者が選んだつもりのもの」と「実際に動いたもの」が食い違う状態を作らない**ためである（§6.1 と同じ思想）。逆に照合を省くと、LTX 2.3 のワーカーへ LTX 2.5 の重みが渡り、意味の分からない場所で落ちる——あるいは落ちずに誤った数値が出る。**交差する組み合わせが「動いてしまわない」ことのほうが、正しい組み合わせが通ることよりも重要である。**

### 2.2 判別材料 — 自前GGUFのKVメタデータ

判別に使うのは、GGUF ファイル（このプロジェクトが配布する量子化済み重みの形式）が先頭に持っている **KVメタデータ**（キーと値の組。ファイルが自分自身を説明するための欄）である。**判別は2段の契約**で、1つのキーでは決めない。

| 段 | キー | 何を決めるか | 判定 |
|---|---|---|---|
| 1段目 | `general.architecture` | **系統**（どのモデル一族か）。LTX 系の重みは 2.3 も 2.5 も一律 `ltxv` | `ltxv` 以外は別系統の重みなので 422 |
| 2段目 | `model_version` | **世代**（同じ系統の中のバリアント）。`2.3.0` / `2.5.0` のように入っており、上位2桁で判定する | そのエンジンが動かせない世代なら 422 |

**2段そろって初めて判別が成立する。1段目だけでは 2.3 と 2.5 を区別できず、2段目だけでは Gemma のような別系統を弾けない**（実ファイル4本での検分は [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §67.1・§68.2）。系統への対応表は `services/engines/__init__.py` の `FAMILY_BY_KV` で、`(ltxv, 2.3) → ltx` ／ `(ltxv, 2.5) → ltx25` の2行だけである。

**判定の対象は transformer カテゴリだけである。** 世代を名乗る刻印を持つのは transformer だけで、映像VAE・音声VAEは safetensors なのでKVを持たず、テキストエンコーダの GGUF は `general.architecture` を持つものの、**カテゴリ間の世代整合**（transformer が 2.5 なのに text_encoder が 2.3 用、という組み合わせの検出）は現時点のスコープ外である。

**キーが無いファイルは警告して通す。** 手元の正規の配布物・自家変換ファイルは2つのキーを必ず持つが、このプロジェクトの変換ツールを通していない持ち込みGGUFは持たないことがある。**キーが有って食い違うのは拒否、キーが無いのは WARNING を出して通す**——判別できない状態と、判別できて不一致な状態を、同じ扱いにしない。最終判断はエンジン自身のローダーに委ねる。

**記述子×KVの4組はすべて契約テストで固定してある**（LTX 2.3 の記述子×2.3のKV／LTX 2.5 の記述子×2.5のKV は通過、交差する2組は明示的な422）。

### 2.3 KVを読む経路

読み書きの分担は3つに分かれている。**読む・拾う・裁くを1本にまとめない**のがこの設計の要点である。

| モジュール | 役割 |
|---|---|
| `services/gguf_kv.py` | **読む**。GGUFヘッダを走査して、指定されたキーだけを文字列の辞書で返す。欲しいキーが揃った時点で読み止める。構造が壊れていれば `GgufParseError`。要求したキーがファイルに無いのは異常ではない（辞書に入らないだけ） |
| `services/model_registry.py` の `precheck_model_file` | **拾う**。軽量チェック（GGUFのマジックナンバー・safetensorsヘッダの妥当性）に加え、GGUFならKVの辞書も一緒に返す |
| `services/engines/__init__.py` の `check_kv` と、各アダプタの `check_kv` | **裁く**。前者が系統の照合（§2.1）、後者が系統内の判定（アーキテクチャ名・扱える世代）を行う。**裁きだけがエンジン固有の知識**なので、エンジン側に置いてある |

`gguf_kv.py` を独立モジュールにしてあるのは責務が別だからである——`model_registry.py` は「名前をパスへ解決する」役、`gguf_kv.py` は「ファイルの先頭を読んで辞書を返す」役で、後者はモデル管理の概念を一切知らない。おかげで単体テスト（`tests/test_gguf_kv.py`）が合成バイト列だけで完結し、実重みファイルを必要としない。

**パーサは自前である。** アプリ側の仮想環境（`.venv`）には GGUF パーサ用の `gguf` パッケージが入っておらず、入れると「アプリ venv は torch 無し」という前提（§5.3）が壊れる。読みたいのは文字列型のKVが2個だけなので、汎用パーサは要らない。なお `engine/gguf/loader_service.py` の `metadata()` は KV から埋め込みconfig JSON（`config` / `ltx.config` / `general.config`）を取り出すだけで、系統の判別には関与しない。

### 2.4 規約（契約）としての保証 【オーナー裁定】

**Nz-Videomni が扱うGGUFは、変換ツールも配布リポジトリも自前である。したがって「transformer のGGUFは `general.architecture` と `model_version` を必ず持つ」を規約として保証する。**

- 変換ツール `Nz-GGUF-Converter-LTX23` が両キーを書き出す（LTX 2.5 用プロファイルを含む）。**別リポジトリへの要求事項**なので、本書側で強制する手段は無い。
- 配布物（HuggingFace の `Rootport/Nz-LTX23-weights` ほか）も、利用者の自家変換ファイルも、同じツールの出力なので自動的に規約を満たす（実ファイルでの確認は [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §67.1）。

将来 safetensors を直接読むエンジンを足すときは、GGUFのKVに相当するものが無い。その場合は **ComfyUI 方式のヘッダキー指紋**——safetensors ヘッダに並ぶテンソル名の特徴的な組み合わせを見てモデル種別を当てる方法——で対応できる。上流の LTX wheel（`ltx_pipelines/utils/constants.py` の `detect_params()`）には safetensors メタデータから `model_version` を読む前例もあり、そちらを流用してもよい。**安全に拡張できる余地があることだけ記録しておき、実装は必要になった時点でよい。**

### 2.5 フールプルーフは作らない。エラー品質で解決する 【オーナー裁定】

**「transformer に LTX 2.5 を選んだから VAE も自動で 2.5 に差し替える」といった自動選択・自動修正は作らない。組み合わせが不整合なら、エラーを出して止まる。**

理由は、自動修正が「利用者が意図してやった実験」まで勝手に書き換えてしまうためである。このプロジェクトは自家変換の重みを混ぜて使う利用者を想定しており、勝手な差し替えは害のほうが大きい。

ただし**エラーの質は上げる**。重みを実際に読み込む前——ワーカープロセスを起動する前——に、選ばれたファイルのKVを読んで整合を確かめ、食い違っていれば§2.1のような「次の一手を言う」文章で止める。これは自動修正ではなく**エラーメッセージの品質**の話であり、既存の `precheck_model_file`（数バイトだけ読む軽量チェック）と同じ位置に、同じ思想で足してある。

---

## 3. 概念モデル — 2つの軸を分ける

本設計の中核は、**「モデル」という1つの概念を、性質の違う2つの軸に分けること**である。

### 3.1 BaseModel（ベースモデル）— 利用者に見える単位

「LTX 2.3」「LTX 2.5」のような、**利用者が名前で認識し、ドロップダウンで選ぶ単位**。ベースモデル1つに対して、ドロップダウンの項目（「LTX 2.3」）・モデル置き場のディレクトリ（`models/LTX23/`）・記述ファイル（`scripts/manifests/10-ltx23.json`）が1対1で対応する。

「置き場所がそのまま所属の宣言になる」という現行の `models/` レイアウト（[`NEXT_SESSION_HANDOFF.md`](NEXT_SESSION_HANDOFF.md) §4）は、この軸にそのまま合致する。**現行レイアウトは変更しない。**

導入口はベースモデル別ではなく `setup.bat` の1本である（§6.2）。

### 3.2 EngineFamily（エンジン系統）— 推論実装の単位

「ltx」「ltx25」のような、**実際に計算を回す実装の単位**。エンジン系統ひとつが次の4つを所有する。

- 専用のワーカープロセス（`engine/worker.py`・`engine25/worker.py`）
- 専用の Python 仮想環境（`.venv-engine`・`.venv-engine-ltx25`）
- ワーカーへ送るペイロード（読み込み要求・生成要求の項目一覧）の形
- パラメータの制約と、扱える機能の範囲（§5.6）

### 3.3 2つの軸の関係 — ベースモデルからエンジン系統へは多対1

```text
BaseModel        EngineFamily
─────────        ────────────
LTX 2.3  ──────→ ltx        （engine/   + .venv-engine）
LTX 2.5  ──────→ ltx25      （engine25/ + .venv-engine-ltx25）

Wan 2.2  ──────→ wan        （将来）
```

写像は多対1である——同じエンジン系統に複数のベースモデルが載ることはありうる。**現在は2つのベースモデルがそれぞれ別の系統に載っており、1対1になっている。**

**LTX 2.5 が LTX 2.3 と同じ `ltx` 系統に載らなかった理由は2つある。どちらも「同居できない」という物理的な事実であり、設計の好みではない。**

1. **LTX 2.3 側の推論スタックには LTX 2.5 のコードが1行も入っていない。** `.venv-engine` が固定している公式パッケージ（`ltx-core` / `ltx-pipelines`）は2026年3月のコミット `00dc53d` で、LTX 2.5 に対応した公式 v1.2.0 の**5か月前**である。2.5 の読み込みに要るシンボルは1つも存在しない。「分岐を足す」のではなく「まだ書かれていないコードを持ち込む」話なので、既存の仮想環境の中では始まらない。
2. **テキストエンコーダの要求バージョンが同居できない。** LTX 2.3 は Gemma 3、LTX 2.5 は Gemma 4 を使い、Gemma 4 を扱えるのは `transformers` 5.8 以上である。1つのインタプリタに両方のバージョンを入れることはできない。

**別系統にしたことで守られたもの**: `engine/` と `.venv-engine`（LTX 2.3側）は**1バイトも変更していない**。同じシードでの LTX 2.3 の生成がビット単位まで従来どおりであることを実機で確認済みである（[`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §69.16）。**同居させていれば 2.3 の推論スタックを丸ごと上げることになり、既存の全機能を再検証する羽目になっていた。**

したがって新しいモデルを足すときの重さは、次の判断で決まる。

| 追加するもの | 必要な作業 |
|---|---|
| **既存のエンジン系統でそのまま読める重み** | 記述ファイルを1本足す。必要ならGGUF変換プロファイルを足す。**新しい仮想環境もワーカーも要らない** |
| **依存関係やコードが同居できない世代・系統**（LTX 2.5・将来の Wan 等） | 上記に加えて、専用ワーカー・専用仮想環境・専用アダプタを新設する。既存系統には手を入れない |

---

## 4. 記述子（SSOT）— ベースモデルの正体を宣言する

### 4.1 カテゴリ集合の正本は記述子である

**「このベースモデルがどのカテゴリのファイルでできているか」を宣言するのは記述ファイル（manifest）であり、コードの定数ではない。** コード側は「記述ファイルが言っているカテゴリ」を扱うだけの汎用処理になっている。

かつては同じ4カテゴリの定義がコードの6箇所（レジストリの定義本体・その順序タプル・API 2箇所・ワーカーペイロードの対応表・フロントエンドの定数）に散らばっており、カテゴリ構成が変わるたびに全部を直す必要があった。現在の残り方は次のとおりである。

- **カテゴリの定義本体は記述ファイルにある。** レジストリはそれを読んでスキャンする。
- `CATEGORIES`（モジュール定数のリテラル）だけは残してある。これを import している3ファイルを触らずに済ませるためである。
- フロントエンドの `MODEL_CATEGORIES`（`webui/src/shell/useModels.ts`）も**型の器として温存**している。撤去すると `ModelCategory` 型が `string` に退化し、カテゴリ名の打ち間違いをコンパイル時に検出できなくなるためである。**「何を何の順で表示するか」を決めるのはサーバー**（`GET /models` の応答）であり、この定数は綴りを固定しているだけで表示の決定権を持たない。

**カテゴリ集合そのものは4種のままで、可変にはしていない。** LTX 2.5 も transformer / text_encoder / video_vae / audio の同じ4種で、変わったのは中身（Gemma 4・2.5の畳み込みVAE）だけだった。**カテゴリ軸は製品の性質であってエンジン世代の性質ではない**——利用者から見て「動画モデル」「テキストエンコーダ」「動画VAE」「音声モデル」という4つの交換単位があるという事実は世代で変わらず、`state.json` もこの名前で選択を覚えている。4種を超えて増減させる一般化は、実際に必要になるまで行わない（§8.1）。

### 4.2 記述ファイルは2種類ある

置き場所は `scripts/manifests/` で、インストーラ `scripts/install_ltx.ps1` と同じものをサーバーも読む。インストーラはこの記述ファイルに完全に駆動されており（`Import-ModelManifests` が `scripts/manifests/*.json` を読み、スクリプト自体はモデル名を一切持たない）、スキーマ番号による版管理も形式検証（`Test-ManifestShape`。schema 1 と 2 の両方を受理する）も備わっている。

- **ベースモデル記述子**（`10-ltx23.json`・`20-ltx25.json`）— `engine_family` を持つ。**これだけがヘッダーのドロップダウン・ベースモデル一覧に出る。**
- **共有資産記述子**（`00-preprocessors.json` など）— `engine_family` を持たない。どのベースモデルからも参照される共有素材を記述する。`setup.bat` の「全部読んで全部入れる」経路には含まれるが、ベースモデル切替のUIには現れない。

### 4.3 記述子の形

```jsonc
{
  "schema": 2,
  "id": "LTX23",
  "display_name": "LTX 2.3",        // ドロップダウンに出す表示名
  "engine_family": "ltx",            // このベースモデルを動かすエンジン系統（§2.1）

  // このベースモデルが必要とするカテゴリの集合。
  // カテゴリごとに「どこを探すか」「どの拡張子を受け付けるか」「既定は何か」を宣言する。
  "categories": {
    "transformer": {
      "scan": ["LTX23/Weights"],
      "extensions": [".gguf"],
      "recursive": true,
      "default_file": "LTX23/Weights/LTX-2.3-22B-distilled-1.1-Q4_K_M.gguf"
    },
    "text_encoder": { "scan": ["LTX23/TextEncoder"], "extensions": [".gguf"],
                      "default_file": "LTX23/TextEncoder/gemma-3-12b-it-Q4_K_M.gguf" },
    "video_vae":   { "scan": ["LTX23/VAE"], "extensions": [".safetensors"], "name_hint": "video",
                      "default_file": "LTX23/VAE/LTX23_video_vae_bf16.safetensors" },
    "audio":       { "scan": ["LTX23/VAE"], "extensions": [".safetensors"], "name_hint": "audio",
                      "default_file": "LTX23/VAE/LTX23_audio_vae_bf16.safetensors" }
  },

  // 選び替えの対象ではないが読み込みに要るファイル（models/ 相対）。
  // カテゴリに混ぜるとドロップダウンに出てしまうので分けてある。
  "assets": {
    "gemma_root": "LTX23/TextEncoder/tokenizer",
    "spatial_upsampler_path": "LTX23/Upscaler/ltx-2.3-spatial-upscaler-x2-1.1.safetensors",
    "component_text_projection_path": "LTX23/TextEncoder/ltx-2.3_text_projection_bf16.safetensors",
    "component_video_vae_pruned_path": "LTX23/VAE/prunavaed/PrunaVAED-decoder-bf16.safetensors"
  },

  // 初回選択時の既定の組み合わせ（カテゴリ → 登録名）。
  "default_selection": {
    "transformer": "default", "text_encoder": "default",
    "video_vae": "default", "audio": "default"
  },

  "downloads": [ /* インストーラが読む */ ],
  "migrate":   [ /* インストーラが読む */ ]
}
```

補足が3点ある。

- **`default_selection` の値は、レジストリの登録名か予約センチネル `"default"` のいずれかでなければならない。** レジストリの走査は、既定パスと同一実体を指すファイルにスキャンで別名を作らない（既定パスと同じファイルは常に `"default"` という名前だけで参照する設計である）。ファイル名ベースで直接書くとその名前がレジストリに存在せず、`resolve()` が 404 を返す。ファイル名で指定したい場合は、config の明示登録map（`model.*_names` 相当）側にも同じ名前で登録しておく必要がある。
- **`assets` の内容は系統ごとに違う。** LTX 2.5 側の `assets` は空間アップスケーラ1つだけである——tokenizer とプロセッサのメタデータは**テキストエンコーダのGGUFの中**にあり、`engine25/assets_export.py` がそれをGGUFの隣へ書き出すためで、LTX 2.3 の `gemma_root` や text projection に相当するものは存在しない。**音声VAEはワーカーに必須だが `assets` へは入れない**（カテゴリ軸で届くので、二重に宣言すると整合先が2箇所になる）。
- 記述ファイルは既存のものにならって **UTF-8（BOM 付き）** で保存する。Python 側は `encoding="utf-8-sig"` で読む。

**この記述ファイルを、インストーラ（`downloads` / `migrate` を読む）・レジストリ（`categories` でスキャンする）・API（`categories` のキー集合でリクエストを検証する）・フロントエンド（`GET /models` の応答で受け取る）の4者が全員読む。** これが「散在の解消」の実体である。

### 4.4 ワーカーへ送る項目名はエンジンアダプタが持つ

**カテゴリ名からワーカーペイロードの項目名への対応表（`SELECTION_FIELDS`）は、記述ファイルではなくエンジンアダプタ側にある**（`services/engines/ltx/adapter.py`・`services/engines/ltx25/adapter.py`）。項目名はエンジン系統に属する知識であってベースモデルに属する知識ではないからである。**カテゴリ名（キー）は両系統とも同じ4種で、違うのは送る項目名（値）だけ**である。

置き場所そのものより大事なのは**二重定義を作らないこと**である。

- **`SELECTION_FIELDS` は写しではなく本体である。** ペイロードを組み立てる `_build_load_payload` がこの辞書を実際に読んで各項目を埋める。転記していないのでずれようがない。**参照されていない定義は作らない。**
- **記述子のカテゴリ集合との一致は契約テストで固定している**（`tests/test_base_model_contract.py`）。記述ファイルの `categories` のキー集合とアダプタの `SELECTION_FIELDS` のキー集合が一致しないとテストが落ちる。**記述ファイルに新しいカテゴリを足してアダプタ側を足し忘れると、そのカテゴリは黙って何もしない**——この「静かな失敗」だけを潰せばよく、そのためには定義を1本化するよりテストで縛るほうが確実である。

---

## 5. 層の構成

### 5.1 モデルレジストリと `GET /models`

**スキャンの根は記述ファイルの `categories[].scan`（配列）から取る。** ベースモデルごとに独立してスキャンし、結果もベースモデルごとに分ける。これにより LTX 2.3 と LTX 2.5 の transformer を同時に一覧できる（config の明示登録map経由なら、任意のパスを名前付きで登録して一覧に載せることも従来どおりできる）。

**`GET /models` の応答は、旧構造 `categories` を残したまま3層構造を並置する「加算」である。**

```jsonc
{
  "categories": { ... },            // 従来どおり。中身は「現在アクティブなベースモデル」のもの
  "active_base_model": "LTX23",
  "base_models": [                  // ベースモデル→カテゴリ→登録名 の3層はこちら
    {
      "id": "LTX23", "display_name": "LTX 2.3", "engine_family": "ltx",
      "active": true,
      "installed": true,            // 全カテゴリのファイルが実在する
      "present": true,              // 1つ以上のカテゴリのファイルが実在する
      "missing_categories": [],
      "category_order": ["transformer", "text_encoder", "video_vae", "audio"],
      "unsupported_features": [],   // §5.6
      "categories": { ... }
    },
    { "id": "LTX25", "installed": false, "present": true,
      "missing_categories": ["text_encoder", "video_vae", "audio"], ... }
  ]
}
```

置き換えではなく加算にしてあるのは、**`categories` を読んでいる既存の利用者が実在するから**である。Gradio 側のアダプタ（`gradio_ui/adapters.py`）がこの構造を直接読んでおり、置き換えれば同時に直す必要が出る。加算なら**その利用者は1文字も変えずに生き続ける**。

**`category_order` を配列で持つ理由**: カテゴリの表示順の正本は記述子のキー順（＝交換頻度順）であり、`categories` オブジェクトのキー順にも同じ並びが出ている。ところが**JSONオブジェクトのキー順は転送を越えて保たれるとは限らない**——サーバーもフロントエンドのコードも順序を保っているのに、AviUtl2 側のWebUIでは並びがアルファベット順になる現象が実際に起きた（疑いは唯一検証できていない AviUtl2 プラグインの WebView2 メッセージ経路に残る）。**配列の要素順にはこの曖昧さが無い**ので、順序は配列で運び、受け手はキー順に依存しない。

**`installed` と `present` を分けてあるのは、「未導入」と「一部だけ導入済み」を利用者に区別して見せるため**である。`present: false` なら「まだ導入されていません」と案内してAPIを叩かず、`present: true` かつ `installed: false` なら「一部未導入」と表示したうえで**サーバーに理由を言わせる**（§6.2）——フロントエンドが勝手に「たぶん無理だろう」と短絡しない、というのがこの2フィールドの目的である。

### 5.2 エンジンアダプタ

**エンジン系統ごとに1つのアダプタがある**（`services/engines/ltx/adapter.py`・`services/engines/ltx25/adapter.py`）。系統の対応表とディスパッチは `services/engines/__init__.py` が持つ。

- **対応表はアダプタのクラスではなく、モジュールパスを文字列で持つ**（`FAMILY_BY_ID`）。呼ばれた系統だけを `importlib` で解決するので、ベースモデルを列挙したいだけのプロセス（インストーラ・MCPサーバー・起動時の記述子検査）が、全アダプタを芋づるに読み込む代金を払わずに済む。
- **`ltx25` アダプタは `ltx` アダプタを継承している。** 共有しているのは `@@LTX@@` プロトコル・サブプロセスの起動と後始末・進捗の受け取り・mock バックエンドで、**エンジン世代で本当に違う事実だけ**を上書きする（どの仮想環境とワーカーか、どのワーカーログへ書くか、どのカテゴリがどのペイロード項目になるか、子プロセスに渡す環境変数、扱える機能の範囲）。
- **「汎用の EngineRunner」と「エンジン別アダプタ」への分割は行っていない。** 共有できたのはすでに1ファイルに固まっていた部分で、別ファイルへ切り出す実利が無かった。**分割の要否は3例目（Wan 等）が来たときに改めて判断する**（§8.1）。

**現行コードは書き直さない。** `_build_load_payload` は「ペイロードのキー集合と挿入順まで契約であり、`tests/test_model_swap_load.py` のゴールデンスナップショットで固定されている」と自身のドキュメント文字列に書いている。系統を足すときも、既存系統の振る舞いを変えないこと——変えていないことは、このゴールデンスナップショットが通り続けることで確認できる。

### 5.3 ワーカーと仮想環境 — エンジン系統ごとに1つ 【オーナー裁定】

**エンジン系統ごとに専用の Python 仮想環境を持つ。** アプリと推論の2プロセス・2仮想環境構成を、そのまま横に増やした形である。

```text
.venv                … アプリ（FastAPI）。torch 無し
.venv-engine         … ltx エンジン系統（LTX 2.3）。torch+cu128
.venv-engine-ltx25   … ltx25 エンジン系統（LTX 2.5）。torch+cu128
.venv-wan            … wan エンジン系統。torch+cu128（将来）
```

系統ごとに違うのは主に次の3点で、**これが「同じ仮想環境に同居できない」ことの実体**である（§3.3）。

| 項目 | ltx（LTX 2.3） | ltx25（LTX 2.5） |
|---|---|---|
| 公式パッケージ | `ltx-core` / `ltx-pipelines`（2026-03 の `00dc53d`） | 同（公式 **v1.2.0** = `d151147`） |
| `transformers` | Gemma 3 が動く版 | **5.14.1**（Gemma 4 には 5.8 以上が要る） |
| 高速化ライブラリ | `sageattention` あり | **あり**（2026-08-25 の高速化第3弾＝C0 `1fb4fb8` で 2.2.0 を導入。**wheel は 2.3 側とバイト単位で同一**。`sage_available` は恒久 false の上書きをやめ、C2 `cb97613` 以降は**2.3 とまったく同じ実探索**〔ロード前＝この仮想環境の site-packages のファイル存在／ロード後＝ワーカー自身の import 判定〕で答える） |

`.venv-engine-ltx25` の確定記録は `engine25/venv-engine-ltx25.freeze.txt`（50パッケージ）。インストーラ側は、エンジン用仮想環境の構築を `Ensure-EngineVenv`（必須引数7つ）へ抽出して**2回呼ぶ**形にしてある。系統ごとにコピーを作らないのは、マーカー判定のロジックが2箇所へ分かれると最初の修正で食い違うためである。

**なぜアプリと推論で venv を分けるか**: (1) LTX 推論スタック（torch+cu128 と `ltx-core`・`ltx-pipelines` ほか）は PyPI ではなく PyTorch の cu128 wheel index から取る必要がある。(2) アプリ側スタックと推論側スタックは**両方ともトップレベルに `services` パッケージを持つため、同一インタプリタに同居できない**（import 時に衝突する）。この分離を新しいエンジン系統にもそのまま踏襲する。

#### 容量は心配しなくてよい

仮想環境を増やすと数GBずつ増える、という直感は**この構成では当たらない**。パッケージ管理ツール `uv` はダウンロードした実体を `.uv_cache/` に1つだけ置き、各仮想環境からはそこへ**ハードリンク**（同じファイル実体に別名を付けたもの）を張る。**実体は `.uv_cache` 側にあり、各仮想環境が示す容量はその別名を数え直した見かけの数字にすぎない。** したがって**同じバージョンの torch を使う限り、2つ目以降のエンジン用仮想環境を作っても物理的なディスク消費はほとんど増えない**（増えるのは、その系統にしか無い固有パッケージの分だけ）。実測値とハードリンク共有の確認手順は [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §67.2 を参照。

> **この実測値は `.venv-engine-ltx25` を作る前のものである。** 追加後の再実測は [`PENDING_TASKS.md`](PENDING_TASKS.md) §3-107 で管理する（2系統とも torch 2.9.1+cu128 で揃っているため、増えたのは 2.5 にしか無いパッケージの分だけのはずだが、数字は取り直していない）。

**方針: 全エンジンで torch のバージョンを揃える 【オーナー裁定】。** 上の利点はバージョンが一致していて初めて成立する。揃えられない依存関係が出てきた場合は、容量が実際に増えることを承知のうえでオーナー判断を仰ぐこと。

**前提としてよい利用者像 【オーナー裁定】**: 「同時に導入するベースモデルは2〜3種類」を前提にしてよい。全ベースモデルを同時に置ける前提での容量最適化は不要である。

### 5.4 状態の3層憲章 【オーナー裁定】

**設定・実行時状態・端末の好みを、3つの置き場所へはっきり分ける。**

| 層 | 置き場所 | 誰が書くか | 何を持つか |
|---|---|---|---|
| **設定** | `config.yaml` | 人と、自動移行時のインストーラ。**サーバーは読むだけ** | **ユーザーの上書き・好みのみ**（明示登録map・プリセット・上限値・VRAMのつまみ）。**モデルの既定パスは持たない** |
| **実行時状態** | `state.json` | **サーバーだけ** | 最後に使ったベースモデル。ベースモデル別の「最後に使った組み合わせ」 |
| **端末のUIの好み** | ブラウザの localStorage | フロントエンドだけ | 表示言語・テーマ・高速化設定・NAGの文面・プリフィル方針（`nzvideomni.lang` / `.theme` / `.acceleration` / `.nagNegativeText` / `.prefillSizePolicy` / `.prefillFpsPolicy` の6キー） |

**モデルの既定パスの正本は記述子である。** インストーラも実行時のサーバーも同じ記述ファイルを読むので、「インストーラが認識するモデル構成」と「config が持つ既定パス」を同期し続けるコストは無い。config.yaml 側には**明示登録map（`model.*_names` 相当）だけを上書き手段として残してある**——利用者が自家変換ファイルを任意のパスで登録できる仕組みは、ベースモデル軸の有無と無関係に必要だからである。

**サーバーは `config.yaml` を書かない。** `config.py` は読み込み専用で、実行中に YAML を書き出す処理は無い。**この性質は維持する。**

#### `state.json`

ドロップダウンを切り替えたとき、Settings のモデル選択欄に何を初期値として出すかを決める必要がある（§6.3）。「前回このベースモデルで使った組み合わせ」を覚えておく場所がこれである。localStorage は端末の好みであってサーバーの状態ではないので、そこへ置くのは層の混同になる。

**置き場所はリポジトリのルート直下**（実装は `services/runtime_state.py`、設定上の位置は `AppConfig.state_file`。`.gitignore` に `/state.json` と `/state.json.bad` を登録済み）。`models/` 配下を選ばなかったのは、**`models/` は「重みファイルの置き場」という単一の意味を持つ場所**であり、そこにサーバーの実行時状態を混ぜると、利用者がモデルフォルダを丸ごと移動・削除したときに状態も道連れになるためである。[`STORAGE_POLICY.md`](STORAGE_POLICY.md) は Outputs（宝物）と Uploads（一時置き場）の設計正本だが、**`state.json` はそのどちらでもない**——生成物でも入力素材でもなく、消えても再生成される付箋にすぎない。

```jsonc
{
  "schema": 1,
  "active_base_model": "LTX23",
  "selections": {
    "LTX23": { "transformer": "Sulphur-2-base-distil-Q4_K_M",
               "text_encoder": "default", "video_vae": "default", "audio": "default" }
  }
}
```

- **読むのは起動時の1回だけ**。実行中に読み直さない（サーバーが唯一の書き手なので、読み直す理由が無い）。
- **書くのは読み込みが成功したときだけ**（§6.3の「失敗した組み合わせを覚えない」に対応）。一時ファイルへ書いてから差し替える原子的な方式なので、途中で電源が落ちても中途半端なファイルが残らない。
- **壊れていても止まらない**。JSONとして読めなければ WARNING を出し、`state.json.bad` へ退避して既定の組み合わせで起動する。未知のベースモデルidなら先頭のベースモデルへ落とす。書き込み失敗も WARNING を残して続行する。

#### LoRA はベースモデル軸を持たない

config.yaml の `lora_dir`（Style LoRA の置き場）と `ic_loras`（IC-LoRA アダプタの明示登録）は記述子へ移していない。したがって LoRA は現在ベースモデル軸を持たず、どのベースモデルを選んでいても同じ1本のディレクトリを見る。LoRA が**モデルの読み込み時ではなくジョブごとに適用される**別系統の資産であり、カテゴリとはライフサイクルが違うためである。**LTX 2.5 用の LoRA が実在するようになった時点で必要になる話**なので、[`PENDING_TASKS.md`](PENDING_TASKS.md) §3-102 が受け皿になっている。

### 5.5 API

**いずれも加算のみ**——既存フィールドの意味は変えない。契約の正本は [`../Videomni_Backend_Specification.md`](../Videomni_Backend_Specification.md) §6.9 で、本節は設計意図の記録である。

**(a) `POST /pipeline/load` の `base_model`**: リクエストに `base_model` を足した（省略時は現在のベースモデル）。現在のものと違えば、旧ワーカーを落としてから新しい系統のワーカーを起こす。`base_model` だけを送って `models` を省略した呼び出し（「ベースモデルだけ切り替えたい」という自然な呼び方）も効くようになっており、「同一選択なら作り直さない」という no-op 判定にも `base_model` を含めてある。

**ベースモデルを変えるときは、`"default"` のカテゴリも含めて全カテゴリを明示パスとして渡す。** ベースモデルが変われば `"default"` の指す先が別のファイルに変わるので、載せないままにすると**中身を一度も確かめないまま別世代の重みをワーカーへ渡す**ことになり、§2.1のKV照合が働くべき場所を素通りしてしまう。**代償**として、一度ベースモデルを切り替えた後のボディ無しロードのペイロードは「旧ペイロードとバイト同一」ではなく「値が同一」に格下げされる（同じファイルパスが、同じキーに、同じ順序で入る）。**ベースモデルを一度も切り替えていない限り、バイト同一の保証はそのまま生きる。** 登録名は `"default"` のままなので、画面の表示・no-op判定・`metadata.json` の `name` はいずれも変わらない。

**(b) `GET /status` の `state` と `base_model`**: サーバー内部には `unloaded` / `loading` / `ready` / `running` / `error` の5状態があるが、従来は `pipeline_loaded`（真偽値）しか外に出ておらず、**`loading`（読み込み中）が外部から観測できなかった**。フィールドを1つ足すだけで観測できるようになったのは、モデルの読み込みが呼び出し元をブロックする同期処理でも、**その間 `GET /status` は別のリクエストとして通る**（FastAPI が別スレッドで処理する）ためである。値の語彙は `POST /pipeline/load` の応答に既にある `state` と揃えてある。

### 5.6 エンジンごとの機能スコープ — `unsupported_features` と 422 `FEATURE_UNSUPPORTED`

**エンジン系統が2つあると、「このエンジンにはこの機能が無い」という状態が発生する。** §7が「エンジン別のUIパラメータ制約は作らない」と決めているのはパラメータの**刻みや倍数**の話であって、**機能そのものの有無**はそれとは別で、避けて通れない。LTX 2.5 の現行範囲は基本生成（テキストから動画／画像から動画 ＋ 畳み込みVAE ＋ 16GB運用）と、**クリップ連結（Chained）・V2V 継続・A2V〔Single・長尺・バッチ〕（いずれも 2026-08-23 に追加）**、**スタイルLoRA と IC-LoRA〔参照動画による制御。長尺＝クリップ別の参照窓を含む〕（2026-08-24 に追加）**、**高速化2つ＝fused GGUF逆量子化カーネルと先読み block swap〔いずれも既定 on。2026-08-24 に追加〕**、**モデル骨格の常駐＝`keep_resident`〔既定 off のオプトイン。2026-08-25 に追加〕**、**SageAttention＝`attention_backend`〔既定は `"sdpa"` のまま。2026-08-25 に追加〕**、**撮り直し＝Retake と 素材（末尾）＝End source（いずれも 2026-08-26 に追加）**、**画角拡張＝Outpainting（2026-08-29 に追加）**、**非CFGネガティブプロンプト＝NAG／VSF（2026-08-30 に追加）**で、非蒸留モデルと PrunaVAED は持っていない。

**LTX 2.5 が現在宣言している `unsupported_features` は2件である**（LTX 2.3 は0件＝空配列）。内訳は**単発生成の拒否表から自動的に作られる2件**（`two_stage_hq` / `prune_vaed`）だけで、**モード名はもう1つも残っていない**——連結生成の側は 2026-08-26 に、単発生成の側は 2026-08-29 にそうなった。**そして 2026-08-30 に `nag` が抜けたことで、残る2件はどちらも「外部要因が揃わないと着手できないもの」だけになった。** **`chain` / `v2v` / `a2v` は 2026-08-23 に、`loras` / `reference_video` は 2026-08-24 に、`keep_resident` と `sage_attention` は 2026-08-25 に、`retake` と `end_source` は 2026-08-26 に、`outpaint` は 2026-08-29 にこの一覧から外れた**——それぞれが LTX 2.5 で動くようになったので、宣言を残すと動くタブやパネルを灰色にしてしまうためである（`a2v` は Chained タブの音声カードに加え、Single タブの A2V とバッチ A2V の灰色化にも使われている）。

> **2026-08-26 の撮り直し・素材（末尾）で、この配列は6件 → 4件になり、同時に配列の性格が変わった。** モード名がすべて抜けたことで、**この配列はいまや単発生成の拒否表（`REJECT_TABLE`）をそのまま並べた式**になっている——**2つの表が食い違いようがない形**になったということであり、chain 側の判定（`CHAIN_REJECT_TABLE`）から付け足すモード名はもう存在しない。**画面では、撮り直しで Edit タブの「撮り直し」サブタブ（とタイムラインの右クリックからそこへ入る導線）の灰色が解け、素材（末尾）で Chained タブの「素材（末尾）」カードの灰色が解けた。** **ここでフロントエンド本体に手が入っている点が、高速化の3弾とは違う**——`keep_resident` と `sage_attention` のときは触ったのがモックだけだったが、本段では**Edit タブをサブタブ単位で灰色にできる仕組みそのものが新たに要った**（開通後の Edit タブは「撮り直しと Outpainting のどちらか一方でも動けば生きたまま」になるため、タブ単位のグレーアウトでは Outpainting だけを閉じられない）。実測は [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §78。

> **2026-08-30 の非CFGネガティブプロンプト（NAG／VSF）で、この配列は3件 → 2件になった。** **`nag` は、7つのフィールドが3つの表から同時に移動した唯一の事例である**——`nag_enabled` は422系から、`negative_prompt` / `neg_method` / `vsf_scale` は「無視＋ログ」から、`nag_scale` / `nag_tau` / `nag_alpha` は「従属」から、それぞれ「動作」へ移った。**従属の表は空になったが、消さずに残してある**（分類の網羅監査が名前で読むこと・「すべての従属フィールドの上位が422であること」を確かめるテストのループが正直に空になること・将来また422の背後に副パラメータができたときの置き場になること、の3点が理由。空であること自体は別のテストが固定している）。**画面のグレーアウトは1つも変わらない**——`nag` はフロントエンドの3つの表のどこにも現れない語で、NAG のトグルは `unsupported_features` を見ずに描かれているためである（押せば422が返る、という関係だった）。**フロントエンド本体の変更は0行で、触ったのはモックとそのテストだけである。** 実測は [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §80。
>
> **2026-08-29 の画角拡張（Outpainting）で、この配列は4件 → 3件になった。** **これで単発生成の側からもモード名が消え、残る3件はすべて「エンジンが持たない機能」である**（`two_stage_hq` / `nag` / `prune_vaed`）。**画面では、Edit タブの「画角拡張」サブタブの灰色が解けて、2つのサブタブが両方とも生きた**——2026-08-26 に新設したサブタブ単位のグレーアウトの仕組みは、いまはどちらのサブタブも灰色にしない状態で動いている（**仕組みを消してはいけない**。LTX 2.3 以外のベースモデルが将来また片方だけを断ったときに、そのまま効く）。**フロントエンド本体の変更は0行で、触ったのはモックの1語とテストだけである**——2026-08-26 の段で下地を作り終えていたためで、この違いがそのまま「下地を先に入れておく」ことの配当になっている。実測は [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §79。

> **高速化第2弾（2026-08-25）で、この配列が動いた（8件 → 7件）——高速化3弾の中では初めてである**（**配列そのものが動くのは初めてではない**。2026-08-23 に `chain` / `v2v` / `a2v`、2026-08-24 に `loras` / `reference_video` が、いずれも 422 側から抜けている）。 モデル骨格の常駐（`keep_resident`）は**もともと 422 で断っていた機能**なので、開通させると3つの表（`REJECT_TABLE` / `CHAIN_REJECT_TABLE` / `unsupported_features`）から1行ずつ消える。**「断っていた機能を開ける」ときと「無視していた項目を効かせる」ときとで、動く表が違う**——前者では4分類の対応表に加えてこの配列も動き、**画面のグレーアウトが1つ解ける**。それでも**フロントエンド本体の変更は0行**だった（Settings 画面はこの配列を読んでいないため。触ったのはモックの2箇所だけである）。実測は [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §76.4。

> **高速化第3弾（2026-08-25）でも、この配列がもう一度動いた（7件 → 6件）。** SageAttention（`attention_backend`）も**もともと 422 で断っていた機能**なので、第2弾と同じく3つの表から1行ずつ消え、**設定画面の attention の選択が LTX 2.5 でも押せるようになった**（フロントエンド本体の変更はここでも0行で、触ったのはモックの2箇所だけである）。**ただし、押せるようになった理由をこの配列に帰さないこと**——設定画面（`SettingsPanel`）は `unsupported_features` をそもそも読んでおらず、attention の可否は `GET /status` の `acceleration.sage_available` だけで決まる。**押せるようになったのは、その真理値表から LTX 2.5 の例外（恒久的に `false` を返す上書き）を削除したためである**（[`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §77.4）。**この配列が減ってもグレーアウトが変わらない項目がある**という区別に注意すること。**ただし本段には、前の2弾に無い性質が1つある**——**選ぶと出来上がる絵の細部が変わる**ので、**固定ベンチマークの SHA-256 一致では合否を出せず、オーナーの目視ゲートが要る**（**2026-08-25 に全項目合格した**。同 §77.10）。実測は [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §77。

> **高速化第1弾（2026-08-24）では `unsupported_features` は動いていない。** fused GGUF逆量子化カーネル（`fused_gguf_dequant_kernel`）と先読み block swap（`block_swap_prefetch`）は、もともと**「無視＋ログ」の側にあった項目**であって、`unsupported_features` で断っていた機能ではない。動いたのは4分類の対応表だけ（**「無視＋ログ」から「動作」へ2件ずつ**）で、**422 の表・従属の表・`unsupported_features` はいずれも無変更**である。したがって**画面のグレーアウトも1つも変わらず、フロントエンドのコードは1行も触っていない**。実測は [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §75.5。

**(1) 判断の主体はエンジンアダプタ1箇所。** 「何が使えないか」を宣言するのはアダプタ（`services/engines/ltx25/adapter.py`）だけで、API層は**系統名を渡して取り次ぐだけ**である。API層が系統ごとの機能表を持ち始めると、同じ表が2箇所に生まれて必ずずれる。宣言していない系統（`ltx`）は素通りするので、**LTX 2.3 から見た振る舞いは1バイトも変わらない**。

**(2) 強制するのは常にサーバー。エラーは 422 `FEATURE_UNSUPPORTED`。** ジョブを作る前に、他のどの検証よりも先に拒否する——素材の有無より前に「そのエンジンにその機能が無い」と答えないと、利用者は直しようのないものを直しに行くことになるためである。連結生成の一族（Chained・Retake・End source・V2V・A2V）はすべて `POST /generate/chain` から来るが、**「この一族を丸ごと扱えるか」ではなく「リクエストのどのフィールドが扱えないか」を答える**——2026-08-23 以降、LTX 2.5 は素のChained・V2V 継続・A2V を走らせるようになり、**2026-08-26 には Retake と End source も加わって、この一族はどのモードも通るようになった**（残る 422 は `nag_enabled` / `pipeline` / `vae_mode` の3つで、いずれもモードではなくフィールドの話である）。したがって取り次ぎの関数はリクエスト本体を受け取る（`engines.reject_chain(family, request)`）。

**(3) 先回りは `GET /models` の加算で行う。** `base_models[]` の各要素に `unsupported_features`（機能名の配列）がある。フロントエンドはこれを読み、選択中のベースモデルで**押しても必ず断られる操作**を押させないようにする（タブとパネルのグレーアウト）。**これはあくまで先回りであって強制ではない**——強制は常に(2)のサーバー側である。

> **省略は「制限なし」であって「全部だめ」ではない。** LTX 2.3 側は空配列を返すので、既存クライアントから見た応答は1バイトも変わらない。フィールドそのものを知らない古いバックエンドに対しても、フロントエンドは「制限なし」として扱う。

リクエストの各フィールドをどう扱うかは、**4分類の対応表として宣言してある**（`GenerateRequest` の全フィールドが必ずどれか1つに属することを機械検証するテスト付き。将来フィールドが増えたらそのテストが落ち、対応表の更新が強制される）。

| 分類 | 扱い |
|---|---|
| **422** | 既定値と違えば `FEATURE_UNSUPPORTED`。**判定は常に「既定と違うか」であって「有るか」ではない**——フロントエンドは毎回スキーマ全体を送るため、既定値のまま届いたフィールドは利用者が求めたものではない |
| **無視＋ログ** | 拒否すると敵対的になるもの。フロントエンドは毎回スキーマ全体を送るので、素のT2V要求にもこれらが必ず載ってくる。**2026-08-24以降ここに残っているのは1種類だけ**——蒸留版2.5にはCFG（プロンプトへの従い具合の制御）もステップ数の概念も無い、という理由の項目である（`negative_prompt`／`guidance_scale`／`num_inference_steps`／`neg_method`／`vsf_scale`）。**高速化2つ（`fused_gguf_dequant_kernel`／`block_swap_prefetch`）は同日この分類を抜けて「動作」へ移った**——LTX 2.5でも既定onで実際に効くようになったため、「無視」のまま置くと嘘になる（[`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §75） |
| **動作** | そのまま効く。`crop_output` は完成したmp4への後処理（ffmpeg）でエンジンに依存しないため、ここに入れて**黙って消さない** |
| **従属** | 上位のフィールドが既定のままなら意味を持たない従属パラメータ。上位が422側にあるので、これらが意味を持つリクエストは上位の時点で既に断られている |

**「無視」と「従属」を分けてある**のは、「無視」が「それでもジョブは走る」という約束を含むためである。従属パラメータはそもそも走るジョブに到達できないので、同じ箱に入れると嘘になる。

**この4分類の表はスキーマごとに1つある**——単発生成（`GenerateRequest`）用と連結生成（`GenerateChainRequest`）用の2つで、同じフィールド名でもスキーマが違えば答えが違うためである（`clips` や `stage2_window` は連結側にしか無く、`outpaint` は単発側にしか無い）。1つの表を両方に使い回すと「このスキーマのときだけ例外」という但し書きが要り、それは監査テストが防ごうとしているものそのものになる。

**どのフィールドがどの分類かの正本は [`../Videomni_Backend_Specification.md`](../Videomni_Backend_Specification.md) §6.10 である**（件数も含め、本書には転記しない。単発生成は同 (d)、連結生成は同 (f)）。実測記録は [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §69.9（単発）・§72.4（連結）・§74.5（スタイルLoRAとIC-LoRAの開通で両方の分類が動いたときの実測）・**§78（撮り直しと素材（末尾）の開通で、連結側の分類が 拒否3／無視5／動作23／従属3 になったときの実測）**。

### 5.7 参考: 16GB対応のための改変は生成品質に影響しない（人間向けの注釈）

**結論から言うと、LTX 2.5エンジンの16GB対応は、公式コードの計算式そのものを1つも変えていない。**変えたのは「重みをどこに置くか」と「計算をどう分割するか」だけであり、品質への影響は**GPU内部の計算順序のゆらぎ**級にとどまる。

| 改変 | 数値への影響 | 根拠 |
|---|---|---|
| Q4量子化（4bitへ圧縮した）重みの常駐＋層単位の逆量子化（元の精度へ戻す処理） | ゼロ | 逆量子化は決定的な算術で、いつ展開しても同じ値になる |
| block swap（重みをCPUとGPUの間で移動させる仕組み） | ゼロ | 破棄→再構築を3周させても出力が最終桁まで一致（§69のG2） |
| テキストエンコーダーの層オフロード（GPUへ載せる層数の調整） | ゼロ | GPU常駐層数0／8／48の3構成で隠れ状態がビット一致（§69のG3） |
| aggregate射影（まとめて行う行列積）のチャンク化（小分けにする処理） | ゆらぎ級 | 数学的には等価だが、cuBLASが出力幅ごとにカーネルを選び直すためビット単位では一致しない。刻み幅を定数として固定してあるので決定性（同じ入力なら毎回同じ出力になる性質）は保たれる |
| cudnn.deterministic固定（計算順序を毎回同じにする設定） | ゆらぎ級 | 総和の順序を固定するだけの変更で、速度コストは実測誤差以下 |
| euler_ancestralサンプラー（ノイズ除去の手順）の強制ON | 公式挙動の復元 | GGUF経由だと公式の自動判定が壊れて別世代のサンプラーへ落ちてしまうのを、明示指定で直したもの |
| VAE・アップスケーラー・sigmas・サンプラー本体 | 無改変 | 公式コードのまま |

**比較の目安**: sdpa（通常の注意機構）からsage attention（近似計算を含む高速化）への切り替えや、通常VAEからPrunaVAED（圧縮率を上げたVAE）への切り替えのほうが、本件より大きな差を生む。

**注意——これはエンジンの差ではなく「重みと設定の選び方」による差である。**同じ重みをComfyUI公式ワークフローで動かした場合との違いは3点ある。(1) ComfyUIは拡散デコーダー版のVAEを使うが、本エンジンv1はConv版のまま（§3-103で保留）。(2) ComfyUIはプロンプト拡張（小型のGemma 4でプロンプトを自動で書き足す機能）が既定ONだが、本エンジンはOFF。(3) Stage-2の入口sigma（ノイズの強さの初期値）はComfyUIが0.85・公式Pythonが0.909375で、本エンジンは公式Python準拠。Stage-1の8ステップぶんのsigmasはバイト一致している。目視で差が出た場合は、まず(1)(2)を疑うとよい。

---

### 5.8 推論モードの規約 — **`engine25` は全経路 `no_grad`。2.3 は全経路 `inference_mode`**（2026-08-30・§3-124より）

**エンジン系統ごとに、推論の囲み（PyTorch の推論モード）が違う。** これは好みの問題ではなく、**混ぜると実行時エラーになる**ので規約として書いておく。

| エンジン系統 | 本番の囲み | 該当箇所 |
|---|---|---|
| `ltx`（LTX 2.3） | **全経路 `torch.inference_mode()`** | 単発・連結・画角拡張のすべて |
| `ltx25`（LTX 2.5） | **全経路 `torch.no_grad()`** | `pipeline25.generate` / `chain25.run_chain` / `outpaint25.run_outpaint` |

**なぜ揃っている必要があるのか。** `inference_mode` の中で**新しく確保された**テンソルは「推論テンソル」の印を帯び、**`no_grad` からその中身を書き換えようとすると `RuntimeError` になる**（逆向きは合法という非対称な規則である）。**先読み block swap のページ固定バッファのように、ジョブをまたいで生き残る受け皿を共有していると、囲みが1経路だけ違うだけで「あるジョブの直後に別のジョブが落ちる」という形の不具合になる。** 実例が §3-124 で、**画角拡張だけが `inference_mode` だったために、その直後の普通の生成が落ちていた**（機構の正本は [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) **§81**）。

**機械で守ってある**: `tests/test_ltx25_outpaint.py` の `test_no_engine25_module_asks_for_torchs_inference_mode` が、**`engine25` 配下に `inference_mode` が1つも現れないこと**をCPUだけで主張する。

**上流（`ltx_core` / `ltx_pipelines`）には `inference_mode` の面が残っている。** **これらを新しく配線するときは、同じ食い違いが再発しうる**ので、囲みを必ず確認すること。

| 上流の場所 | 内容 |
|---|---|
| `ltx_core/block_streaming/block_fetcher.py:70-72` | **上流自身が同じ罠を明文化している**——「ページ固定バッファは呼び出し側の inference_mode の下で確保されるので、このスレッドからの in-place `copy_` にも inference_mode が要る」と書いて `with torch.inference_mode():` で囲っている |
| `ltx_pipelines/distilled.py:320`（ほか `a2vid_two_stage.py:308` / `dfr_pipeline.py:564` / `ic_lora.py:410` / `retake.py:332` など） | 公式 **CLI の `main`** に `@torch.inference_mode()` が付いている。**当エンジンは `main` を呼ばず、下のブロックを直接駆動しているので今は無関係**だが、CLI 経路を使う配線を足すと入ってくる |
| `ltx_core/text_encoders/gemma/encoders/base_encoder.py:118` | テキスト符号化（プロンプト側）が `with torch.inference_mode()` で囲まれている。**上流の内部で完結しているうちは無害**である |

**規約**: **`engine25` に新しい経路を足すときは `torch.no_grad()` で書く。** **LTX 2.3 の経路を移植するときは、デコレータを一緒に持ってこないこと**——これが再発の最も安い経路である。

---

## 6. 利用者から見た振る舞い（UX仕様）【オーナー裁定】

### 6.1 切り替え＝即ロード

**ドロップダウンでベースモデルを選んだ瞬間に、モデルの読み込みが始まる。** 内部的には、Settings の「Load selected models」ボタンを1回押したのと同じ処理が自動で走る。

**「選択中とロード済みが食い違っている」という状態を作らない**のがこの仕様の目的である。ドロップダウンが「LTX 2.5」を指しているのに動いているのは LTX 2.3、という状況は、利用者にとって説明のしようがない。

### 6.2 4つのガード

| # | 状況 | 振る舞い |
|---|---|---|
| (1) | **生成ジョブの実行中** | 切り替えできない。既存の 409 `JOB_BUSY` をそのまま使う |
| (2) | **未導入のベースモデルを選んだ** | ドロップダウンの項目自体に導入済み／未導入を表示する。未導入を選んだ場合は導入を促す案内を出し、切り替えを中止して元の選択へ戻す。**案内の文面は導入バッチを名指しする**（「まだ導入されていません。バックエンドのフォルダにある `install-LTX25.bat` をダブルクリックして重みファイルを取得したあと、この画面を開き直してから選び直してください」）。**バッチ名は規約 `install-<ID>.bat` で、`ID` は記述子の `id`（＝ `models/<ID>/` のフォルダ名）である。** 組み立てているのは `webui/src/shell/useBaseModels.ts` の `baseModelInstaller()` で、記述子の `id` だけから導くので、ベースモデルが増えても文面側の変更は要らない。**「開き直してから」を必ず書く**——ベースモデル一覧はこの画面を開いたときにしか取り直さないため、開いたままではいつまでも未導入のままに見える |
| (3) | **ロード中の二重切替** | 409 `PIPELINE_LOADING` で断る（下記） |
| (4) | **失敗時のUI復帰** | 切り替えが409／503で失敗したとき、フロントエンドのドロップダウンは選択中の表示を**元の（切り替え前の）ベースモデルへ必ず戻す**。ただし**黙って戻すのではなく、理由を必ず伝える**——選んだ表示のまま止まると §6.1 が禁じる食い違い状態そのものになり、無言で戻ると利用者は自分の操作が届いたのか判断できない |

**ガード(3) の実装は2点が要点である。**

**(i) 判定はAPI層ではなく `PipelineManager` のロックの中で行う。** API層で `pm.state` を読んでから `pm.load()` を呼ぶまでの間に別のリクエストが割り込めば、2本とも「読み込み中ではない」と判断して通ってしまう。**状態を見る場所と状態を変える場所は同じロックの中でなければならない**ので、判定は `state = LOADING` を代入するのと同じ `with self._lock:` の中に置いてある。

**(ii) `POST /pipeline/unload` にはこのガードを置かない。** 一見「読み込み中なら unload も断るべき」に思えるが逆である。**`unload` は、読み込みが異常終了して状態が `loading` のまま張り付いたときの唯一の復帰路**だからである。ここに409を置くと、張り付いた状態から抜け出す手段がサーバー再起動しか無くなる。**「二重ロードを防ぐ」ことと「詰まりを解消する」ことは別の目的であり、後者を前者の巻き添えにしない。**

**自動ロード経由のときは 409 がそのまま出ない。** `auto_load_on_generate` が有効な状態で読み込み中に生成を投げると、409 は生成ジョブの中で起きるため `generation_failed`（ジョブの失敗）に包まれて返る。**利用者が読むのは包まれた側のメッセージ**なので、そこに「読み込み中である」と分かる文言が残ることをテストで固定してある。

**ダウンロードはサーバーの責務にしない 【オーナー裁定】。** サーバーが数十GBのダウンロードを開始する設計は、進捗表示・中断・再開・失敗時の後始末をすべてサーバー側に抱え込むことになる。導入はバッチファイル（`setup.bat` とその仲間）の役目である。

**導入口は「本体＋ベースモデル別」の2階建てである 【オーナー裁定】。** `setup.bat` は **Nz-Videomni 本体のインストーラ**であり、最初に試していただくAIとして **LTX 2.3 だけを一緒に導入する**（お試し用の1本という位置づけである）。**別のベースモデルは、そのモデル専用の導入バッチで足す**——ダブルクリックすると、ダウンロード → 配置 → ドロップダウンで選べる状態、までが一続きで走るものを想定している。

**最初の1本が `install-LTX25.bat` である（2026-08-30 実装）。** ダブルクリックすると `scripts/install_model.ps1 -BaseModel LTX25`（日本語の入口）を経て、既存のインストーラ本体 `scripts/install_ltx.ps1` を `-BaseModel LTX25 -SkipVenv -SkipMigrate` で呼ぶ。実装の記録と実測は[`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §82（**2026-08-30 に開発機ゲートA0〜A8・サブマシンゲートG0〜G7とも全合格で完結**）と[`PENDING_TASKS_CLOSED.md`](PENDING_TASKS_CLOSED.md) §3-111。

> **「どの記述子をどのバッチが担当するか」のスイッチは、`install_ltx.ps1` の `-BaseModel` である。** 起票時の懸案は、`install_ltx.ps1` が `scripts/manifests/*.json` を一括で読むため、`20-ltx25.json` の `downloads[]` を埋めた瞬間に `setup.bat` が LTX 2.5 まで取りにいってしまう（本体を入れるだけで 60GB 超のダウンロードになる）ことだった。**解いた形は「既定値を、`setup.bat` が同梱する集合そのものにする」である**——`-BaseModel` は記述子の `id` を並べる配列で、既定値は LTX 2.3 と共用前処理器。引数なしで呼ばれる `setup.bat` の経路は既定値の担当分しか見ないので、**`setup.ps1` を1行も触らずに「本体インストーラの取得対象は変わらない」が構造的に保証される。**
>
> **絞るのはダウンロード段と検証テーブルだけである。** 記述子の読み込みと妥当性検証、旧レイアウトからの移行の集約は、どのバッチから呼ばれても常に全件を対象にする——記述子は重みの有無にかかわらず読まれる必要があり（§4.3）、移行は「このディスク全体をどう直すか」の話だからである。
>
> **`models/INSTALLED_PATHS.txt` も `-BaseModel` では絞らない【オーナー裁定 2026-08-30】。** この一覧は「いまこのディスクに何があるか」を書いたものなので、最後に走ったバッチの担当範囲で縮んではならない。代わりに**実在するファイルの行だけ**を載せる。だから `install-LTX25.bat` のあとも LTX 2.3 の行は残り、まだ誰も落としていないベースモデルは1行も足さない。
>
> **3本目以降のベースモデルは、バッチ1本と記述子1本で足せる。** `install_ltx.ps1` の既定値に手を入れるのは、**本体インストーラの同梱物そのものが増えるとき**（たとえば新しい共用記述子を `setup.bat` にも配らせるとき）だけである。

### 6.3 切り替えたとき、Settings のモデル選択欄に何が入るか

| 場面 | 初期値 |
|---|---|
| そのベースモデルを**初めて**選んだとき | 記述ファイルの `default_selection` |
| **2回目以降** | `state.json` が覚えている「そのベースモデルで最後に使った組み合わせ」 |

「最後に使った組み合わせ」は、**読み込みが成功したときに更新する**（失敗した組み合わせを覚えて次回も同じ失敗を繰り返す事態を避けるため）。

### 6.4 「デフォルトに戻す」ボタン（未実装）

Settings のモデル選択欄に、既定の組み合わせへ戻すボタンを置く。押したときの処理は、①`default_selection` が指すファイルが実際に揃っているかを確かめる、②揃っていれば適用する、③欠けていれば導入案内を出す（**サーバーはダウンロードしない**）の順である。**実装するときの案内文は §6.2 のガード(2) と同じく、`baseModelInstaller()` が組み立てるバッチ名を名指しする形にする。** **このボタン自体はまだ実装していない。**

### 6.5 ヘッダーの状態バッジ

ヘッダーの状態バッジは7種類ある——`checking`（確認中）/ `bridge-unavailable`（ブリッジ未接続）/ `offline`（サーバー停止）/ `online`（稼働中）/ `busy`（生成中）/ `loading-models`（モデル読み込み中）/ `error`（エラー）（`webui/src/modes/single/StatusHeader.tsx`）。

**`loading-models` は §5.5(b) の `state` フィールドとセットで意味を持つ。** これが無いと、モデルの読み込み中もヘッダーは `online`（稼働中）のまま数分間留まる——`GET /status` へのフェッチ自体は成功し続けるためで、利用者からは「切り替えが終わって使える状態になった」と読めてしまい、生成を試みて失敗する。バッジだけを足しても判定材料が無く、フィールドだけを足しても利用者には見えない。

---

## 7. スコープ外 — エンジン別のUIパラメータ制約 【オーナー裁定】

**エンジンごとに違うパラメータ制約（フレーム数の刻み・解像度の倍数など）をUIが切り替える機能は、まだ作らない。**

現在の2系統はどちらも LTX 系なので、フレーム数の `8n+1` 規約（LTXの映像VAEが時間方向を8分の1に圧縮し、因果的な先頭1フレームが加わることに由来する）を共有しており、必要になっていない。

**この対応が「定数を1つ差し替えれば済む」話ではないことは記録しておく。** 規約の出どころは `chain_math.py` の定数

```python
VIDEO_TIME_FACTOR = 8            # 映像VAEの時間圧縮率
AUDIO_LATENTS_PER_SEC = 25.0     # 16000 / 160 / 4
```

だが、実装は定数を共有しているのではなく**規約だけが伝播し、独立したリテラルとして複数箇所に散らばっている**。`api/models.py` だけでも `8` という数値が `chain_math` を参照せず5箇所（`:342`・`:377`・`:515`・`:582`・`:587`）に直書きされており、フロントエンド側にも独立実装がある（例: `webui/src/bridge/mockBridge.ts:1014` の `(numFrames - 1) % 8 !== 0`）。**エンジン別化に着手するときは、これらをすべて洗い出す必要がある。**

### 作業の順序 【オーナー裁定】

```text
第1段階  推論エンジン切り替えの土台づくり         ← 完了（§8.3）
            ↓
第2段階  LTX 2.5 の実装と、切り替えの成功         ← 基本生成＋クリップ連結＋V2V継続＋A2V＋スタイルLoRA・IC-LoRA（長尺含む）まで完了（§8.5）
            ↓
第3段階  UI調整（エンジン別のパラメータ制約など）  ← 未着手。別系統（Wan 等）を入れる段で必須になる
```

---

## 8. 段階分割と検証の戦略

### 8.1 抽象の正しさは2例目で実証する 【オーナー裁定】

**「将来いろいろなモデルを載せられる作り」を、1例しか無い状態で完成させようとしない。** 抽象化が正しかったかどうかは、2例目を載せてみるまで分からない。1例しか無いうちに凝った作りにすると、2例目が来たときに「想定と違った」という理由で作り直すことになる——**過剰抽象化は、抽象化しないのと同じくらい手戻りを生む。**

この原則は実際に効いた。2例目（LTX 2.5）を載せたとき、当初「LTX 2.5 は ltx 系統のまま載る」と見込んでいた点が外れたが、**先回りして作り込んでいなかったので、訂正は表の書き換えで済んだ**。ベースモデル軸とエンジン系統軸を分けた設計そのものは正しく、外れたのは「LTX 2.5 がどちらの軸に載るか」という当てはめだけだった。**同じ理由で、3例目が来るまで判断を保留している事項がいくつかある**——汎用 `EngineRunner` への分割（§5.2）、mock バックエンドの共通化（§9.1）、カテゴリ集合の可変化（§4.1）である。

### 8.2 土台整備と2例目は抱き合わせで行う

土台だけを先に作って寝かせない。**土台と2例目を1つのテーマとして進める**ことで、抽象が正しかったかどうかがその場で判明する。実際、第1段階（土台）と第2段階（LTX 2.5）は連続したテーマとして実施し、2例目でベースモデル軸とエンジン系統軸の両方を同時に実証した。

### 8.3 第1段階の実績

| 段 | コミット | 内容 | 通過したゲート |
|---|---|---|---|
| P0 | `29ec4c6` | `metadata.json` へ使用モデル情報を記録 | 全テスト緑＋新規2本 |
| P1 | `e32c824` | 記述子 schema 2 の定義とインストーラ検証の1/2両受理化 | `install_ltx.ps1 -DryRun` 完走・`INSTALLED_PATHS.txt` がバイト一致 |
| P2 | `13c3437` | GGUF KV パーサ `services/gguf_kv.py` の新設（**純加算**。既存コードを1行も触らない） | 新規テスト緑＋実ファイル4本でKV確認 |
| P3a | `302a694` | レジストリの記述子駆動・多ルート化、`GET /models` の加算3層化 | `categories` ブロックのバイト一致回帰テスト |
| P4 | `5df32d8` | `services/ltx_runner.py` を `services/engines/ltx/adapter.py` へ引っ越し（**移動のみ**） | `git diff --stat` に tests/ の変更ゼロ |
| P3b | `e55f8f5` | config からの既定パス撤去、precheck の記述子対応、KV判別の配線 | ゴールデンのリテラル無変更を目視確認 |
| P5 | `550bd41` | `state.json`（`services/runtime_state.py`）の新設 | 新規テスト緑・リポジトリ直下に非生成 |
| P6 | `669604d` | API のベースモデル軸、ロード中409、`GET /status` の `state` | 新規6観点のテスト |
| — | `61cf8e7` | 起動時に復元した選択名を実パスへ解決する修正 | 全テスト緑 |
| P7 | `2134509` | フロントエンドのヘッダードロップダウン実配線 | typecheck / vitest / lint / build:single 全緑 |
| P8 | — | 実機ゲート（G9〜G12）と文書更新 | [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §68 |

バックエンド 1804 本・フロントエンド 2486 本のテストが全て通り、実機で LTX 2.3 の生成が従来どおり成功し、`_build_load_payload` のゴールデンスナップショットは無変更のままである。

### 8.4 コミット分割とゲートの作法

両段階で共通の進め方であり、次に系統を足すときも踏襲する。

- **分割の基準は「何をしたコミットか一言で言えること」。** 特に**「引っ越しだけのコミット」と「振る舞いを変えるコミット」を混ぜない**——移動と変更が同じ差分に入ると、ゴールデンスナップショットが落ちたときに原因が引っ越しなのか変更なのか判別できなくなる。
- **フェーズごとに1つずつゲートを置き、前の段が合格しない限り次へ進まない。** ゲートは2種類ある——**機械ゲート**（エージェントが実行できるもの。自己診断・pytest・バイト一致）と、**実機ゲート**（実GPUで実際に動画を作るもの）である。
- **実機ゲートの実施中はコードを1行も変えない。** 作業ツリーが直前のコミットとバイト同一であることが、測った対象がそのコミットだと言える根拠になる。

### 8.5 第2段階の実績

| 段 | コミット | 内容 | 通過したゲート |
|---|---|---|---|
| P0 | （変換ツール側 `905f348` ほか） | transformer GGUF へ `gemma_source_checkpoint` KV を足して再出力。前提4点の実測確認 | G0 |
| P1 | `62d67b0` `766c0fb` | 専用仮想環境 `.venv-engine-ltx25` とワーカー骨格。インストーラの `Ensure-EngineVenv` 化（2系統対応） | G1 |
| P2a | `8006dd3` | 公式パッケージのシンボル集約モジュール（`ltxcore_compat`）。engine25 から公式を直接importしない規約 | — |
| P2b | `2e480a9` | transformer GGUF のロード経路（層ごとの逆量子化＋ブロックスワップ） | G2 |
| P2c | `efe361f` | テキストエンコーダ（Gemma 4）の GGUF ロード経路と層オフロード | G3 |
| P2d | `1f32786` | 二段生成パイプラインの組み立てとワーカー結線 | G4 |
| P3a | `2602215` | LTX アダプタに系統ごとの差し替え点を導入（**挙動は完全不変**） | G5a |
| P3b | `ca21f74` | LTX 2.5 アダプタ（`LTX25Runner` / `_RealBackend25`）の新設 | G5 |
| P3c | `18c47b0` | 系統のディスパッチと Runner 差し替え（`_point_runner_at`） | G5 |
| P4 | `cda3006` `0bd532c` | 記述ファイルの実ファイル名での完成と、重み置き場・案内ファイルの用意 | G6 |
| P5 | `0df3346` `da4b926` `7c29ca3` | 機能スコープの422（§5.6）、フロントエンドのグレーアウト、`.aux2` の再配布 | G7 |
| P6 | — | 実機ゲート H1〜H9（実GPU） | 全合格 |

**実機ゲートは16GBの回避策を1段も使わずに全項目合格した**（LTX 2.5 側の最悪フェーズのVRAMピークは 7.00 GiB）。バックエンド 1932 本・フロントエンド 2515 本のテストが全て通り、**LTX 2.3 のロードペイロードのゴールデンスナップショットは無変更**、同じシードでの生成もビット単位で従来どおりである。実測はすべて [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §69。

#### 続き: クリップ連結（Chained）本体（2026-08-23）

第2段階が v1＝基本生成でクローズしたあと、**台帳 [`PENDING_TASKS.md`](PENDING_TASKS.md) §3-102 の第1段としてクリップ連結の本体を移植した**。「LTX 2.3 でできたことを LTX 2.5 でも一通り」という同等化が目的で、窓の大きさもクロスフェードの手順も 2.3 の値のまま持ち込んでいる。

| 段 | コミット | 内容 | 通過したゲート |
|---|---|---|---|
| C1 | `b646524` `c0b539d` | `ltxcore_compat` への公式部品追加と進捗・フェーズ命名の口／`engine25/chain25.py`（連結生成の本体・1,050行）。幾何計算は `chain_math.py` を共有import | G1（(a)〜(g)） |
| C2 | `910d395` | ワーカーの操作 `generate_chain` と `--selftest-chain` | G2 |
| C3 | `80798a9` | アプリ側（`CHAIN_*` の4分類表・フィールド単位の422・`_RealBackend25.generate_chain`） | G3 |
| C4 | `de5442c` `ea6cff8` `33d28df` | 画面のChainedタブ開放と4パネルの個別グレーアウト／MCPの説明文／素材（冒頭）欄は動画の口だけを閉じる追補 | G4 |
| G5 | — | 実機ゲート（実GPU・固定ベンチマーク B1〜B4） | 全合格 |

**設計上の要点は「継ぎ目の帯を凍結する仕掛けを公式部品で作った」ことである。** LTX 2.5 の公式パッケージには 2.3 が手作業で行っていたマスク書き換えと同じことをする部品があり、その結果**凍結の計算は 2.3 とビット単位で同一**になった。音声側だけは公式に対応部品が無いので、同じ2本の式で自作している。

**オーナーの実機目視も 2026-08-23 に合格している**——検証用の継ぎ目画像6件に加え、オーナー自身が AviUtl2 の操作パネルの Chained タブから生成した複数本について「繋ぎ目に違和感なし・プロンプトに追従」との評価である（[`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §72.10）。

**実機ゲートも16GBの回避策を1段も使わずに全項目合格した**（1920×1088・2クリップ×169フレームで確保ピーク 13.2 GiB＝溢れ判定は「快適」）。継ぎ目は実機33か所すべてで判定基準に届かず、同じシードでの再現もmp4のSHA-256一致で確認済み。バックエンド 1,986 本・フロントエンド 2,554 本のテストが全て通り、**LTX 2.3 のゴールデンスナップショットと同一シードの出力は無変更**である。実測はすべて [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §72、API 契約への反映は [`../Videomni_Backend_Specification.md`](../Videomni_Backend_Specification.md) §6.10(f)。

#### 続き: V2V 継続と A2V（長尺 A2V を含む）（2026-08-23）

連結生成の本体に続く**§3-102 の第2段**である。オーナー裁定により、必須級の残り（V2V 継続・A2V）と準必須の長尺 A2V は**部品を共有するので1つの計画**として実装した——素材（動画・音声）を潜在へ変換する経路と、それを先頭に置いて凍結する仕掛けが同じものだからである。

| 段 | コミット | 内容 | 通過したゲート |
|---|---|---|---|
| C1 | `adb173b` `300c8ae` | `ltxcore_compat` へ素材取り込みの公式部品を追加し、音声エンコーダ（`AudioConditioner`）をパイプラインに常備／`engine25/chain25.py` に V2V と A2V を実装 | G1（(a)〜(g)） |
| C2 | `c8b64f8` | ワーカーの `source` / `audio_source` 受け口と、素材ファイルの実在チェック。`--selftest-chain` の `--source` / `--audio-source` | G2 |
| C3 | `fc3e5cc` | アプリ側（`source_video` / `source_audio` を拒否表から動作表へ・4分類を 9／7／13／5 へ・ペイロードの `source` / `audio_source` ブロック） | G3 |
| C4 | `05bdb9a` `74baeea` | 画面の V2V・A2V・バッチ A2V の開放（Single タブの A2V を含む）／MCP の説明文 | G4 |
| G5 | — | 実機ゲート（実GPU・固定ベンチマーク B5〜B8） | 全合格（記録は [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §73） |

**オーナーの実機確認も 2026-08-24 に合格している**——AviUtl2 の操作パネルから V2V と A2V を実際に生成し、機能していることを確認したという報告である。**長尺 A2V はオーナー裁定で合格扱い**（裁定の全文と理由は [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §73.10(2)）。記録は同 §73.10。

**設計上の要点は「凍結の仕掛けを2種類使い分けた」ことである。** V2V の頭は**連結生成と同じ帯アイテム**で凍結する（前のクリップの尻尾を受け取るのと同じ仕組みを、ファイルから読んだ潜在で行うだけ）。一方 A2V は**音声モダリティ全体**を公式の `ModalitySpec(frozen=True, noise_scale=0.0)` で凍結する——公式の A2V パイプラインがそうしており、「絵はアップロードされた音声そのものに従う」という A2V の約束はタイムライン全体についての宣言だからである。**映像側は通常どおりクロスフェードの帯を保つ**（ここを一緒に固めてしまうのが、LTX 2.3 が長尺 A2V で踏んだ不具合そのものである）。差分の詳細は [`CHAIN_STAGE2_RESEARCH_NOTES.md`](CHAIN_STAGE2_RESEARCH_NOTES.md) 12節。

**LTX 2.3 との差を1つ意図的に作った**——モノラル音声をステレオへ複製する処理を、V2V と A2V の**両方**で行うようにした。2.3 は A2V の経路でだけ複製しており、V2V ではモノラルのまま音声エンコーダへ渡していた。下流はこの2つを区別していないので、振る舞いが2種類あったのは判断ではなくコードの育ち方の偶然である。

**この段で LTX 2.5 が断るモードは Retake と End source の2つだけになった**（この時点の `unsupported_features` は10件。**その後、§3-102 の第3段〔スタイルLoRA・IC-LoRA〕で8件へ、高速化第2弾〔`keep_resident`〕で7件へ、高速化第3弾〔SageAttention〕で6件へ、2026-08-26 の撮り直し・素材（末尾）で4件へ減っている**。現在値は§5.6）。API 契約への反映は [`../Videomni_Backend_Specification.md`](../Videomni_Backend_Specification.md) §6.10(f)。

#### 続き: スタイルLoRA と IC-LoRA（参照動画による制御・長尺を含む）（2026-08-24）

**§3-102 の第3段であり、必須級として残っていた最後の2件である。** スタイルLoRA（絵柄を寄せる追加学習ファイル）と IC-LoRA（参照動画から輪郭線・骨格・深度などの制御信号を作って生成を導く仕組み）は、LoRAを取り付ける経路と参照動画を条件として足す経路が同じ部品の上に乗るため、**1つの計画**として実装した。クリップごとに参照動画の担当区間を切り出す長尺IC-LoRAも同じ段に含めている。

| 段 | コミット | 内容 | 通過したゲート |
|---|---|---|---|
| C0 | `825c3f0` | LTX 2.5 用の仮想環境へ前処理の依存を2行追加（`opencv-python-headless` / `torchvision`）。**インストーラのコードは無変更** | G0 |
| C1 | `6cdf259` | `engine25/gguf_transformer.py` へのLoRA取り付け／取り外し、参照条件付けの新モジュール `engine25/reference25.py`、`engine25/chain25.py` への配線 | G1 |
| C2 | `9bdc20e` | ワーカーの `loras` / `reference_video` 受け口と前処理の呼び出し、セルフテストの引数追加 | G2 |
| C3 | `4110f33` | アプリ側（`loras` / `reference_video_id` と2つの強度を拒否表から動作表へ・単発 6／7／12／3・連結 7／7／17／3） | G3 |
| C4 | `7fab22d` `41b5bb4` | 画面の参照動画パネルの解禁と `.aux2` の再配布／MCP の説明文 | G4 |
| G5 | — | 実機ゲート（実GPU・固定ベンチマーク B9〜B13） | 全合格（記録は [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §74） |

**設計上の要点は4つある。**

1. **LoRAの取り付けは「付ける／外す」を必ず対で書く。** 取り付け位置は 2.3 と同じくモデルを組み立てた直後・ブロックをGPUへ配る直前で、**LoRAが無いときは明示的に外す**形にしてある。連結生成は1つのジョブでモデルを何度も組み直すため、「付ける」だけでは前のクリップのLoRAが残りうる。**毎回どちらかを必ず通るので、その芽が構造的に消える。** LoRAの状態はパイプラインの段（`Ltx25DiffusionStage`）が持ち、ジョブの開始時に必ずクリアされる。
2. **参照動画の条件付けは新しいモジュール（`engine25/reference25.py`）に閉じ込め、公式関数を一時的に差し替える方式で足す。** LTX 2.5 の公式パイプラインには参照を足す差し込み口が無いためで、差し替えと元通しは必ず対（例外が出ても戻す）にしてある。差し替え先の関数の在り処・引数・内部の式は照合検査（`verify()`）で固定してあり、公式パッケージ側が変わったら気づける。
3. **連結生成では、参照条件を全セグメントぶん最初に作り切る。** 2.5 の映像エンコーダは呼び出しの中でしか生きないので、2.3 のような遅延方式が取れない。**エンコーダを1回組み立てるその場で全クリップぶんを作り、以後は小さな潜在表現だけを持ち回る**（512×384・49フレームで1セグメントあたり約70KB）。
4. **前処理（canny／dwpose／depth）は LTX 2.3 と同じコードを共有する。** `engine/preprocess/` は公式パッケージに依存しないため、2.5 用の仮想環境に依存を2行足すだけで動く。**エンジンごとに前処理を二重に持たない**という判断で、その正しさは実機で裏づけが取れた——同じ素材から 2.3 と 2.5 が書いた制御動画の SHA-256 が完全に一致している（[`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §74.7(f)）。

**公式との差を2点、意図して作っている。** ①**LoRAを Stage-1・Stage-2 の両方へ適用する**（公式の IC-LoRA パイプラインは Stage-1 のみ。LTX 2.3 に揃えた）。②**参照アイテムのノイズ側の扱いが 2.3 と実装レベルで違う**（2.5 はゼロで埋め、2.3 は「ここは参照だ」という印を立てる）。②は公式部品の仕様なのでそのまま使い、照合検査で固定してある。

**既定の強度は動かしていない。** 前提実験では LTX 2.5 でLoRAがやや弱く効くことが分かっている（ぼけ取りの復元率が強度1.0で46%・1.3で136%）が、同等化のスコープでは既定を変えない。強めに効かせたい利用者は画面から強度を指定できる（[`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §74.9(1)）。

**この段で `unsupported_features` は8件になった**（`retake` / `end_source` / `two_stage_hq` / `outpaint` / `nag` / `prune_vaed` / `sage_attention` / `keep_resident`）。API 契約への反映は [`../Videomni_Backend_Specification.md`](../Videomni_Backend_Specification.md) §6.10(d)・(f)。**オーナーの目視は 2026-08-25 に合格した**——AviUtl2 の操作パネルからスタイルLoRAと IC-LoRA の両方を試し、いずれも効果があることを確認しての判定である（[`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §74.12）。

#### 続き: 高速化第1弾＝fusedカーネルと先読み block swap（2026-08-24）

**必須級（同等化）が出尽くしたあと、オーナー裁定の段階分けに従って高速化技術の前倒しに入った、その第1弾である。** 対象は高速化5つのうち**出力がビット単位で変わらない2つ**——**fused GGUF逆量子化カーネル**（圧縮された重みを計算に使える形へ展開する処理を、量子化形式ごとに1つのGPU処理へまとめる）と**先読み block swap**（CPU と GPU のあいだの重みの受け渡しを計算の裏で先回りさせる）——で、**どちらも既定 on** にした。残る3つ（SageAttention・`keep_resident`・PrunaVAED）は出力または品質が変わりうるので別の弾として扱う。

| 段 | コミット | 内容 | 通過したゲート |
|---|---|---|---|
| C0 | `d4311ca` | LTX 2.5 用の仮想環境へ `triton-windows` を導入。逆量子化の初回検算のキーを「型」から「**型＋ポインタの整列**」へ。自己診断を任意の GGUF へ向けられるように | G0 |
| C1 | `ae21067` | `engine25` 側の fusedカーネルの受け口（有効化・後始末とエコー2キー） | G1 |
| C2 | `0ed330b` | `engine25` 側の先読み block swap の受け口（ビルドごとの張り直し・量子化テンソルの型の持ち回り・自己診断C15） | G2 |
| C3 | `22aebf7` | アプリ側の配線（4分類の対応表を「無視＋ログ」から「動作」へ。**既定で両方 on**） | G3 |
| C2b | `6027180` | 先読みの受け皿（アリーナ）の**リング化**と `expandable_segments` の整理・自己診断C16 | C2b再ゲート |
| G5 | — | 実機ゲート（実GPU・45ジョブ・固定ベンチマークB系17本） | 全合格（記録は [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §75） |

**設計上の要点は4つある。**

1. **fusedカーネルは、共有部品の上に「配線」しただけである。** Triton を使う逆量子化の分岐は、もともと両エンジンが共有する `engine/gguf/` の中に存在していた。したがって `engine25` 側の仕事は「ジョブの頭で有効にし、終わりで必ず戻す」という作法の写し取りだけで、**有効化は `try` の外・後始末は `finally`**、しかも2つの後始末を別々に囲って片方の失敗がもう片方を有効のまま残さないようにしてある。**エンジンごとにカーネルを二重に持たない**という判断は、前処理（canny 等）を共有した第3段と同じ形である。
2. **先読みは「1ジョブでモデルを何度も組み直す」ことへの対応が本体である。** 連結生成はクリップ数×タイル数だけ transformer を組み直すので、ビルドのたびに張り直す必要がある。**取り外しはビルドの1行目に固定**（組み立て処理が内蔵する防御的な取り外しに任せると、重みを読み込んだ後に前ビルドの CPU 正本が書き戻され、今回のビルドを静かに上書きする）、**forward の張り替えは今回の生きたモデルに対して明示的に剥がす**（同じ器が使い回されるため、剥がさないと1プロセスに1回しか効かない）。エコーの畳み込み方は LTX 2.3 と同型（全ビルドで効いたときだけ `"on"`）。
3. **アリーナリングは、`hold_arenas` による意図的な系統別の例外である。** Windows の PyTorch は `expandable_segments` を受け付けない（**一度も効いていなかった**）ため従来型のアロケータで動き、ブロックごとの確保・返却が予約プールを一方向に押し広げていた。固定本数の受け皿を使い回す「アリーナリング」でこれを解消したが、**リングが得になるかどうかはエンジンごとに違う**——LTX 2.5 は VRAM の天井が denoise の**中**にあるので得しかない（B4 で 15,292 → 14,840MB）のに対し、LTX 2.3 は天井が denoise の**外**（デコード段）にあるためリングぶんがそのまま上積みされる（Chain A で 13,264 → 15,550MB）。**「どちらのエンジンにも同じ挙動を」の例外を作るときは、違いを隠さずフラグの名前で表に出す**という形にしてある（`hold_arenas`。LTX 2.5 だけが on）。顛末は [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §75.7。
4. **エンジンの範囲が広がっても、`unsupported_features` は動いていない。** この2つは 422 で断っていた機能ではなく「無視＋ログ」の側にあった項目なので、動いたのは4分類の対応表だけである（§5.6 の補足）。**フロントエンドのコードは1行も触っていない。**

**実測と API 契約への反映の正本は、[`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §75 と [`../Videomni_Backend_Specification.md`](../Videomni_Backend_Specification.md) §6.10(d)・(f) である**（交互対比較での短縮幅・固定ベンチマークB系17本の SHA-256 一致・予約VRAMの増分・4分類の件数。**この段の数値をここに写さない**——同じ数値が文書ごとに食い違う事故を避けるためである）。

#### 続き: 高速化第2弾＝モデル骨格の常駐（`keep_resident`）のテキストエンコーダ側（2026-08-25）

**高速化5つのうち、残っていた3つ（SageAttention・`keep_resident`・PrunaVAED）の先頭に着手した第2弾である。** LTX 2.5 の変換器（映像を作る本体）側の重みは以前からメインメモリに残る設計（ロードペイロードの `cache_weights`。既定 on）だったが、**Gemma 4 テキストエンコーダの重みだけは毎ジョブ GGUF から読み直していた**——そこが唯一の未回収分だった。**既定は off のオプトイン**で、他の2つの高速化とは向きが逆である（メインメモリと引き換えの機能のため）。

| 段 | コミット | 内容 | 通過したゲート |
|---|---|---|---|
| C0 | `f72c8e7` | 計測下地（機能変更ゼロ）。`10a_te_build` / `10b_ep_build` / `40_job_end` の3フェーズ新設と、chain の `seconds_total` からサブフェーズを除外する規約 | G0 |
| C1 | `6a10960` | `engine25` 側の開通（`_swap_keep_resident`・`set_acceleration_job` への引数追加・`close()` でのハンドル破棄・`verify()` の挙動ピン） | G1 |
| C2 | `69b58ec` | アプリ側の開通（**拒否表から受理表へ**・ペイロードの加算・エコーの中継。`unsupported_features` は 8件 → 7件） | G3 |
| G5 | — | 実機ゲート（実GPU・GPUジョブ11本） | 全合格（記録は [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §76） |

**設計上の要点は4つある。**

1. **同じ名前でも、LTX 2.3 とは別の実装である。** 契約（キー欠落＝off＝明示的な解放指示・ジョブ末にリセットしない・エコー3値・`GET /status` にフラグを載せない・RAM の自動降格を作らない）は 2.3 から逐語で写したが、**コードは1行も共有していない**——2.3 が常駐させるのは全サブモデルの骨格（約20GB）、**2.5 が抱えるのは Gemma 4 テキストエンコーダの state dict ただ1つ（実測 7.68GiB）**である。**この区別は仕様書・README・MCP の説明文・コードの docstring の4箇所に1文ずつ入れてある**（同名別実装の誤読が最大のリスクだったため）。
2. **on は「フラグの書き換え」だけで済むが、off は `pop()` が要る。** 公式の登録簿（`ModelRegistry`）は `add()` のときしか `_cache_weights` を読まず、**`get()` はこのフラグを見ない**。したがってフラグを戻すだけでは、一度入った重みが永遠に配られ続ける。**「解放は次に off を要求したジョブの頭で起きる」という非対称**（ジョブ末では戻さない＝残ることが機能そのもの）も、2.3 の規律をそのまま写したものである。
3. **私的属性への依存は、署名検査では守れない。** 公式パッケージが `_cache_weights` を改名しても関数の形は変わらないので、検査は通ったまま**静かに 7.68GiB を取りこぼす**。そこで `verify()` に**使い捨ての登録簿で add / get / pop を実際に往復させる挙動ピン**を置いた（偽のホイール2種で `CompatError` になることを実証済み）。**同じ形の依存を足すときは、同じ手当てをすること。**
4. **RAM の自動降格は作らない、という判断は据え置いた。** 「常駐に十分なメモリがあるかどうかは利用者の選択であって、サーバーが判定すべき環境の能力ではない」（[`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §48.1-10）。同じ理由で `GET /status` にも能力フラグを載せていない（同 §48.7）。台帳 [`PENDING_TASKS.md`](PENDING_TASKS.md) §3-51 は先送りのまま生きている。

**実測と API 契約への反映の正本は、[`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §76 と [`../Videomni_Backend_Specification.md`](../Videomni_Backend_Specification.md) §6.10(d)・(f) である**（2本目以降の短縮幅・テキストエンコーダ構築の短縮・常駐の増分・**VRAM が on/off で動かないこと**〔RAM と引き換えの機能なので、動かないことが期待どおりの結果である〕・SHA-256 の一致・4分類の件数）。

> **副産物として、LTX 2.5 のメインメモリの見え方が変わった。** 計測の標本点を増やしたところ、**`keep_resident` が off のときでも2本目以降のジョブは 25.9GiB に達している**ことが分かった（変換器の常駐と、**組み立て途中のテキストエンコーダ 約7.6GiB**〔`10a_te_build` の 25.89GiB と直後の 18.33GiB の実測差分〕が同時に生きる瞬間を初めて捉えたもので、従来の報告値 18.3GiB は「山を越えたあとの谷」を読んでいた。**連結生成の既定ジョブではこの山が 26.33GiB まで上がる**）。**LTX 2.5 の RAM を語るときは、この前提から始めること。** 詳細は [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §76.2(3)。

**次の一手の材料も1つ出た**——埋め込み処理器（embeddings processor）の構築が毎ジョブ 3.3〜4.7秒かかっており、**費用はテキストエンコーダのおよそ半分**である（テキストエンコーダの構築が 6〜8秒。[`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §76.8(8) の訂正に合わせた）。しかも `keep_resident` を on にしてもこの区間は縮まない（別の登録簿を使っているため）。台帳 [`PENDING_TASKS.md`](PENDING_TASKS.md) §3-114 へ起票した。

#### 続き: 高速化第3弾＝SageAttention（`attention_backend`）（2026-08-25）

**高速化5つのうち残っていた2つのうち、SageAttention を開通させた第3弾である。** SageAttention は、注意機構（映像の各部分が互いをどれだけ参照するかを計算する部分）の掛け算を量子化して速くする外部カーネルで、**掛け算の精度を落とすぶん、出来上がる絵の細部が変わる**。**ここが第1弾・第2弾と決定的に違う**——固定ベンチマークの SHA-256 一致では合否を出せないので、判定の作り方そのものを変える必要があった。**既定は `"sdpa"` のまま据え置いてある**（反転の是非は台帳 [`PENDING_TASKS.md`](PENDING_TASKS.md) §4-22 が LTX 2.3・LTX 2.5 をまとめて扱う）。

| 段 | コミット | 内容 | 通過したゲート |
|---|---|---|---|
| C0 | `1fb4fb8` | LTX 2.5 用の仮想環境へ `sageattention` 2.2.0 を導入（インストーラの直リンク wheel・freeze・注記）と、品質計測の下地（1フレームごとの SSIM〔構造類似度。2 つの映像がどれだけ同じ構造をしているかを 0〜1 で表す指標で、1 に近いほど同じ〕計測の新設） | G0 |
| C1 | `3235f15` | `engine25` 側の開通（共有サービスへ `uninstall()` を新設・`install()` の自己修復化・毎ビルドの貼り直し・`verify()` のピン2件・LTX 2.5 用の自己診断を新設） | G1 |
| C2 | `cb97613` | アプリ側の開通（**拒否表から受理表へ**・ペイロードの加算・エコーの中継・`GET /status` の真理値表からエンジン固有の例外を削除。`unsupported_features` は 7件 → 6件） | G3 |
| G5 | — | 実機ゲート（実GPU・全11項） | 全合格（記録は [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §77） |
| G8 | — | **オーナーの目視（sage on/off の対を見比べる）** | **全合格（2026-08-25。絵の見比べ4項目・音の聴き取り1項目。記録は [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §77.10）** |

**設計上の要点は4つある。**

1. **LTX 2.3 のコードをそのまま共有し、専用のサブクラスは作っていない。** 2つのエンジンの注意機構の規約（q/k/v が平坦な `(B, S, H*D)` で来る・転置が要らない・head_dim が 128 と 64）が**完全に同一**だからである。**同じ名前で別実装だった第2弾（`keep_resident`）とは、ちょうど逆の関係にある**——本段は**契約も実装も共有**していて、違うのは貼ったラッパーの寿命だけである。
2. **LTX 2.5 は1つのモデルの殻をジョブ間で使い回すので、変換器を組み立てるたびに「剥がして貼り直す」。** 貼りっぱなしにするとラッパーが二重・三重に積み上がり、**無症状のまま遅くなる**（実機のログで実在を確認済み）。`sage` が off のジョブでは剥がすだけで何もしないので、既定のジョブには一切の上乗せが無い。
3. **`install()` は自己修復型で、製品の経路では決して例外を投げない。** 「sage が原因でジョブが死ぬことは絶対にあってはならない」という 2.3 以来の規律を守るためで、生き残ったラッパーを見つけたら ERROR ログを1行出したうえで、その場で剥がしてから貼り直す。
4. **`GET /status` の真理値表から、LTX 2.5 の例外を丸ごと削除した。** それまで `sage_available` はこの系統だけ恒久的に `false` を返していたが、上書きを消したことで**基底のファイル存在チェックが自動的に LTX 2.5 用の仮想環境を見る**ようになり、2つの系統がまったく同じ2経路（ロード前＝ファイル存在チェック／ロード後＝ワーカー自身の import 判定）で答えるようになった。**降格の規律も 2.3 と逐語で同じ**である（sageattention 不在なら 422 にせず `"sdpa"` へ降格し、エコーは `"sage->sdpa"`）。

> **★「`unsupported_features` が減ったこと」と「画面のグレーアウトが解けたこと」は独立である**——この段はその教訓の正本である。設定画面は `unsupported_features` を読んでいないので、`keep_resident`（第2弾）は Settings 画面のコードを1行も変えずモック側の変更だけで灰色が解け、`attention` は **`GET /status` の `acceleration.sage_available` が実探索化されたこと**で解けた。**配列が減ったから解けたのではない。** フロントエンドが機能名を読んで灰色にしているのは `webui/src/shell/useBaseModels.ts` の表だけで、そこに現れる語は `chain` / `retake` / `outpaint` / `v2v` / `a2v` / `end_source` / `reference_video` の7語しかない（`two_stage_hq` / `prune_vaed` はどの表にも無く、画面のどの部品とも結びついていない）。詳細は [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) **§77.9(5)**。

**実測の正本は [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §77 である**（連結生成での倍率と合格線・VRAM の中立性・PSNR〔ピーク信号対雑音比。数値が大きいほど元の絵に近い〕と SSIM）。**1点だけここに残す——速さは形状に強く依存し、512×320 級では効かないか、むしろ遅くなる。**

> **設計上いちばん大きな学びは、「合格線は既知良品を測ってから引く」ということである。** 当初は SSIM の絶対値（平均 0.98／最悪 0.96）を合格線に置いていたが、**すでに実績のある LTX 2.3 の sage 自身が 0.8946／0.8330 でこれを割った**。線の出どころ（PrunaVAED の実測 0.9854／0.9856）が**同じ潜在表現を別のデコーダで戻した比較**だったのに対し、**sage は軌道そのものが分岐した別のサンプル**で、そもそも土俵が違っていた。オーナー再裁定により、**「LTX 2.3 の既知良品からどれだけ離れているか」を幾何ごとに見る相対の帯**へ作り直してある（[`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §77.2(3)）。

#### 続き: 撮り直し（Retake）と素材（末尾）（End source）（2026-08-26）

**高速化の前倒しが打ち止めになったあと、オーナー裁定の段階分けに従って準必須級に入った、その最初の2件である。** 撮り直し（既存クリップの真ん中だけを作り直し、前後の「のりしろ」を凍結したまま元の映像へ重ねて戻せる形で出す機能）と素材（末尾）（手持ちの動画や静止画をチェーンの終点として指定し、そこへ着地するように生成させる機能）は、**どちらも「窓の先頭だけでなく末尾も凍結する」ことを必要とする**ので、その受け皿を作る不活性なコミット（C1）を先に置いてから、1つずつ開通させた。**これで `POST /generate/chain` のスキーマが表現できるモードは、すべて LTX 2.5 に経路を持った。**

| 段 | コミット | 内容 | 通過したゲート |
|---|---|---|---|
| C0 | `dc480f4` | フロントエンド。Edit タブの**サブタブ単位**のグレーアウトと、**無効モード行きの右クリックを仮オブジェクトの書き込みより前で弾く**既存バグの修正 | vitest・型検査 |
| C1 | `c6de7fb` | `engine25` の凍結帯に**尾側**を足し、帯の強さと先頭マーカーの判定を切り離した（**不活性**。尾幅の既定は0） | G1 |
| C2 | `18a501e` | **撮り直しの開通**（アダプタ／ワーカー／`chain25` 本体／モック） | G2（実GPU・6本） |
| C3 | `a53d086` | **素材（末尾）の開通**（同じ4箇所。chain 系で最後のモード名） | G3（実GPU・9本） |
| G5 | — | 実機ゲート（実GPU・一時サーバー・4項目） | 全合格（記録は [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §78） |
| G8 | — | **オーナーの目視・試聴** | **全合格**（2026-08-30。AviUtl2 の操作パネルからの実機生成で、撮り直しを4観点・逆順 Chained を3観点。結果は [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) **§78.14**。素材は `outputs/ltx25-rtes-gate/g8_material/`） |

**設計上の要点は5つある。**

1. **幾何計算（`chain_math.py`）は100%無改修である。** 逆順の3本の表も、凍結のマスク値も、音声帯の走査も、stage-2 のタイル帯も、**2.3 の実装がそのまま両エンジンから呼ばれている。** したがって「どこを凍結するか」という答えは2つのエンジンで**同一**であり、本段が実装したのは「その答えをどう書き込むか」だけである。**これは第1段（連結生成）で「帯の凍結が 2.3 とビット単位で同一になった」ことの、いちばん大きな配当である。**
2. **撮り直しは専用のパイプラインを作らず、`chain25` の「1クリップ」として実装した。** stage-1 は初期テンソルをゼロで作って頭と尾にだけ元の潜在を書き、stage-2 では全解像度で再エンコードしたものから書き直して**再凍結**する。**stage-2 の再凍結は省略できない**——stage-2 は sigma[0]≒0.909 で再ノイズを掛けるので、stage-1 だけの凍結は消し飛ぶ。
3. **素材（末尾）は「生成順ループ」で実装した。** stage-1 のループが `layout.seg_generation_order` を回り、`i`（タイムライン添字）と `order_idx`（待ち行列の何番目か）を役割で分けた。**進捗の報告には `order_idx` を使う**——受け側は分数で進み具合を計算するので、逆順でタイムライン添字を報告すると**進捗バーが逆走する**。**逆順以外では順番が `[0..n_seg)` なので、既存のチェーンは1バイトも変わらない。**
4. **撮り直しの窓の符号化には、専用のタイル上限が要った。** このエンジンで**唯一「素材の全体を、2回」読む経路**（stage-1 用の半解像度と stage-2 用の全解像度）であり、**LTX 2.5 の自動タイル化はこの負荷を想定していない**——畳み込みデコーダ版の映像 VAE では縦横比だけを見る分岐へ解決し、**空き VRAM を一度も読まない**。アプリが許す最悪の点で自動タイル化が作るのは単一の 8.66 GiB の確保で、16 GiB のカードで起きるのは失敗ではなく**こぼれ**である（予約プールが共有システムメモリで裏打ちされ、ジョブが這うように遅くなる）。**面積の予算（448×384）で大きいほうの軸だけを絞る**という形にし、予約 26.6 GiB を 13.6 GiB へ落とした（実測は 27,190 MiB → 13,926 MiB。**MiB を GiB へ直すのは ÷1024 である**）。**時間軸を絞ってはならない**——その軸が映像 VAE の因果的な受容野を担っており、短い窓で4デシベル近く落ちる。実測表は [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §78.5、コードの正本は `engine25/chain25.py` の `RETAKE_ENCODE_TILE_AREA_BUDGET` である。
5. **先頭フレームのキーフレーム印の述語は、理論ではなく実測で決めた。** 敵対的レビューは「印が正しいのは**タイムライン位置0**のときだけ」と論じたが、**2シードの実測が逆の結果を出し**（位置基準が 1.5〜2.2 dB 負ける）、**監督裁定**で**キャリー基準**（`clear_kf and layout.seg_head_source[i] is not None`）を採った（**2026-08-30にオーナーが追認済み**）。出荷構成での再判定は **A（キャリー）32.912 dB 対 B（位置）30.236 dB、差 +2.676 dB で合格**である。**正順では 2 つの述語が同じ集合を選ぶので、既存のチェーンの SHA は1つも動かない。** 顛末は同 §78.7、**この判断の実測根拠の正本はコードのコメント**（`engine25/chain25.py` の `seg_clear_kf` の代入行の直前）である。**この監督裁定（および R-1 の天井の読み）は、2026-08-30 にオーナーが追認して確定した**（同 **§78.15**）。**G8 の合格宣言にはこの2件についての明示の言葉が含まれておらず、追認は別の出来事として出ている**（同 §78.14(4)）ので、**いまは「オーナーがキャリー基準を選んだ」と読んでよい**——見直すなら、その裁定を覆す話として起票すること。

**フロントエンド本体に手が入った点で、高速化の3弾とは違う段だった。** `keep_resident`（第2弾）と `sage_attention`（第3弾）のときに触ったのはモックだけだったが、**本段では Edit タブをサブタブ単位で灰色にできる仕組みそのものが新たに要った**——開通後の Edit タブは「撮り直しと Outpainting のどちらか一方でも動けば生きたまま」になるので、タブ単位のグレーアウトでは Outpainting だけを閉じられない。**その下地を作る過程で、今日でも再現する既存バグが1つ見つかって直っている**（タイムラインの右クリックは AviUtl2 が描くのでタブの灰色が効かず、無効モードへ向かうルートが**仮オブジェクトを書いてから**タブを戻していた。C0 は書き込みより前でルートを弾く）。

**実測と既知の制限の正本は [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §78 である**（凍結の証明・境界の一致度・VRAM・固定ベンチマークの SHA-256 一致・**出荷中のアダプタが組み立てるペイロードに `retake` / `end_source` の鍵が1つも無いことの直接測定**は同 §78.1〜§78.8。**既知の制限3件は同 §78.9**——①逆順モードの音声の継ぎ目が LTX 2.3 より劣化する〔**2026-08-30 の目視ゲートでオーナーが実際に聴き取ったうえで「想定の範囲内なので合格」と裁定済み＝劣化は仕様として受け入れられており、残るのは原因の究明だけである**。同 §78.14(3)〕・②73フレームの窓では真ん中を作り直す余地がほとんど無い・③モックのクリップ数の下限が実 API とずれている。3件とも台帳 [`PENDING_TASKS.md`](PENDING_TASKS.md) へ起票済み）。API 契約への反映は [`../Videomni_Backend_Specification.md`](../Videomni_Backend_Specification.md) §6.10(a)・(b)・(f)。

#### 続き: 画角拡張（Outpainting）（2026-08-29）

**準必須級の最後の1件で、LTX 2.5 が断っていた「モード」はこれで1つも無くなった。** 画角拡張（Outpainting＝動画の外側へ絵を描き足してキャンバスを広げる機能）が LTX 2.5 でも走る。`GET /models` の `unsupported_features` は4件 → 3件になり、**残る3件はいずれもモードではなく、エンジンが持たない機能である**（`two_stage_hq` / `nag` / `prune_vaed`）。

**方針は「公式の LTX 2.5 ワークフローの忠実な移植」である。** 台帳が2026-08-25の棚卸しで出していた旧方針（潜在空間のマスクで描き足す範囲を指定する新設計）は**棄却した**——Web の一次情報を当たり直したところ、**公式の2.5ワークフローそのものが「2.5の本体＋2.3が配布している In/Outpainting IC-LoRA」という世代をまたぐ組み合わせ**で、**LoRA を使わない公式経路は存在しなかった**からである。つまり2.3の作り方は「2.5では成り立たない古いやり方」ではなく、**いまも公式のやり方**だった。経緯は [`PENDING_TASKS.md`](PENDING_TASKS.md) §3-102 の判断材料(10)。

**方式の骨子は5つある。**

1. **アプリ層は1行も変えていない。** 緑のキャンバス（描き足させたい余白を `#66FF00` の緑で塗った動画。緑は前処理の対象ではなく「ここを描け」と伝えるための目印である）を作るのは `services/video_io.py` の `pad_green_mp4` で、**これは元からエンジン非依存**である。APIの `OutpaintSpec`・5つの検査・キャンバス128倍数・保持領域256画素の下限も、2.3のままである。
2. **新しいモジュールは1本だけ**（`engine25/outpaint25.py`）。このエンジンの単発生成は「1回の呼び出しで stage-1 と stage-2 を通す」黒箱なので、**段の間へ割り込めない**——だから連結生成と同じく、二段を手で駆動する薄いドライバを別に置いた（2.3が `engine/pipeline/outpaint_pipeline.py` を持っているのと同じ理由である）。中身は**借用だけで、複製は1つも作っていない**: 幾何とブレンド（`engine/outpaint/canvas.py`・`pyramid_blend.py`）は2.3・API検証器・モックと共有の実体そのもの、その他は `chain25` と `reference25` から取っている。
3. **画素の扱いが2.3とは別物なので、8ビット整数と浮動小数の境界を4箇所で明示した。** LTX 2.5 の復号は0〜1の浮動小数を返し（形は「フレーム・縦・横・色」の順）、符号化器も0〜1の浮動小数を要求するが、**ラプラシアン・ピラミッドのブレンドは8ビット整数しか受け取らない**（それ以外は明示的に拒否する）。**ブレンドと2倍拡大はすべて8ビットのまま行い、境界の4箇所だけで型を変える。**
4. **全解像度の再符号化は `set_conv3d_memory_format` で囲む。** 前段の関門（G0-d）の実測で、素のまま通すと予約VRAMが **27,188 MB** まで膨らんでこぼれることが分かった（1280×768×241フレーム）。原因はタイルの大きさではなく **bf16 の3次元畳み込みが im2col 経路へ落ちること**で、重みの並びを変えるだけで **6,174 MB**・**52.3秒 → 19.0秒**になり、往復のPSNR（ピーク信号対雑音比。元の絵との違いを1つの数値にまとめた指標で、単位はdB。大きいほど元に近い）は **0.002 dB** しか動かない。**2.3が同じ場所で使っているタイル面積の予算は採らなかった**——そちらは 13,922 MB までしか落ちないうえ **0.378 dB** 劣化する。**これは意図的差分として記録してある。**
5. **音声は元の波形を凍結する。** 2.3と同じ式（凍結する帯のマスクを 1.0 にする）1本で、全部凍結・一部凍結・凍結なしの3通りを扱う。**凍結が無いときの証明は `None` であって `0.0` ではない**——何も凍結していないのに「差は 0.0 だった」と書けば、測っていないものを測ったことになる。**stage-2 の音声の初期値だけは2.3と違う**（2.5はstage-1の出力を引き継ぎ、2.3はゼロから作り直す）。**音声がすべて揃っていれば両者は一致し、差が出るのは音声が足りないときだけ**なので、その腕を2.3の数値と絶対比較してはならない。

**実測の正本は [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §79 である**（8腕すべて合格・緑の残存率が全腕全フレーム全パッド帯で 0.000000・境界勾配比・凍結の証明・VRAM・別プロセスでの mp4 バイト一致・固定ベンチマークの SHA-256 一致・実機7項目）。API 契約への反映は [`../Videomni_Backend_Specification.md`](../Videomni_Backend_Specification.md) §6.10(a)・(b)・(d)。**1点だけここに残す——実機ゲートで観測した所要時間（LTX 2.3 が 108.2秒・LTX 2.5 が 77.2秒）を「2.5 のほうが速い」と読んではならない。** 交互対比較でなく、2.3 側はワーカーを載せ替えた直後の1本目（コールドキャッシュ込み）なので、速度計測の作法（同 §43.6）を満たしていない。**両エンジンで同じ依頼が完走したことの記録として読むこと。**

**同じ 2026-08-29、この画角拡張の出力から「新しく描かれた領域全体の淡い緑かぶり」を取り除いた（「脱緑ブレンド」）。** **ブレンドへ渡すキャンバスの余白（pad帯）を、その同じコマの生成画素で先に置き換える**という**約25行**の修正で、**LTX 2.3 と LTX 2.5 の両方へ同時に入れてある**（`engine/` の凍結に対するオーナー承認済みの例外）。**設計上の要点は「混ざること自体は止めていない」ことである**——ラプラシアンピラミッドのマスクは粗い段では pad帯の奥でも 0.76 にしかならず、キャンバスの低周波の色は生成領域の奥まで必ず混ざる。本修正が変えたのは**混ざる中身**であって、緑のセンチネル `#66FF00` の代わりにモデルが描いた絵そのものが染み出すようにした。**モデルに渡す条件付けは緑のキャンバスのままである**——公式LoRAはその色ちょうどを描き替えるよう学習されているので、**センチネルの学習は1ビットも損なわれていない。** 実GPUゲートは**両エンジンとも取り直して全項目合格**で、**副作用は無かったどころか、残すはずだった元映像の再現もよくなった**（LTX 2.5 の基準腕で 36.59 → **38.11 dB**、LTX 2.3 の W2 で 33.32 → **37.81 dB**）。**判定線は1つも動かしていない。** 正本は [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) **§79.10**。

**オーナーの目視ゲートは、脱緑で出力が全面的に変わったため取り直し（G8′）になり、2026-08-29 に合格した**（同 **§79.11**。脱緑後の素材の目視と、AviUtl2 の操作パネルからの実機生成の両方）。**これで画角拡張の LTX 2.5 開通は完結した。** **裁定を依頼していた2点（ぼかしの既定値 5/2 対 5/6、stage-2 の2ステップ対3ステップ）も、同じ 2026-08-29 にオーナーの明示の裁定が出て、どちらも据え置きで確定した**（「どちらも今のままでいい」。裁定の正本は [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) **§79.12**、判断材料は同 §79.11(3)）。既定は 5/2 と2ステップのままなので、`api/models.py` の既定値もフロントエンドの送信値も変えていない。**これで §79 に未決の項目は1つも残っていない。**

**既知の制限は2つで、正本は [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §79.7 である**（①画角拡張に固有のメタデータ〔音声の凍結の証明・音声の分岐〕が `metadata.json` に届かない——アプリ層の改修が要るため本段の範囲外。**VRAM のピークは通常どおり書かれる**。②進捗が50%付近で長く止まって見える——**2.5 固有ではなく、単発生成の進捗計算が復号を数えないという両エンジン共通の性質である**）。

#### 続き: 非CFGネガティブプロンプト（NAG／VSF）（2026-08-30）

**これで LTX 2.5 の「いま着手できる課題」は1つも無くなった。** ネガティブプロンプト（「こういう絵にはしないでほしい」と言葉で指定する機能）が LTX 2.5 でも効く。`GET /models` の `unsupported_features` は3件 → **2件**になり、**残る2件（`two_stage_hq` / `prune_vaed`）はどちらも外部要因待ちである。**

**位置づけを正確に書いておく。** **NAG と同じ設計は KJNodes の `LTX2_NAG` ノードとして LTX-2 世代で稼働している**（先行事例がある）が、**LTX 2.5 での稼働報告は調査した範囲で世界に1件も無く、VSF を LTX 系へ適用しているのは本プロジェクトが世界で唯一である**（VSF 論文の実証は Wan 2.1）。**「2.3で動いているものを運ぶ」作業であると同時に、前例の無い組み合わせの検証でもある**——機械ゲートを厚くし、実GPUの腕を11本立てたのはそのためである。

**実装差は4点だけで、それ以外はすべて2.3から `import` している。** 借用したのは7つ——`NagParams` / `VsfParams` / `NagState` / `nag_combine` / `_cross_attn_modules` / `_MassLogBudget` / `_log_negative_mass` である。**書き下ろしたのは、置換する `Attention.forward` だけ**で、その4点は次のとおり。

1. **`preattention_function` を1回だけ呼び、K を連結して渡してから切り分ける。** 素直に2回呼ぶと **Q の正規化結果を二重に確保する**（実用サイズで約 +230MB）ためである。**等価性は2本の脚で支えている**——①テキストのクロス注意には位置埋め込み（`pe`）が絶対に来ない（互換ピン(14d)のソース側で固定し、置換版自身も非 `None` を拒否する）・②`q_norm` / `k_norm` が `torch.nn.RMSNorm`＝最後の次元だけを見る正規化なので、連結してから切り分けても別々に正規化したのと同じ数になる（`install` が96モジュールぶん実行時に `isinstance` で確かめる）。**どちらかが崩れたときの退避路は「preattention を2回呼ぶ」で、これは数学的に正しい正規の道である**（単に +230MB 高いだけ）。
2. **`attention_function` は4引数で呼ぶ。** 2.3 の5引数（末尾に `mask`）は、2.5 が載せうる FlashAttention 3／4 の呼び出し可能オブジェクトに対して `TypeError` になる。そもそも `mask` があるときは別のスロットへ回る設計なので、渡す先が無い（互換ピン(14b)）。
3. **per-head ゲートは公式スロット `gated_attention_function` を通す**（2.3 は4行を自前で持っていた）。**順序は「混合 → ゲート → 出力射影」で、これは互換ピン(14c)が固定している。**
4. **fail-loud のガードは2.3と同一集合**（`context` が `None`・`pe`・`k_pe`・`mask`・`perturbation_mask`・`all_perturbed`）。**元の `forward` を保存していないので退避先が無く、推測で続けると「それらしい形の、間違った計算」になる。**

**取り付け・取り外しは SageAttention と同じ流儀に揃えた。** 2.5 はモデルの殻をジョブ間で使い回すので、**毎ビルドで無条件に剥がしてから貼り直す。** `uninstall` は `attn.__dict__.pop("forward")` だけを行い、**元の `forward` は保存しない**——保存すると、殻の使い回しで**次のビルドの閉包が前のビルドの閉包を「元」として捕まえ、パッチが1段ずつ入れ子になる**（しかも入れ子でも数の形は正しいので静かに壊れる）。**テンソルには一切触れず、例外も投げない**（`dispose()` 後の殻は全パラメータが `device="meta"` である。`_unpatch_block_swap` が大文字で書いているのと同じ規律）。**剥がした枚数が0でないのは、常駐ワーカーでは正常であって欠陥ではない**ので INFO で記録する。

> **★2.3側の private な名前4つは、改名してはならない。** `_cross_attn_modules` / `_MassLogBudget` / `_log_negative_mass` と、それらが読む `_` 始まりの定数（`_MASS_LOG_BUDGET`）は、**先頭のアンダースコアが「2.3の公開APIではない」という意味であって「使われていない」という意味ではない**——**`engine25/neg_prompt25.py` がこれらに依存している。** **2.3側でこの4つを改名・移動・削除するときは、必ず `engine25/neg_prompt25.py` を同時に直すこと。** 同ファイルの docstring にも同じ趣旨を明記してある（本テーマでは2.3側のファイルを0行しか触っていない）。

> **★`Builder` を使わない経路（`StreamingModelBuilder`）では、この取り付けは走らない。** 取り付け点は `Ltx25DiffusionStage._build_transformer` の末尾（SageAttention の隣）なので、**その `_build_transformer` を通らない組み立て方をした場合、ネガティブプロンプトも SageAttention も掛からない。** **これは SageAttention と同じ前提であり、本テーマが新しく持ち込んだ制約ではない。** 現在の出荷経路はすべて `_build_transformer` を通るので実害は無いが、**将来ストリーミング側の組み立てを使うようになったら、両方の取り付けを同時に見直すこと。**

**負のプロンプトの符号化は「リストの末尾に1件足して、末尾を切り落とす」方式である。** 公式のエンコーダは全プロンプトを固定長1024へ詰めて1つのバッチにするので、**追加コストは Gemma を1行ぶん余計に通すだけで、モデルの読み込みは増えない。** **要求が無いジョブは `super().__call__(prompts, **kwargs)` をそのまま通る**——「同等の経路」ではなく**同じ行**で、これが「使わないジョブが1バイトも変わらない」ことの構造的な根拠である。**VSF のときだけ、負のコンテキストを実トークン数まで切り詰める**（符号化器の返す列の後ろは学習済みの register 埋め込み＝実データなので、符号反転するとプロンプトと無関係な強い反発が混入する）。**先頭を切り出せばよいことの根拠は 2.5 の実体で書いてある**——`embeddings_processor._compute_right_pad_order`（有効トークンを前へ寄せる安定ソート）。**2.3 は同じ保証をコネクタ内部の再パックから得ているが、2.5 では1つ上の層で起きる**（結論は同じで、出典が違う）。

**F9 の正直な切り分け**: バッチが1行から2行になっても、**パディングは常に1024固定・行間の相互作用なし・乱数の消費なし**である。**したがって変わりうるのは行列積のカーネル選択によるビットだけで、`alpha=0` での end-to-end のビット一致は主張していない**（恒等短絡そのものは自己診断が `is` 比較で証明している）。**保証しているのは「要求しないジョブの経路が今日のままであること」である。**

**実測の正本は [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §80 である**（11腕すべて合格・**A0 が凍結ベンチマーク B9-off と SHA-256 完全一致**・**A10 が9本の NAG／VSF／sage ジョブを挟んだ常駐ワーカーでもなお A0 と完全一致＝取り外しが実機で本当に走っていることの証拠**・テキストエンコーダ窓のピーク・VSF の負側 softmax 質量と `vsf_scale` の上限10の妥当性・A8 で INT8 カーネルの latch が作動しないこと）。API 契約への反映は [`../Videomni_Backend_Specification.md`](../Videomni_Backend_Specification.md) §6.10(a)・(b)・(d)・(f)。

**既知の制限は2つで、正本は [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) §80.6 である**（①**`alpha=0` にしても出力は「なし」とビット一致しない**——**無効化はチェックボックスを切ること**。②「無視＋ログ」に残った2件〔`guidance_scale` / `num_inference_steps`〕は、実運用ではログ行が出ない）。

> **★このゲートで、本テーマとは無関係な既存の不具合を1件見つけた。** **画角拡張のジョブの直後に、同じ常駐ワーカーで普通の生成を投げると失敗する**（`RuntimeError: Inplace update to inference tensor outside InferenceMode is not allowed.`）。**原因は囲みの食い違いで、同じ 2026-08-30 に推論モードを統一して直し、台帳 [`PENDING_TASKS_CLOSED.md`](PENDING_TASKS_CLOSED.md) §3-124 としてクローズした**（機構の記述と回帰証明の正本は [`VERIFICATION_LOG.md`](VERIFICATION_LOG.md) **§81**。**「共有コードに触るので 2.3 側の回帰確認が必須」という当初の但し書きは、この直し方には当たらない**——同 §81.4）。

---

## 9. 既存コードの写像表

**エンジン系統を足すときは既存コードを書き直さない。「そのまま使い回すもの」と「エンジン系統に固有のもの」に仕分けるだけである。**

### 9.1 エンジン非依存のもの（そのまま流用する）

| 資産 | 場所 | 使い回せる理由 |
|---|---|---|
| ワーカープロトコルの骨格 | `services/engines/ltx/adapter.py`（`@@LTX@@` 接頭辞による行フレーミング、`_read_frame` のタイムアウト処理、stderr の回収） | プロセス間の通信方式であって、モデルの中身と無関係 |
| モデル名の解決とエラー設計 | `services/model_registry.py`（名前→パスの解決、`MODEL_NOT_FOUND` / `MODEL_FILE_MISSING` / `MODEL_INCOMPATIBLE` の3段のエラー） | 「名前をパスとして受け取らない」「見つからなければ黙って落とさず404」という設計はエンジンに依存しない |
| 軽量な事前チェック | `services/model_registry.py` の `precheck_model_file` | GGUFのマジックナンバー確認と safetensors ヘッダの妥当性確認。§2.3 のKV読み取りもここが返す |
| `PipelineManager` の状態機械 | `services/pipeline_manager.py` の5状態と、その遷移 | どのエンジンでも「未ロード→読み込み中→準備完了→実行中／エラー」は同じ |
| 記述ファイル基盤 | `scripts/manifests/*.json` と `scripts/install_ltx.ps1` の読み込み・検証・移行の仕組み | 「スクリプトはモデル名を持たない」形になっている。§4 はこの上に足すだけ |
| ジョブ実行中のガード | `api/pipeline.py` の409 `JOB_BUSY` | §6.2 のガード(1)がそのまま流用する |

> **mock バックエンドはエンジン非依存ではない。** 実物の mock は**LTXの出力の形そのものを模して**おり、クリップ連結の窓計算（`chain_math.py`）まで使って LTX と同じフレーム規約の結果を組み立てる。したがって汎用側へは切り出さず、`ltx` アダプタの中に置いたままにしてある——`ltx25` アダプタはそれを**継承して**使っている（LTX 2.5 も同じ `8n+1` 規約を共有するため、模造の骨格がそのまま通用した）。真に別規約の系統（Wan 等）が来たときに、改めて判断する。

なお `services/ltx_runner.py` は再エクスポートのための薄い層（shim）として残っているので、旧パスを指す既存の import はすべて生きている。

### 9.2 エンジン系統に固有のもの（アダプタ側へ隔離する）

| 資産 | 場所 | 隔離する理由 |
|---|---|---|
| 読み込みペイロードの組み立て | 各アダプタの `_build_load_payload` | 項目名（`gguf_transformer_path`・`component_video_vae_path` ほか）がそのエンジンの構成そのもの。**キー集合と挿入順が契約である**（§5.2） |
| 実バックエンド可否の判定 | 各アダプタの `_real_available` | 「そのエンジンが要るファイル一式＋エンジン用python＋ワーカー」が揃っているかという、構成に完全に固有のゲート |
| 低VRAM制御のつまみ | LTX 2.3 は `block_swap_blocks_on_gpu`・`vae_spatial_tile_size`・`vae_temporal_tile_size` と `services/low_vram.py`、LTX 2.5 は `blocks_on_gpu`・`te_layers_on_gpu`・`cache_weights` | DiT ブロック構造とVAEのタイル方式に固有で、系統ごとに機構そのものが違う |
| 扱える機能の範囲 | 各アダプタの `UNSUPPORTED_FEATURES` と拒否関数 | §5.6。エンジンの事実であってエンドポイントの事情ではない |
| 推論パイプライン一式 | `engine/`（ltx）・`engine25/`（ltx25） | モデル構造そのもの。それぞれの仮想環境側に閉じたままでよい |
| フレーム規約の定数 | `chain_math.py` の `VIDEO_TIME_FACTOR` / `AUDIO_LATENTS_PER_SEC` | LTXの映像VAE（時間8分の1圧縮）と音声レイテント（毎秒25）の実装値。**現在の2系統はどちらも同じ規約なので隔離していない**（§7） |

---

## 10. 未決事項

**本書に残っている未決事項は無い。** 今後の課題は台帳 [`PENDING_TASKS.md`](PENDING_TASKS.md) §3 側で管理する（**どの項目がいくつ生きているかは台帳 §3 を見ること**。件数と項目番号を本書に書かないのは、同じ数が文書ごとに食い違う事故を繰り返したためである）。

判断が必要になったとき参照する現状を1つだけ挙げておく。**LTX 2.5 の映像デコーダは畳み込み版（conv）を既定としている。** 拡散デコーダ版は反復的なノイズ除去を伴うため決定性が壊れる（同じシードでも同じ動画にならない）。ファイル自体は捨てずに `models/LTX25/VAE/diffvae/` へ退避してあり、カテゴリの走査は非再帰なので**自動認識の対象にならない**（置いてあるが選べない、という状態）。採用の可否は §3-103 で判断する。
