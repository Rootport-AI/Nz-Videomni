> **このファイルは実機の `outputs/comfort-calib-2026-09-26/progress.md`（git追跡外）のスナップショットである（2026-09-26複写）。** 正本は実機側にあり、実機側が更新された場合はこの複写も更新する。複写の目的は、git cloneした読者が参照を辿れるようにすること。

# 進行記録（B-3・fp8 の快適上限較正・2026-09-26）



- 02:06 `server start --pid 49264`（監督役が起動したサーバー）。`original_state.json`＝active LTX25・LTX23 `sulphur_distil_fp8mixed`・LTX25 `default`。

- 02:06 `load LTX23 sulphur_distil_fp8mixed`（22.8 秒・選択登録）。02:06〜02:09 `idle-calib 3 分`（LUID 0x0000f1dc・専有 1042.8 MB）。

- 02:10〜02:11 捨てラン `f_warm`（768×512×121・全 on）: completed・`used_matches_request` ok（sage／prefetch／keep_resident／fused／prune_vaed／fp8）。**コミット: 最大 90.51 GiB（79.5%）→ 落ち着き 81.15 GiB（71.3%）**（監視は `scratchpad/calib26/commit_log.csv`・上限 113.82）。停止線 95%＝108.1 GiB まで約 17.6 GiB。

- 運用メモ: `DELETE /jobs/{id}` は実行中ジョブの推論を止めない（取り消し要求のみ）→ 95% 到達時は「完了を待ち次を投入しない」。Tier D は断片 3 本を個別に流し、断片ごとにコミットの関門を通す。

- 02:12 Tier D 開始（断片 `f_b1216` → `f_w46` → `f_w46_r2`、続けて Tier A の計画ファイル 4 本を駆動スクリプト `scratchpad/calib26/drive.sh` で順に流す。計画ファイル間にキュー空の確認・150 秒の冷却・コミット 95% の関門）。

- 02:14 **警戒 90% 超え（記録して続行）**: `f_b1216`（1216×704・[361,361]・窓 22）の stage-1 から stage-2 への移行時にコミット最大 **105.31 GiB（92.5%）**。stage-2 のタイル中は 102.4 GiB（90.0%）。停止線 95%（108.1 GiB）には達していない。

- 02:17 `f_b1216` 完了（rc=0・コミット最大 105.31 GiB＝92.5%・完了後 80.2%）。

- 02:22 **停止線 95% 超え**: `f_w46`（1 回目）の stage-2 デノイズ中にコミット最大 **108.73 GiB（95.5%）**・上限 113.82。計画 §5-9 どおり実行中のジョブは完走させ、完了後の冷却中に駆動スクリプトを止めて **`f_w46_r2` と Tier A の連続投入を中止**。`DELETE /jobs` は推論を止めないため取り消しは使わない。

- 判断（オーナー就寝中・自律）: 705 フレームの連結点 [361,361]（Tier D）は CPU 側に復元映像を最も抱え、fp8 29 GB＋keep_resident の写しと合わせて上限に届く。**Tier D の 2 回目は保留（オーナー報告事項）**。CPU 側の負荷が軽い単発点（最大 369 フレーム）と連結 [25,161]（186 フレーム）は、**1 点 1 計画ファイル**にして「点ごとに CSV のピークを確かめ、95% 以上なら次を投入しない」駆動に切り替えて続行する。1 点でも 95% に達したら全体を止めて報告する。

- 02:27 `f_w46`（1 回目）完了・**規則 v3＝VRAM 溢れ（plateau）**: stage2 窓 +2,126.7 MB／restore −18.7／s23_gap_mid +872.5／s23_stage1 +13.4／s23_gap_tail −18.7（基準点 `f_b1216` 比）。fp8 でも w46@1216×704（38,456 トークン・線の 96%）は Q6_K と同じく stage-2 で溢れる。**2 回目は保留**（コミット停止線のため）。

- 02:27 駆動を `drive2.sh`（1 点 1 計画ファイル・点ごとにコミットのピークを確かめ 95% なら次を投入しない）へ切り替え、2.3 fp8 の Tier A を 1080p（89/161/169/177f）→ 720p（185/345/361/369f）→ 連結 [25,161]（1280×768 基準・1792×1024・1856×1024・1920×1088）の 12 点で開始。768×1280 の腕は削る優先順位①として後回し。

- 02:29 `f_s_1080_b89`（基準点・89f）完了・ピーク 103.53 GiB（91.0%）。点の重さに依らず、捨てラン後の落ち着き（約 81 GiB）から約 22 GiB の一時的な上昇がある。

- 02:30 参考（Get-Process の PagedMemorySize）: ワーカー python 48008＝**67.67 GiB**、他は dwm 1.29・explorer 1.05・python 46804（リスナー）0.86・VS Code 合計 約 1.8・Notion 約 0.9・Discord 0.43 など。ワーカー以外の常駐アプリは合計数 GiB で、コミットの大半は fp8 の重み（CPU 正本 29 GB）＋常駐の写し＋pinned 中継のワーカー分。

- 02:33 **停止線 95% 超え（2 回目）**: `f_s_1080_161`（1920×1088・161f・42,840 トークン＝線の 95%）でコミット最大 **109.18 GiB（95.9%）**。点ごと駆動はこの点の完了後に停止（`f_s_1080_169`・`177`・720p・連結は未投入）。

- 判断: LTX 2.3 fp8 の全 on（keep_resident 含む）は、この機体のコミット上限 113.82 GiB の下では 1080p 級の線の付近を安全に測れない（基準点 89f でも 91.0%）。**2.3 fp8 の腕はここで打ち切り**、結論の材料「全 on の fp8 は 2.3 では線の付近でコミット上限に届く」として記録する。続けて LTX 2.5 fp8 の腕（B-2 の実測ではコミットが 2.3 より軽い: keep_resident 併用ピーク 84.9 GiB）へ進む。

- 02:39 `f_s_1080_161`（1920×1088・161f・42,840＝線の 95%）完了・**規則 v3＝VRAM 溢れ**: stage2 +3,864.8 MB・s23_gap_mid +2,538.1・restore −14.2（基準点 `f_s_1080_b89` 比）。コミット最大 110.59 GiB（97.2%）→ 点ごと駆動が停止。Q6_K（§113）は同幾何 169f で 2 回快適だったので、fp8 全 on は 2.3 で線がはっきり下がる側。ただしコミット 97% と重なるため切り分けが必要。

- 02:41 計画外の追加診断（README §3 の「追加診断点」）: 同じ 1080p を**サーバー既定構成（accel 指定なし＝keep_resident 等を含まない）**で 89f 基準点＋161f の 1 対（ラベル `_d`）。狙い＝fp8 固有の VRAM 溢れか、コミット圧の巻き添えかの切り分け。線の定義（全 on）とは別の構成なので結論の表には「診断」として別枠で載せる。

- 02:44〜02:53 診断 2 点（既定構成＝sdpa・prefetch on・keep_resident off・fused on・vae_mode off）完了: `f_s_1080_b89_d` ピーク 94.80 GiB（83.3%）／`f_s_1080_161_d` **VRAM 溢れ**（stage2 +3,529.5 MB・s23_gap_mid +2,233.4・restore −42.5）・コミット最大 97.44 GiB（85.6%）・専有ピーク 15,865 MB・共有ピーク（s2）5,749.5 MB。全 on の 161f（stage2 +3,864.8・専有 15,911.8・共有 6,078.7）とほぼ同じ形。**結論の材料: 2.3 fp8 の 1080p 161f（42,840）の溢れはコミット圧の巻き添えではなく fp8 固有の VRAM 側の事実**（専有が 16 GB の天井に張り付く）。全 on の線はこの既定構成の境界より下にある。

- 02:55 LTX 2.5 fp8 の腕へ切り替え（load → idle-calib → 捨てラン `g_warm` → 1 点ずつ Tier A）。2.3 fp8 の既定構成での降りる段（153f→145f→137f）は 2.5 の後に時間があれば診断として追加する。

- 02:54〜02:57 LTX 2.5 fp8 へ切替（load 10.7 秒）・idle-calib 3 分。02:58 捨てラン `g_warm` 完了・コミット最大 79.20 GiB（69.6%・2.3 fp8 より約 11 GiB 軽い）。

- 03:02 `g_s_1080_b89`（基準点）完了・ピーク 94.91 GiB（83.4%）。03:09 `g_s_1080_161`（42,840＝線の 95%）**VRAM 溢れ**: stage2 +920.2 MB・restore −17.9・p25_30_decode_encode −17.9・専有ピーク 15,944.5 MB・共有ピーク（s2）2,119 MB・コミット最大 100.04 GiB（87.9%）。2.5 でも fp8 は線の 95% で溢れる（2.3 の +3,865 MB より小さい）。

- 03:16 `g_s_1080_169`（44,880＝線上）**VRAM 溢れ**: stage2 +1,574.7 MB・restore −8.9・コミット最大 100.59 GiB（88.4%）。161f（+920）→169f（+1,575）と単調に増える。03:20 `g_s_720_b169`（基準点）完了・ピーク 93.99 GiB（82.6%）。

- 03:27 `g_s_720_361`（44,160＝線の 98%）**VRAM 溢れ** stage2 +1,303.5・restore −31.6（ピーク 88.2%）。03:35 `g_s_720_369`（45,120）**溢れ** stage2 +1,623.4・restore −13.8（ピーク 88.4%）。720p でも fp8 は線の 98% で溢れる。Tier B は 1080p 153f→145f・720p 353f→345f→329f と降り、最初に快適が出た段で 2 回目（Tier A の残り＝896×1152・連結 [25,161] の完了後に自動投入）。

- 03:39 `g_s_896_b161`（基準点）完了・ピーク 82.6%。03:46 `g_s_896_345`（44,352＝線の 99%）**VRAM 溢れ** stage2 +1,376.4・restore +0.1（ピーク 88.3%）。3 幾何とも fp8 は線の 95〜100% で stage-2 が 0.9〜1.6 GB 溢れる。Tier B の第 2 弾（896×1152 329f→313f→297f・連結 1984×1024→1920×1024）を第 1 弾の後に自動投入。

- 03:54 `g_s_896_353`（45,360）**溢れ** stage2 +1,692.2・restore −26.7（ピーク 88.6%）。連結の基準点 `g_c_b1280x768` 完了（03:58）。Tier B の 2 回目候補（各降りる段の `_r2`）を事前に生成・preflight。

- 04:03 `g_c_1856x1024`（連結 [25,161]・窓 22・40,832＝線の 91%）**溢れ** stage2 +533.7・restore −60.9（ピーク 87.6%）。連結は 91% でも溢れるため、第 2 弾の連結の降りる段を 1984×1024／1920×1024（どちらも上）から **1792×1024（39,424）→1728×1024（38,016）→1664×960（34,320）** に差し替え。

- 04:11 Tier A（2.5 fp8）完了。`g_c_1920x1088`（連結・44,880＝線上）**溢れ** stage2 +1,527.5・restore −60.8（ピーク 88.6%）。**2.5 fp8 の Tier A は内側の点がすべて溢れ（1080p 161/169f・720p 361/369f・896×1152 345/353f・連結 1856×1024／1920×1088）**、基準点はすべて正常。第 1 弾の降りる段（1080p 153f→145f・720p 353f→345f→329f）が自動開始。

- 04:17 `g_s_1080_153`（40,800＝91%）**溢れ** stage2 +536.5（ピーク 87.5%）。04:22 `g_s_1080_145`（38,760＝86%）**快適** stage2 −35.9・restore −9.1（ピーク 86.9%）。2.5 fp8 の 1080p は 145f 快適／153f 溢れが境界候補 → 145f と 153f の 2 回目を第 3 弾で投入。
- 04:29 `g_s_720_353`（43,200＝96%）**溢れ** stage2 +1,001.4。04:35 `g_s_720_345`（42,240＝94%）**溢れ** stage2 +785.4。720p は 329f（40,320）が 3 段目。1080p が 86%（38,760）で快適になったことから、720p にも 4 段目 313f（38,400＝86%）を追加診断として用意。
- 04:40 `g_s_720_329`（40,320＝90%）**快適（ただし stage2 +297.0 MB＝閾値 300 のすれすれ）**・restore −4.7（ピーク 87.5%）。第 1 弾完了。720p は 329f の 2 回目と 313f（38,400）を第 4 弾で投入（329f が割れたら 313f を境界の下端候補に）。第 2 弾（896×1152 329/313/297f・連結 1792×1024／1728×1024／1664×960）が自動開始。
- 04:47 `g_s_896_329`（42,336＝94%）**溢れ** stage2 +776.6・restore −26.4（ピーク 88.0%）。
- 04:52 `g_s_896_313`（40,320＝90%）**溢れ（stage2 +302.3 MB＝閾値 300 のすれすれ）**・restore −0.1（ピーク 87.5%）。720p の 329f（同じ 40,320 トークン・+297.0＝快適側すれすれ）と対をなす。313f の 2 回目を第 5 弾に含める。
- 04:57 `g_s_896_297`（38,304＝85%）v3′＝clean stage2 -35.8 MB・restore -8.9（ピーク 86.9%）。
- 05:02 `g_c_1792x1024`（連結・39,424＝88%）v3′＝clean stage2 +88.7 MB・restore -53.2（ピーク 87.3%）。
- 05:03 連結は 1856×1024（40,832）溢れ／1792×1024（39,424）快適 → 両点の 2 回目を第 6 弾に予約。制御段階（LTX25 `default` 対照 → LTX23 Q6_K 対照 → 2.3 fp8 既定構成の降りる段 153/145/137f → `restore-state` LTX23→LTX25）を第 6 弾の後に予約（`scratchpad/calib26/phase_controls.sh`）。
- 05:07 `g_c_1728x1024`（連結・38,016＝85%）v3′＝clean stage2 -37.6 MB・restore -64.7（ピーク 86.9%）。
- 05:11 `g_c_1664x960`（連結・34,320＝76%）v3′＝clean stage2 -21.4 MB。第 2 弾完了。第 3 弾（1080p 145f・153f の 2 回目）が自動開始。
- 05:16 `g_s_1080_145_r2` v3′＝clean stage2 -35.2 MB（ピーク 87.1%）。
- 05:22 `g_s_1080_153_r2` v3′＝plateau stage2 +536.6 MB。第 3 弾完了（1080p: 145f 快適×2／153f の判定は左）。第 4 弾（720p 329f r2・313f）が自動開始。
- 05:27 `g_s_720_329_r2` v3′＝plateau stage2 +305.7 MB（1 回目は快適 +297.0）。
- 05:27 720p 329f（40,320）は 1 回目 快適 +297.0／2 回目 溢れ +305.7 で**割れ**（境界に採らず 3 回目は走らせない・規約どおり）。720p の快適側は 313f（38,400・第 4 弾で進行中）の結果次第: 快適なら 313f の 2 回目、溢れなら 297f（36,480）→ 2 回目を第 7 弾（第 6 弾の後）に条件付きで予約。制御段階は第 7 弾の後へ繰り下げ。
- 05:33 `g_s_720_313`（38,400＝86%）v3′＝clean stage2 -40.6 MB。第 4 弾完了。第 5 弾（896×1152 297f・313f の 2 回目）が自動開始。
- 05:37 `g_s_896_297_r2` v3′＝clean stage2 -35.4 MB。
- 05:42 `g_s_896_313_r2` v3′＝plateau stage2 +303.2 MB（1 回目は溢れ +302.3）。第 5 弾完了。第 6 弾（連結 1792×1024・1856×1024 の 2 回目）が自動開始。
- 05:47 `g_c_1792x1024_r2` v3′＝clean stage2 +40.0 MB。
- 05:52 `g_c_1856x1024_r2` v3′＝plateau stage2 +512.5 MB（1 回目 +533.7）。第 6 弾完了。第 7 弾（720p 313f の 2 回目）→ 制御段階が自動で続く。
- 05:56 **運用上の事故**: 05:28 に止めたはずの制御段階の待ち受け（第 6 弾の完了待ち）の子プロセス `phase_controls.sh` が生き残っており、第 6 弾の ALL DONE（05:53:39）＋150 秒で 05:56:09 に制御段階（`load LTX25 default` → idle-calib）を開始した。同時刻 05:56:16 に第 7 弾の `g_s_720_313_r2` が投入されたため、この 2 回目は変換器が `default` に切り替わった直後に走った疑いがある（`used_matches_request` で確認し、不一致なら無効として後で fp8 で取り直す）。重複していた正規の制御段階の待ち受け（第 7 弾の完了待ち）はプロセス木ごと停止し、生き残った 1 本だけを進める。教訓: 待ち受けの停止は TaskStop だけでは子が残るので、`taskkill /T` で木ごと止める。
- 05:58 生き残った制御段階（1 本・PID 31564）を進める: `load default` は 05:56:17 に 200（第 7 弾の `g_s_720_313_r2` は 05:56:18 投入＝**`default` で走った疑い**→ `used_matches_request` で不一致なら無効）。制御段階の完了（CONTROLS DONE）後に修正段階を予約: `load LTX25 fp8` → idle-calib → `g_warm2` → **`g_s_720_313_r2b`**（有効な 2 回目）→ `restore-state` LTX23 → LTX25。
- 05:59 `g_s_720_313_r2` は **無効**（`used_matches_request` 不一致: requested fp8／used `default`。判定 clean・stage2 −500.8 は公式 GGUF のもの）。有効な 2 回目は修正段階の `g_s_720_313_r2b` で取る。結果表ではこの行を「無効（変換器不一致）」として扱う。
- 06:08 対照 `b_s_1080_161`（公式 GGUF・全 on・42,840）v3′＝clean stage2 -0.6 MB・restore -43.7（ピーク 66.7%）。同じ点で fp8 は +920。
- 06:13 対照 `b_s_1080_169`（公式 GGUF・44,880＝線上）v3′＝clean stage2 -43.3 MB（ピーク 67.3%）。公式 GGUF は線上で快適（§3-165 と一致）、fp8 は同点で +1,575。(1) 完了 → (2) Q6_K の対照へ。
- 06:30 対照 `a_s_1080_161`（2.3 Q6_K・全 on・42,840）v3′＝clean stage2 +1.2 MB・restore -17.3（ピーク 79.1%）。同じ点で fp8 全 on は +3,865・既定構成でも +3,530。
- 06:35 対照 `a_s_1080_169`（2.3 Q6_K・44,880＝線上）v3′＝clean stage2 -23.8 MB・restore +0.3（ピーク 79.6%）。§113 の「線上は快適×2」と一致（コード変更後も再現）。(2) 完了 → (3) 2.3 fp8 既定構成の降りる段へ。
- 06:47 診断 `f_s_1080_153_d`（2.3 fp8・既定構成・40,800）v3′＝plateau stage2 +2909.0 MB・s23_gap_mid +1664.5（ピーク 71.6%）。
- 06:48 2.3 fp8 既定構成は 153f（40,800）でも +2,909 の大きな溢れ。137f（36,720）でも溢れなら、修正段階の後に追加診断 X3（121f＝32,640・105f＝28,560・既定構成）を条件付きで予約（`x3.log`）。終了後に再度 `restore-state` ×2。
- 06:55 診断 `f_s_1080_145_d`（2.3 fp8・既定構成・38,760）v3′＝plateau stage2 +2001.9 MB・s23_gap_mid +798.0（ピーク 78.4%）。
- 07:02 診断 `f_s_1080_137_d`（2.3 fp8・既定構成・36,720）v3′＝plateau stage2 +1316.7 MB・s23_gap_mid +206.0（ピーク 77.8%）。(3) 完了 → (4) 復元へ。
- 07:03 制御段階完了（CONTROLS DONE）。`restore-state` LTX23（fp8・変更なし）→ LTX25（`default`・200・`state.json` 検証済み: active LTX25／LTX23 `sulphur_distil_fp8mixed`／LTX25 `default`＝開始時の状態）。続けて修正段階（2.5 fp8 の 313f 2 回目）→ 追加診断 X3 → 各段の後に再復元。
- 07:13 修正段階 `g_s_720_313_r2b`（2.5 fp8・38,400）v3′＝clean stage2 -41.4 MB・`used_matches_request` ok=True（ピーク 86.8%）。720p の快適側 313f が 2 回一致。
- 07:22 追加診断 `f_s_1080_121_d`（2.3 fp8・既定構成・32,640）v3′＝clean stage2 -42.2 MB・s23_gap_mid -3.2（ピーク 69.1%）。
- 07:23 2.3 fp8 既定構成の 1080p は 121f（32,640）で快適（−42.2）・137f（36,720）で溢れ（+1,317）。境界を 1 段に絞るため追加診断 X4（129f＝34,680・121f の 2 回目）を X3 の後に予約（`x4.log`・終了後 restore ×2）。
- 07:28 追加診断 `f_s_1080_105_d`（2.3 fp8・既定構成・28,560）v3′＝clean stage2 -15.8 MB（ピーク 77.2%）。X3 完了 → 復元 → X4。
- 07:38 追加診断 `f_s_1080_129_d`（2.3 fp8・既定構成・34,680）v3′＝plateau stage2 +483.3 MB・s23_gap_mid -17.0（ピーク 69.6%）。
- 07:44 追加診断 `f_s_1080_121_d_r2` v3′＝clean stage2 -24.1 MB。2.3 fp8 既定構成の 1080p 境界＝121f（32,640）快適×2／129f（34,680）溢れ。X4 完了 → 最終復元。
- 07:46 X4 完了・最終復元（LTX23＝fp8・LTX25＝default・active LTX25）。**計測終了: 57 ラン・失敗 0**（fp8 2.3 全 on 5・fp8 2.3 既定構成 9・fp8 2.5 全 on 33・対照 8＝default 4／Q6_K 4・捨てラン 6、無効 1）。`make_results_output.md`・`report_output.txt`・`digest.md`・`commit_table.md`・`preflight_output.txt`（85 点）・`harness_diff.txt`（コードは同一・README と節 H のみ差分）・`git_state_end.txt` を生成。駆動スクリプト（drive.sh／drive2.sh／phase_controls.sh／commit_monitor.ps1／digest.py／commit_table.py）とコミット記録 `commit_log.csv` をフォルダへ複写。
