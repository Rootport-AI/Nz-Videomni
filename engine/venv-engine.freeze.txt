# Dependency snapshot of .venv-engine (first-party engine worker interpreter).
#
# WHY: the fork tree that held uv.lock was deleted in Stage 2b, so this file
# is the reproducibility record for the known-good engine venv (Critic C).
# Rebuild reference: engine/engine-venv-pyproject.toml (the resolved lock is gone).
#
# METHOD: generated via importlib.metadata (enumerating installed *.dist-info in
# .venv-engine/Lib/site-packages). `pip` and `uv` are BOTH absent from this
# uv-built venv, so `pip freeze` / `uv pip freeze` were not available; the
# dist-info enumeration is the exact-installed equivalent (name==version).
# Interpreter: CPython 3.12.9  (.venv-engine/Scripts/python.exe)
# Captured: 2026-07-01
#
# NOTE: three distributions are git direct-url installs (NOT PyPI); their
# lines below are bare 'name==version' pins, so the exact source revs are
# recorded here for reproducibility (see engine-venv-pyproject.toml sources):
#   diffusers: git+https://github.com/huggingface/diffusers.git @ 01de02e8b4f2cc91df4f3e91cb6535ebcbeb490c
#   ltx-core: git+https://github.com/Lightricks/LTX-2.git#subdirectory=packages/ltx-core @ 00dc53d3f81c405932f9f16d9c57557de411e702
#   ltx-pipelines: git+https://github.com/Lightricks/LTX-2.git#subdirectory=packages/ltx-pipelines @ 00dc53d3f81c405932f9f16d9c57557de411e702
#
# 2026-07-31 re-addition: a FOURTH distribution, sageattention, is also a bare
# 'name==version' pin below for the same reason (no matching PyPI build), but
# via a direct WHEEL URL, not git:
#   sageattention: https://github.com/woct0rdho/SageAttention/releases/download/v2.2.0-windows.post6/sageattention-2.2.0%2Bcu128torch2.9.1.post6-cp310-abi3-win_amd64.whl
# See scripts/install_ltx.ps1's $engineDirectPins for the authoritative pin.
#
# 2026-07-28 cleanup (PENDING_TASKS.md §3-25): removed 19 entries -- the 10
# fork-era (4ea5c4b) leftovers with zero importers (annotated-doc, fastapi,
# ftfy, ImageIO, imageio-ffmpeg, peft, pynvml, sageattention, triton-windows,
# uvicorn; consumer code deleted in d0d3df5) PLUS 9 transitive orphans that
# became unreachable once those 10 were removed (starlette + click +
# httptools + python-dotenv + watchfiles + websockets, pulled in only by
# fastapi/uvicorn[standard]; wcwidth, pulled in only by ftfy; nvidia-ml-py,
# pulled in only by pynvml). Orphan status was verified against every
# remaining package's Requires-Dist (importlib.metadata METADATA) -- see the
# Wave B1 report for the full reverse-dependency table. sentencepiece and
# protobuf are deliberately KEPT (Gemma tokenizer fallback concern, owner
# decision).
#
# 2026-07-31 (Acceleration feature, D4/STEP 5): sageattention and
# triton-windows are RE-ADDED, two of the 19 above. This time each has a real
# importer -- engine/transformer/sage_attention_service.py -- so this is not a
# repeat of the fork-era dead-weight mistake. sageattention==2.2.0+... below is
# a BARE line (no matching PyPI build exists for this wheel): it is installed
# from the direct wheel URL pinned in scripts/install_ltx.ps1's
# $engineDirectPins, same mechanism as the diffusers/ltx-core/ltx-pipelines git
# pins, and that bare line is what lets `$gitLineRe`-style filtering exclude it
# from the plain `-r freeze` install. triton-windows is a NORMAL line (real
# PyPI package) and installs straight from this freeze like everything else.
#
# 2026-08-30 (PENDING_TASKS_CLOSED.md §3-128): hf-xet==1.6.0 is ADDED. It does
# NOT arrive on its own: huggingface_hub 0.36.2 guards its hf-xet requirement
# with `platform_machine == "x86_64" or "amd64" or "arm64" or "aarch64"`,
# environment markers compare case-SENSITIVELY, and Windows reports "AMD64" --
# so that guard is False here and this freeze is the ONLY install path for the
# Xet transfer backend. Pinning it keeps every machine on the same version.
#
accelerate==1.12.0
annotated-types==0.7.0
anyio==4.12.1
av==16.1.0
certifi==2026.1.4
charset-normalizer==3.4.4
colorama==0.4.6
diffusers==0.37.0.dev0
einops==0.8.2
filelock==3.24.2
fsspec==2026.2.0
gguf==0.18.0
h11==0.16.0
hf-xet==1.6.0
httpcore==1.0.9
httpx==0.28.1
huggingface_hub==0.36.2
idna==3.11
importlib_metadata==8.7.1
Jinja2==3.1.6
ltx-core==1.0.0
ltx-pipelines==1.0.0
MarkupSafe==3.0.3
mpmath==1.3.0
networkx==3.6.1
numpy==2.4.2
opencv-python-headless==4.13.0.92
packaging==26.0
pillow==12.1.1
protobuf==6.33.5
psutil==7.2.2
pydantic==2.12.5
pydantic_core==2.41.5
PyYAML==6.0.3
regex==2026.1.15
requests==2.32.5
safetensors==0.7.0
sageattention==2.2.0+cu128torch2.9.1.post6
scipy==1.17.0
sentencepiece==0.2.1
setuptools==82.0.0
sympy==1.14.0
tokenizers==0.22.2
torch==2.9.1+cu128
torchaudio==2.9.1+cu128
torchvision==0.24.1+cu128
tqdm==4.67.3
transformers==4.57.6
triton-windows==3.5.1.post24
typing-inspection==0.4.2
typing_extensions==4.15.0
urllib3==2.6.3
zipp==3.23.0
