# =============================================================================
# Backend (FastAPI side) dependencies ONLY.
#
# This mirrors [project].dependencies in pyproject.toml for users who prefer
# pip over uv. Install into a PROJECT-LOCAL venv (never the system Python):
#
#     python -m venv .venv
#     .venv\Scripts\pip install -r requirements.txt
#
# NOTE: The LTX inference stack (torch 2.9.1+cu128, ltx-core, ltx-pipelines,
# and the rest of the GPU side) is NOT listed here, and is NOT installed into
# this venv at all. It lives in a SEPARATE engine venv (./.venv-engine), pinned
# by engine/venv-engine.freeze.txt and applied by scripts/install_ltx.ps1,
# because:
#   - torch must come from the PyTorch cu128 wheel index (not PyPI), and
#   - the two stacks cannot share one interpreter (both ship a top-level
#     `services` package).
# Attention backend: PyTorch SDPA on EVERY GPU architecture. Neither xformers
# nor flash-attn is installed or imported; there is no per-architecture branch
# and no `xformers` extra (pyproject.toml declares only the `dev` extra).
# See README "7. LTX 2.3 のインストール".
# =============================================================================

fastapi>=0.110
uvicorn[standard]>=0.29
gradio>=6.19,<7
pydantic>=2.6
pydantic-settings>=2.2
pyyaml>=6.0
pillow>=10.2
python-multipart>=0.0.9
httpx>=0.27
mcp>=1.28,<2

# dev / test
pytest>=8.0
